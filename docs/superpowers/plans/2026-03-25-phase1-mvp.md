# Phase 1 MVP Implementation Plan

> **For agentic workers:**  Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build the Phase 1 MVP — template-based 3D modular shelf designer with auth, save/load, undo/redo, auto-save, and viewport controls.

**Architecture:** Monorepo (pnpm workspaces + Turborepo) with `packages/core` (platform-agnostic types, schemas, templates, utils) and `packages/web` (Next.js App Router + R3F + Zustand + Prisma/SQLite). All state is an immutable JSON scene graph. Docker-first development — Docker setup (Task 1) must complete before any other task.

**Prerequisite:** Docker Desktop installed and running on host machine.

**Tech Stack:** Next.js 15 (App Router), React Three Fiber, Zustand, Prisma + SQLite, Zod, bcrypt + JWT, Tailwind CSS + Radix UI, Vitest, Playwright, Docker + Turborepo

**Design Doc:** `docs/2026-03-23-3d-modeler-design.md`

---

## File Structure

```
3dModel/
├── package.json                          # Root workspace config
├── pnpm-workspace.yaml                   # pnpm workspace definition
├── turbo.json                            # Turborepo pipeline config
├── tsconfig.base.json                    # Shared TypeScript config
├── Dockerfile                            # Multi-stage (dev/build/production)
├── docker-compose.yml                    # Dev environment
├── docker-compose.prod.yml               # Production environment
├── scripts/
│   └── docker-entrypoint.sh              # Dev container entrypoint
├── .env.example                          # Environment template
├── .dockerignore
├── packages/
│   ├── core/
│   │   ├── package.json
│   │   ├── tsconfig.json
│   │   ├── vitest.config.ts
│   │   ├── src/
│   │   │   ├── index.ts                  # Barrel export
│   │   │   ├── types/
│   │   │   │   ├── scene.ts              # SceneNode, Vec3, component params
│   │   │   │   ├── design.ts             # DesignDTO, DesignRecord
│   │   │   │   └── template.ts           # TemplateDefinition, TemplateParams
│   │   │   ├── schemas/
│   │   │   │   ├── scene.ts              # Zod schemas for all SceneNode types
│   │   │   │   ├── design.ts             # Zod schemas for Design CRUD
│   │   │   │   ├── template.ts           # Zod schema for TemplateParams
│   │   │   │   └── auth.ts              # Zod schemas for register/login
│   │   │   ├── templates/
│   │   │   │   ├── registry.ts           # Template registry (metadata + generate)
│   │   │   │   ├── single-shelf.ts       # Single-layer shelf
│   │   │   │   ├── multi-shelf.ts        # Multi-layer shelf
│   │   │   │   ├── standalone.ts         # Standalone shelf (4-post)
│   │   │   │   ├── wall-mounted.ts       # Wall-mounted shelf (2-post)
│   │   │   │   ├── corner-shelf.ts       # Corner L-shape
│   │   │   │   └── l-shape.ts            # L-shape shelf
│   │   │   └── utils/
│   │   │       ├── id.ts                 # nanoid wrapper
│   │   │       └── scene-tree.ts         # findNode, updateNode, addNode, removeNode, duplicateNode
│   │   └── __tests__/
│   │       ├── schemas/
│   │       │   ├── scene.test.ts
│   │       │   ├── design.test.ts
│   │       │   └── auth.test.ts
│   │       ├── templates/
│   │       │   ├── single-shelf.test.ts
│   │       │   ├── multi-shelf.test.ts
│   │       │   └── standalone.test.ts
│   │       └── utils/
│   │           └── scene-tree.test.ts
│   └── web/
│       ├── package.json
│       ├── tsconfig.json
│       ├── next.config.ts
│       ├── vitest.config.ts
│       ├── playwright.config.ts
│       ├── tailwind.config.ts
│       ├── postcss.config.js
│       ├── prisma/
│       │   └── schema.prisma
│       ├── src/
│       │   ├── app/
│       │   │   ├── layout.tsx            # Root layout (Tailwind, fonts)
│       │   │   ├── page.tsx              # Landing page
│       │   │   ├── login/
│       │   │   │   └── page.tsx
│       │   │   ├── register/
│       │   │   │   └── page.tsx
│       │   │   ├── dashboard/
│       │   │   │   └── page.tsx
│       │   │   ├── editor/
│       │   │   │   ├── [id]/
│       │   │   │   │   └── page.tsx      # Edit existing design
│       │   │   │   └── new/
│       │   │   │       └── page.tsx      # Create from template
│       │   │   └── api/
│       │   │       ├── health/
│       │   │       │   └── route.ts      # Health check
│       │   │       ├── auth/
│       │   │       │   ├── register/
│       │   │       │   │   └── route.ts
│       │   │       │   ├── login/
│       │   │       │   │   └── route.ts
│       │   │       │   └── logout/
│       │   │       │       └── route.ts
│       │   │       ├── designs/
│       │   │       │   ├── route.ts      # GET list, POST create
│       │   │       │   └── [id]/
│       │   │       │       └── route.ts  # GET, PATCH, DELETE
│       │   │       └── templates/
│       │   │           └── route.ts      # GET template metadata
│       │   ├── lib/
│       │   │   ├── prisma.ts             # Prisma client singleton
│       │   │   ├── auth.ts               # JWT sign/verify, cookie helpers
│       │   │   ├── with-auth.ts          # Auth middleware for API routes
│       │   │   ├── rate-limit.ts         # In-memory rate limiter
│       │   │   └── env.ts                # Env var validation (fail-fast)
│       │   ├── stores/
│       │   │   └── editor-store.ts       # Zustand EditorStore
│       │   ├── hooks/
│       │   │   ├── use-auto-save.ts      # Debounced auto-save
│       │   │   └── use-auth.ts           # Auth state hook
│       │   ├── components/
│       │   │   ├── editor/
│       │   │   │   ├── EditorShell.tsx    # Three-column layout wrapper
│       │   │   │   ├── Header.tsx         # Design name, save, back
│       │   │   │   ├── ComponentPanel.tsx # Left panel (template list)
│       │   │   │   ├── Viewport.tsx       # R3F Canvas + controls
│       │   │   │   ├── SceneRenderer.tsx  # Recursive scene graph renderer
│       │   │   │   ├── PropertiesPanel.tsx# Right panel (params editor)
│       │   │   │   ├── TemplateParamsEditor.tsx # Template-level params
│       │   │   │   ├── NodeParamsEditor.tsx     # Node-level params
│       │   │   │   └── StatusBar.tsx      # Bottom status bar
│       │   │   ├── meshes/
│       │   │   │   ├── RodMesh.tsx
│       │   │   │   ├── CrossClampMesh.tsx
│       │   │   │   ├── FixedRingMesh.tsx
│       │   │   │   ├── TeeMesh.tsx
│       │   │   │   ├── ShelfMesh.tsx
│       │   │   │   └── registry.ts       # componentRegistry map
│       │   │   └── ui/
│       │   │       └── (empty — toast via inline error messages for MVP)
│       │   └── __tests__/
│       │       ├── lib/
│       │       │   ├── auth.test.ts
│       │       │   ├── rate-limit.test.ts
│       │       │   └── env.test.ts
│       │       ├── api/
│       │       │   ├── auth.test.ts
│       │       │   └── designs.test.ts
│       │       ├── stores/
│       │       │   └── editor-store.test.ts
│       │       └── hooks/
│       │           └── use-auto-save.test.ts
│       └── e2e/
│           ├── auth.spec.ts
│           └── editor.spec.ts
```

---

## Phase A: Foundation (Tasks 1-5)

### Task 1: Docker + Monorepo Scaffolding

> Docker setup comes first so all subsequent tasks can use `docker compose exec web` commands.

**Files:**
- Create: `Dockerfile`
- Create: `docker-compose.yml`
- Create: `scripts/docker-entrypoint.sh`
- Create: `.dockerignore`
- Create: `package.json`
- Create: `pnpm-workspace.yaml`
- Create: `turbo.json`
- Create: `tsconfig.base.json`
- Create: `.env.example`
- Create: `.gitignore`
- Create: `packages/core/package.json`
- Create: `packages/core/tsconfig.json`
- Create: `packages/core/vitest.config.ts`
- Create: `packages/core/src/index.ts`
- Create: `packages/web/package.json`
- Create: `packages/web/tsconfig.json`
- Create: `packages/web/next.config.ts`
- Create: `packages/web/vitest.config.ts`
- Create: `packages/web/tailwind.config.ts`
- Create: `packages/web/postcss.config.js`

- [x] **Step 0a: Create Dockerfile**

Copy the multi-stage Dockerfile from design doc Section 16.2 exactly (base → deps → dev → build → production).

- [x] **Step 0b: Create `docker-compose.yml`**

Copy from design doc Section 16.3. Key: bind mount `.:/app`, named volumes for `node_modules`, `sqlite_data` volume, entrypoint script.

- [x] **Step 0c: Create `scripts/docker-entrypoint.sh`**

```bash
#!/bin/sh
set -e
echo "Installing dependencies..."
pnpm install
echo "Running Prisma migrations..."
pnpm --filter web exec prisma migrate dev --skip-generate 2>/dev/null || \
  pnpm --filter web exec prisma migrate deploy
echo "Generating Prisma client..."
pnpm --filter web exec prisma generate
echo "Starting dev server..."
exec pnpm --filter web dev
```

Make executable: `chmod +x scripts/docker-entrypoint.sh`

- [x] **Step 0d: Create `.dockerignore`**

```
node_modules
.next
.git
*.md
.DS_Store
data/*.db
.env.production
```

- [x] **Step 1: Create root `package.json`**

```json
{
  "name": "3d-modeler",
  "private": true,
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "test:e2e": "turbo test:e2e",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2"
  },
  "packageManager": "pnpm@9.15.0"
}
```

- [x] **Step 2: Create `pnpm-workspace.yaml`**

```yaml
packages:
  - "packages/*"
```

- [x] **Step 3: Create `turbo.json`**

```json
{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "dist/**"]
    },
    "test": {
      "dependsOn": ["^build"]
    },
    "test:e2e": {
      "dependsOn": ["build"]
    },
    "lint": {}
  }
}
```

- [x] **Step 4: Create `tsconfig.base.json`**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "declaration": true,
    "declarationMap": true,
    "sourceMap": true,
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "resolveJsonModule": true,
    "isolatedModules": true
  }
}
```

- [x] **Step 5: Create `packages/core/package.json` and `tsconfig.json`**

`packages/core/package.json`:
```json
{
  "name": "@3d-modeler/core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "nanoid": "^5",
    "zod": "^3"
  },
  "devDependencies": {
    "typescript": "^5",
    "vitest": "^3"
  }
}
```

`packages/core/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./dist",
    "rootDir": "./src"
  },
  "include": ["src/**/*", "__tests__/**/*"]
}
```

`packages/core/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    include: ['__tests__/**/*.test.ts'],
  },
})
```

`packages/core/src/index.ts`:
```typescript
// Types
export type * from './types/scene'
export type * from './types/design'
export type * from './types/template'

// Schemas
export * from './schemas/scene'
export * from './schemas/design'
export * from './schemas/template'
export * from './schemas/auth'

// Utils
export * from './utils/id'
export * from './utils/scene-tree'

// Templates
export * from './templates/registry'
```

- [x] **Step 6: Create `packages/web/package.json`, configs, and Next.js scaffolding**

`packages/web/package.json`:
```json
{
  "name": "@3d-modeler/web",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "@3d-modeler/core": "workspace:*",
    "@radix-ui/react-dialog": "^1",
    "@radix-ui/react-dropdown-menu": "^2",
    "@radix-ui/react-select": "^2",
    "@radix-ui/react-slider": "^1",
    "@radix-ui/react-toast": "^1",
    "@react-three/drei": "^9",
    "@react-three/fiber": "^8",
    "@react-three/postprocessing": "^2",
    "bcryptjs": "^2",
    "jose": "^5",
    "next": "^15",
    "react": "^19",
    "react-dom": "^19",
    "three": "^0.170",
    "zustand": "^5",
    "@prisma/client": "^6"
  },
  "devDependencies": {
    "@playwright/test": "^1",
    "@testing-library/react": "^16",
    "@types/bcryptjs": "^2",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@types/three": "^0.170",
    "autoprefixer": "^10",
    "postcss": "^8",
    "prisma": "^6",
    "tailwindcss": "^3",
    "typescript": "^5",
    "vitest": "^3",
    "@vitejs/plugin-react": "^4",
    "jsdom": "^25"
  }
}
```

`packages/web/tsconfig.json`:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "jsx": "preserve",
    "lib": ["dom", "dom.iterable", "esnext"],
    "module": "esnext",
    "allowJs": true,
    "noEmit": true,
    "incremental": true,
    "plugins": [{ "name": "next" }],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx"],
  "exclude": ["node_modules"]
}
```

`packages/web/next.config.ts`:
```typescript
import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  output: 'standalone',
  transpilePackages: ['@3d-modeler/core'],
}

export default nextConfig
```

`packages/web/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  test: {
    environment: 'jsdom',
    include: ['src/__tests__/**/*.test.ts', 'src/__tests__/**/*.test.tsx'],
  },
})
```

`packages/web/tailwind.config.ts`:
```typescript
import type { Config } from 'tailwindcss'

const config: Config = {
  content: ['./src/**/*.{ts,tsx}'],
  theme: {
    extend: {},
  },
  plugins: [],
}

export default config
```

`packages/web/postcss.config.js`:
```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
```

- [x] **Step 7: Create `.env.example` and `.gitignore`**

`.env.example`:
```bash
JWT_SECRET=change-me-to-a-random-string-at-least-32-chars
DATABASE_URL=file:/app/data/dev.db
```

`.gitignore`:
```
node_modules
.next
dist
*.db
*.db-journal
.env
.env.production
.env.local
.DS_Store
data/
```

- [x] **Step 8: Install dependencies and verify build**

```bash
docker compose exec web pnpm install
docker compose exec web pnpm --filter core test  # should pass (no tests yet, 0 failures)
```

Expected: All packages install, no errors.

- [x] **Step 9: Commit**

```bash
git add -A
git commit -m "chore: scaffold monorepo with pnpm workspaces + turborepo"
```

---

### Task 2: Core Types

**Files:**
- Create: `packages/core/src/types/scene.ts`
- Create: `packages/core/src/types/design.ts`
- Create: `packages/core/src/types/template.ts`

- [x] **Step 1: Create `packages/core/src/types/scene.ts`**

```typescript
export type Vec3 = [number, number, number]

// --- Component Params ---

export interface RodParams {
  diameter: 6 | 8 | 13
  length: number
}

export interface CrossClampParams {
  rodDiameter: 6 | 8 | 13
}

export interface FixedRingParams {
  rodDiameter: 6 | 8 | 13
}

export interface TeeConnectorParams {
  rodDiameter: 6 | 8 | 13
}

export interface ShelfParams {
  width: number
  depth: number
  thickness: number
  material: 'wood' | 'acrylic' | 'metal'
}

export interface LedStripParams {
  length: number
  color: string
}

export interface BackPanelParams {
  width: number
  height: number
  material: 'wood' | 'acrylic' | 'metal'
}

// --- Scene Node (discriminated union) ---

interface BaseNode {
  id: string
  position: Vec3
  rotation: Vec3 // radians, Euler XYZ order
  children: SceneNode[]
}

export type SceneNode =
  | (BaseNode & { type: 'rod'; params: RodParams })
  | (BaseNode & { type: 'crossClamp'; params: CrossClampParams })
  | (BaseNode & { type: 'fixedRing'; params: FixedRingParams })
  | (BaseNode & { type: 'teeConnector'; params: TeeConnectorParams })
  | (BaseNode & { type: 'shelf'; params: ShelfParams })
  | (BaseNode & { type: 'ledStrip'; params: LedStripParams })
  | (BaseNode & { type: 'backPanel'; params: BackPanelParams })
  | (BaseNode & { type: 'group'; params: Record<string, never> })

export type SceneNodeType = SceneNode['type']
```

- [x] **Step 2: Create `packages/core/src/types/design.ts`**

```typescript
import type { SceneNode } from './scene'

export interface DesignDTO {
  id: string
  name: string
  userId: string
  templateId: string | null
  sceneGraph: SceneNode
  thumbnail: string | null
  createdAt: string  // ISO 8601
  updatedAt: string  // ISO 8601
}

export interface DesignRecord {
  id: string
  name: string
  userId: string
  templateId: string | null
  sceneGraph: string  // JSON string
  thumbnail: string | null
  createdAt: Date
  updatedAt: Date
}
```

- [x] **Step 3: Create `packages/core/src/types/template.ts`**

```typescript
import type { SceneNode } from './scene'

export interface TemplateParams {
  width: number
  height: number
  depth: number
  layers: number
  rodDiameter: 6 | 8 | 13
  shelfMaterial: 'wood' | 'acrylic' | 'metal'
}

export interface TemplateDefinition {
  id: string
  name: string
  category: 'single' | 'multi' | 'corner' | 'lShape' | 'wallMounted' | 'standalone'
  thumbnail: string
  defaultParams: TemplateParams
  generate: (params: TemplateParams) => SceneNode
}

export interface TemplateMetadata {
  id: string
  name: string
  category: TemplateDefinition['category']
  thumbnail: string
  defaultParams: TemplateParams
}
```

- [x] **Step 4: Verify TypeScript compiles**

```bash
docker compose exec web pnpm --filter core exec tsc --noEmit
```

Expected: No errors.

- [x] **Step 5: Commit**

```bash
git add packages/core/src/types/
git commit -m "feat(core): add type definitions for scene graph, design, and templates"
```

---

### Task 3: Core Zod Schemas

**Files:**
- Create: `packages/core/src/schemas/scene.ts`
- Create: `packages/core/src/schemas/design.ts`
- Create: `packages/core/src/schemas/template.ts`
- Create: `packages/core/src/schemas/auth.ts`
- Test: `packages/core/__tests__/schemas/scene.test.ts`
- Test: `packages/core/__tests__/schemas/design.test.ts`
- Test: `packages/core/__tests__/schemas/auth.test.ts`

- [x] **Step 1: Write failing tests for scene schemas**

`packages/core/__tests__/schemas/scene.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { sceneNodeSchema, vec3Schema } from '../../src/schemas/scene'

describe('vec3Schema', () => {
  it('accepts valid Vec3', () => {
    expect(vec3Schema.parse([1, 2, 3])).toEqual([1, 2, 3])
  })

  it('rejects array with wrong length', () => {
    expect(() => vec3Schema.parse([1, 2])).toThrow()
  })
})

describe('sceneNodeSchema', () => {
  it('validates rod node', () => {
    const rod = {
      id: 'rod-1',
      type: 'rod',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { diameter: 8, length: 1.0 },
      children: [],
    }
    expect(sceneNodeSchema.parse(rod)).toEqual(rod)
  })

  it('rejects rod with invalid diameter', () => {
    const rod = {
      id: 'rod-1',
      type: 'rod',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { diameter: 10, length: 1.0 },
      children: [],
    }
    expect(() => sceneNodeSchema.parse(rod)).toThrow()
  })

  it('validates shelf node', () => {
    const shelf = {
      id: 'shelf-1',
      type: 'shelf',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { width: 0.8, depth: 0.4, thickness: 0.02, material: 'wood' },
      children: [],
    }
    expect(sceneNodeSchema.parse(shelf)).toEqual(shelf)
  })

  it('validates group node with children', () => {
    const group = {
      id: 'root',
      type: 'group',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: {},
      children: [
        {
          id: 'rod-1',
          type: 'rod',
          position: [0, 0, 0],
          rotation: [0, 0, 0],
          params: { diameter: 8, length: 1.0 },
          children: [],
        },
      ],
    }
    expect(sceneNodeSchema.parse(group)).toBeDefined()
  })

  it('rejects node with mismatched type/params', () => {
    const bad = {
      id: 'bad-1',
      type: 'rod',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { width: 0.8, depth: 0.4, thickness: 0.02, material: 'wood' },
      children: [],
    }
    expect(() => sceneNodeSchema.parse(bad)).toThrow()
  })
})
```

- [x] **Step 2: Run test to verify it fails**

```bash
docker compose exec web pnpm --filter core test
```

Expected: FAIL — module `../../src/schemas/scene` not found.

- [x] **Step 3: Implement scene schemas**

`packages/core/src/schemas/scene.ts`:
```typescript
import { z } from 'zod'

export const vec3Schema = z.tuple([z.number(), z.number(), z.number()])

const rodDiameterSchema = z.union([z.literal(6), z.literal(8), z.literal(13)])
const shelfMaterialSchema = z.enum(['wood', 'acrylic', 'metal'])

export const rodParamsSchema = z.object({
  diameter: rodDiameterSchema,
  length: z.number().positive(),
})

export const crossClampParamsSchema = z.object({
  rodDiameter: rodDiameterSchema,
})

export const fixedRingParamsSchema = z.object({
  rodDiameter: rodDiameterSchema,
})

export const teeConnectorParamsSchema = z.object({
  rodDiameter: rodDiameterSchema,
})

export const shelfParamsSchema = z.object({
  width: z.number().positive(),
  depth: z.number().positive(),
  thickness: z.number().positive(),
  material: shelfMaterialSchema,
})

export const ledStripParamsSchema = z.object({
  length: z.number().positive(),
  color: z.string(),
})

export const backPanelParamsSchema = z.object({
  width: z.number().positive(),
  height: z.number().positive(),
  material: shelfMaterialSchema,
})

const baseNodeSchema = z.object({
  id: z.string().min(1),
  position: vec3Schema,
  rotation: vec3Schema,
})

import type { SceneNode } from '../types/scene'

export const sceneNodeSchema: z.ZodType<SceneNode> = z.discriminatedUnion('type', [
  baseNodeSchema.extend({
    type: z.literal('rod'),
    params: rodParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('crossClamp'),
    params: crossClampParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('fixedRing'),
    params: fixedRingParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('teeConnector'),
    params: teeConnectorParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('shelf'),
    params: shelfParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('ledStrip'),
    params: ledStripParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('backPanel'),
    params: backPanelParamsSchema,
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
  baseNodeSchema.extend({
    type: z.literal('group'),
    params: z.object({}),
    children: z.lazy(() => z.array(sceneNodeSchema)),
  }),
])
```

- [x] **Step 4: Run scene schema tests**

```bash
docker compose exec web pnpm --filter core test -- __tests__/schemas/scene.test.ts
```

Expected: All PASS.

- [x] **Step 5: Write failing tests for auth schemas**

`packages/core/__tests__/schemas/auth.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { registerSchema, loginSchema } from '../../src/schemas/auth'

describe('registerSchema', () => {
  it('accepts valid input', () => {
    expect(registerSchema.parse({ email: 'a@b.com', password: '12345678' })).toBeDefined()
  })

  it('rejects short password', () => {
    expect(() => registerSchema.parse({ email: 'a@b.com', password: '1234567' })).toThrow()
  })

  it('rejects invalid email', () => {
    expect(() => registerSchema.parse({ email: 'not-email', password: '12345678' })).toThrow()
  })
})

describe('loginSchema', () => {
  it('accepts valid input', () => {
    expect(loginSchema.parse({ email: 'a@b.com', password: '12345678' })).toBeDefined()
  })
})
```

- [x] **Step 6: Run auth test to verify failure**

```bash
docker compose exec web pnpm --filter core test -- __tests__/schemas/auth.test.ts
```

Expected: FAIL — module not found.

- [x] **Step 7: Implement auth, design, and template schemas**

`packages/core/src/schemas/auth.ts`:
```typescript
import { z } from 'zod'

export const registerSchema = z.object({
  email: z.string().email(),
  password: z.string().min(8),
})

export const loginSchema = z.object({
  email: z.string().email(),
  password: z.string().min(1),
})
```

`packages/core/src/schemas/template.ts`:
```typescript
import { z } from 'zod'

export const templateParamsSchema = z.object({
  width: z.number().positive().max(5),
  height: z.number().positive().max(5),
  depth: z.number().positive().max(5),
  layers: z.number().int().min(1).max(10),
  rodDiameter: z.union([z.literal(6), z.literal(8), z.literal(13)]),
  shelfMaterial: z.enum(['wood', 'acrylic', 'metal']),
})
```

`packages/core/src/schemas/design.ts`:
```typescript
import { z } from 'zod'
import { sceneNodeSchema } from './scene'

export const createDesignSchema = z.object({
  name: z.string().min(1).max(100),
  templateId: z.string().nullable().optional(),
  sceneGraph: sceneNodeSchema,
})

export const updateDesignSchema = z.object({
  name: z.string().min(1).max(100).optional(),
  sceneGraph: sceneNodeSchema.optional(),
  thumbnail: z.string().max(300_000).optional(), // ~200KB base64
})
```

- [x] **Step 8: Run all schema tests**

```bash
docker compose exec web pnpm --filter core test
```

Expected: All PASS.

- [x] **Step 9: Commit**

```bash
git add packages/core/src/schemas/ packages/core/__tests__/schemas/
git commit -m "feat(core): add Zod validation schemas for scene, auth, design, template"
```

---

### Task 4: Core Utils — Scene Tree Operations

**Files:**
- Create: `packages/core/src/utils/id.ts`
- Create: `packages/core/src/utils/scene-tree.ts`
- Test: `packages/core/__tests__/utils/scene-tree.test.ts`

- [x] **Step 1: Write failing tests for scene tree utils**

`packages/core/__tests__/utils/scene-tree.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { findNode, updateNode, addNode, removeNode, duplicateNode } from '../../src/utils/scene-tree'
import type { SceneNode } from '../../src/types/scene'

const makeRoot = (): SceneNode => ({
  id: 'root',
  type: 'group',
  position: [0, 0, 0],
  rotation: [0, 0, 0],
  params: {},
  children: [
    {
      id: 'rod-1',
      type: 'rod',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { diameter: 8, length: 1.0 },
      children: [],
    },
    {
      id: 'shelf-1',
      type: 'shelf',
      position: [0, 0.5, 0],
      rotation: [0, 0, 0],
      params: { width: 0.8, depth: 0.4, thickness: 0.02, material: 'wood' },
      children: [],
    },
  ],
})

describe('findNode', () => {
  it('finds root node', () => {
    const root = makeRoot()
    expect(findNode(root, 'root')?.id).toBe('root')
  })

  it('finds child node', () => {
    const root = makeRoot()
    expect(findNode(root, 'rod-1')?.type).toBe('rod')
  })

  it('returns undefined for missing id', () => {
    const root = makeRoot()
    expect(findNode(root, 'nonexistent')).toBeUndefined()
  })
})

describe('updateNode', () => {
  it('updates node params immutably', () => {
    const root = makeRoot()
    const updated = updateNode(root, 'rod-1', { params: { diameter: 13, length: 1.5 } })
    expect(findNode(updated, 'rod-1')?.params).toEqual({ diameter: 13, length: 1.5 })
    // original unchanged
    expect(findNode(root, 'rod-1')?.params).toEqual({ diameter: 8, length: 1.0 })
  })

  it('updates node position', () => {
    const root = makeRoot()
    const updated = updateNode(root, 'shelf-1', { position: [1, 1, 1] })
    const node = findNode(updated, 'shelf-1')
    expect(node?.position).toEqual([1, 1, 1])
  })
})

describe('addNode', () => {
  it('adds node to root children', () => {
    const root = makeRoot()
    const newNode: SceneNode = {
      id: 'rod-2',
      type: 'rod',
      position: [1, 0, 0],
      rotation: [0, 0, 0],
      params: { diameter: 8, length: 1.0 },
      children: [],
    }
    const updated = addNode(root, newNode)
    expect(updated.children).toHaveLength(3)
    expect(findNode(updated, 'rod-2')).toBeDefined()
    // original unchanged
    expect(root.children).toHaveLength(2)
  })
})

describe('removeNode', () => {
  it('removes node from root children', () => {
    const root = makeRoot()
    const updated = removeNode(root, 'rod-1')
    expect(updated.children).toHaveLength(1)
    expect(findNode(updated, 'rod-1')).toBeUndefined()
    // original unchanged
    expect(root.children).toHaveLength(2)
  })
})

describe('duplicateNode', () => {
  it('duplicates node with new id', () => {
    const root = makeRoot()
    const updated = duplicateNode(root, 'rod-1')
    expect(updated.children).toHaveLength(3)
    const dup = updated.children[2]
    expect(dup.type).toBe('rod')
    expect(dup.id).not.toBe('rod-1')
  })
})
```

- [x] **Step 2: Run tests to verify failure**

```bash
docker compose exec web pnpm --filter core test -- __tests__/utils/scene-tree.test.ts
```

Expected: FAIL — module not found.

- [x] **Step 3: Implement id util**

`packages/core/src/utils/id.ts`:
```typescript
import { nanoid } from 'nanoid'

export function generateId(prefix?: string): string {
  const id = nanoid(12)
  return prefix ? `${prefix}-${id}` : id
}
```

- [x] **Step 4: Implement scene tree utils**

`packages/core/src/utils/scene-tree.ts`:
```typescript
import type { SceneNode } from '../types/scene'
import { generateId } from './id'

export function findNode(root: SceneNode, id: string): SceneNode | undefined {
  if (root.id === id) return root
  for (const child of root.children) {
    const found = findNode(child, id)
    if (found) return found
  }
  return undefined
}

export function updateNode(
  root: SceneNode,
  id: string,
  updates: Partial<Pick<SceneNode, 'position' | 'rotation'>> & { params?: SceneNode['params'] },
): SceneNode {
  if (root.id === id) {
    return {
      ...root,
      ...(updates.position !== undefined && { position: updates.position }),
      ...(updates.rotation !== undefined && { rotation: updates.rotation }),
      ...(updates.params !== undefined && { params: updates.params }),
      children: root.children,
    } as SceneNode
  }
  return {
    ...root,
    children: root.children.map((child) => updateNode(child, id, updates)),
  } as SceneNode
}

export function addNode(root: SceneNode, node: SceneNode): SceneNode {
  return {
    ...root,
    children: [...root.children, node],
  } as SceneNode
}

export function removeNode(root: SceneNode, id: string): SceneNode {
  return {
    ...root,
    children: root.children
      .filter((child) => child.id !== id)
      .map((child) => removeNode(child, id)),
  } as SceneNode
}

export function duplicateNode(root: SceneNode, id: string): SceneNode {
  const node = findNode(root, id)
  if (!node || node.type === 'group') return root

  const clone = structuredClone(node)
  clone.id = generateId(node.type)
  // Offset slightly so clone is visible
  clone.position = [
    node.position[0] + 0.1,
    node.position[1],
    node.position[2],
  ]

  return addNode(root, clone)
}
```

- [x] **Step 5: Run tests**

```bash
docker compose exec web pnpm --filter core test -- __tests__/utils/scene-tree.test.ts
```

Expected: All PASS.

- [x] **Step 6: Commit**

```bash
git add packages/core/src/utils/ packages/core/__tests__/utils/
git commit -m "feat(core): add scene tree utilities (find, update, add, remove, duplicate)"
```

---

### Task 5: Core Templates (3 of 6 for MVP)

**Files:**
- Create: `packages/core/src/templates/standalone.ts`
- Create: `packages/core/src/templates/multi-shelf.ts`
- Create: `packages/core/src/templates/single-shelf.ts`
- Create: `packages/core/src/templates/wall-mounted.ts`
- Create: `packages/core/src/templates/corner-shelf.ts`
- Create: `packages/core/src/templates/l-shape.ts`
- Create: `packages/core/src/templates/registry.ts`
- Test: `packages/core/__tests__/templates/standalone.test.ts`
- Test: `packages/core/__tests__/templates/multi-shelf.test.ts`
- Test: `packages/core/__tests__/templates/single-shelf.test.ts`

MVP ships all 6 templates but tests cover the 3 most representative ones (standalone, multi-shelf, single-shelf). The remaining 3 follow the same pattern.

- [x] **Step 1: Write failing tests for standalone template**

`packages/core/__tests__/templates/standalone.test.ts`:
```typescript
import { describe, it, expect } from 'vitest'
import { standaloneTemplate } from '../../src/templates/standalone'
import { sceneNodeSchema } from '../../src/schemas/scene'
import type { TemplateParams } from '../../src/types/template'

const defaultParams: TemplateParams = {
  width: 0.8,
  height: 1.0,
  depth: 0.4,
  layers: 2,
  rodDiameter: 8,
  shelfMaterial: 'wood',
}

describe('standaloneTemplate', () => {
  it('has correct metadata', () => {
    expect(standaloneTemplate.id).toBe('standalone')
    expect(standaloneTemplate.category).toBe('standalone')
  })

  it('generates valid scene graph', () => {
    const scene = standaloneTemplate.generate(defaultParams)
    expect(() => sceneNodeSchema.parse(scene)).not.toThrow()
  })

  it('root is a group node', () => {
    const scene = standaloneTemplate.generate(defaultParams)
    expect(scene.type).toBe('group')
  })

  it('generates 4 vertical rods', () => {
    const scene = standaloneTemplate.generate(defaultParams)
    const rods = scene.children.filter((n) => n.type === 'rod')
    expect(rods).toHaveLength(4)
    rods.forEach((rod) => {
      if (rod.type === 'rod') {
        expect(rod.params.diameter).toBe(8)
        expect(rod.params.length).toBe(1.0)
      }
    })
  })

  it('generates correct number of shelves', () => {
    const scene = standaloneTemplate.generate(defaultParams)
    const shelves = scene.children.filter((n) => n.type === 'shelf')
    expect(shelves).toHaveLength(2)
  })

  it('generates cross clamps at each rod-shelf intersection', () => {
    const scene = standaloneTemplate.generate(defaultParams)
    const clamps = scene.children.filter((n) => n.type === 'crossClamp')
    // 4 rods * 2 shelves = 8 clamps
    expect(clamps).toHaveLength(8)
  })

  it('respects rodDiameter parameter', () => {
    const scene = standaloneTemplate.generate({ ...defaultParams, rodDiameter: 13 })
    const rods = scene.children.filter((n) => n.type === 'rod')
    rods.forEach((rod) => {
      if (rod.type === 'rod') expect(rod.params.diameter).toBe(13)
    })
  })

  it('distributes shelves evenly by height', () => {
    const scene = standaloneTemplate.generate(defaultParams)
    const shelves = scene.children
      .filter((n): n is Extract<typeof n, { type: 'shelf' }> => n.type === 'shelf')
      .sort((a, b) => a.position[1] - b.position[1])
    // 2 layers, height 1.0 → shelves at y=0 and y=0.5
    expect(shelves[0].position[1]).toBeCloseTo(0)
    expect(shelves[1].position[1]).toBeCloseTo(0.5)
  })
})
```

- [x] **Step 2: Run test to verify failure**

```bash
docker compose exec web pnpm --filter core test -- __tests__/templates/standalone.test.ts
```

Expected: FAIL — module not found.

- [x] **Step 3: Implement standalone template**

`packages/core/src/templates/standalone.ts`:
```typescript
import type { TemplateDefinition, TemplateParams } from '../types/template'
import type { SceneNode } from '../types/scene'
import { generateId } from '../utils/id'

function generate(params: TemplateParams): SceneNode {
  const { width, height, depth, layers, rodDiameter, shelfMaterial } = params
  const children: SceneNode[] = []

  // 4 vertical rods at corners
  const corners: [number, number][] = [
    [0, 0],
    [width, 0],
    [0, depth],
    [width, depth],
  ]

  for (const [x, z] of corners) {
    children.push({
      id: generateId('rod'),
      type: 'rod',
      position: [x, 0, z],
      rotation: [0, 0, 0],
      params: { diameter: rodDiameter, length: height },
      children: [],
    })
  }

  // Shelves distributed evenly
  const spacing = height / layers
  for (let i = 0; i < layers; i++) {
    const y = i * spacing

    children.push({
      id: generateId('shelf'),
      type: 'shelf',
      position: [0, y, 0],
      rotation: [0, 0, 0],
      params: { width, depth, thickness: 0.02, material: shelfMaterial },
      children: [],
    })

    // Cross clamps at each rod-shelf intersection
    for (const [x, z] of corners) {
      children.push({
        id: generateId('clamp'),
        type: 'crossClamp',
        position: [x, y, z],
        rotation: [0, 0, 0],
        params: { rodDiameter },
        children: [],
      })
    }
  }

  return {
    id: generateId('group'),
    type: 'group',
    position: [0, 0, 0],
    rotation: [0, 0, 0],
    params: {},
    children,
  }
}

export const standaloneTemplate: TemplateDefinition = {
  id: 'standalone',
  name: '独立架',
  category: 'standalone',
  thumbnail: '/templates/standalone.svg',
  defaultParams: {
    width: 0.8,
    height: 1.0,
    depth: 0.4,
    layers: 3,
    rodDiameter: 8,
    shelfMaterial: 'wood',
  },
  generate,
}
```

- [x] **Step 4: Run standalone template tests**

```bash
docker compose exec web pnpm --filter core test -- __tests__/templates/standalone.test.ts
```

Expected: All PASS.

- [x] **Step 5: Implement remaining 5 templates**

Follow the same pattern as `standalone.ts`. Key differences:

- `single-shelf.ts` (`category: 'single'`): 4 rods + 1 shelf (layers forced to 1).
- `multi-shelf.ts` (`category: 'multi'`): Same as standalone but default 5 layers, taller.
- `wall-mounted.ts` (`category: 'wallMounted'`): 2 rear rods only (no front rods), shelves attach to wall.
- `corner-shelf.ts` (`category: 'corner'`): L-shaped footprint — two perpendicular shelf sections sharing corner rods.
- `l-shape.ts` (`category: 'lShape'`): Similar to corner but asymmetric arm lengths.

Each template:
1. Exports a `TemplateDefinition` with correct `id`, `name`, `category`, `defaultParams`, `generate`.
2. `generate()` returns a valid `SceneNode` root group.
3. All node IDs use `generateId()`.

- [x] **Step 6: Write tests for multi-shelf and single-shelf templates**

`packages/core/__tests__/templates/multi-shelf.test.ts`: Test that it generates correct rod count (4), shelf count (= layers), and clamp count (= 4 * layers).

`packages/core/__tests__/templates/single-shelf.test.ts`: Test that only 1 shelf is generated regardless of `layers` param.

- [x] **Step 7: Run all template tests**

```bash
docker compose exec web pnpm --filter core test -- __tests__/templates/
```

Expected: All PASS.

- [x] **Step 8: Implement template registry**

`packages/core/src/templates/registry.ts`:
```typescript
import type { TemplateDefinition, TemplateMetadata } from '../types/template'
import { standaloneTemplate } from './standalone'
import { multiShelfTemplate } from './multi-shelf'
import { singleShelfTemplate } from './single-shelf'
import { wallMountedTemplate } from './wall-mounted'
import { cornerShelfTemplate } from './corner-shelf'
import { lShapeTemplate } from './l-shape'

export const templates: readonly TemplateDefinition[] = [
  singleShelfTemplate,
  multiShelfTemplate,
  standaloneTemplate,
  wallMountedTemplate,
  cornerShelfTemplate,
  lShapeTemplate,
]

export function getTemplateById(id: string): TemplateDefinition | undefined {
  return templates.find((t) => t.id === id)
}

export function getTemplateMetadata(): TemplateMetadata[] {
  return templates.map(({ generate: _, ...meta }) => meta)
}
```

- [x] **Step 9: Commit**

```bash
git add packages/core/src/templates/ packages/core/__tests__/templates/
git commit -m "feat(core): add 6 template generators with registry"
```

---

## Phase B: Backend (Tasks 6-10)

### Task 6: Prisma + SQLite Setup

**Files:**
- Create: `packages/web/prisma/schema.prisma`
- Create: `packages/web/src/lib/prisma.ts`
- Create: `packages/web/src/lib/env.ts`
- Test: `packages/web/src/__tests__/lib/env.test.ts`

- [x] **Step 1: Write failing test for env validation**

`packages/web/src/__tests__/lib/env.test.ts`:
```typescript
import { describe, it, expect, vi, afterEach } from 'vitest'

describe('validateEnv', () => {
  const originalEnv = process.env

  afterEach(() => {
    process.env = originalEnv
    vi.resetModules()
  })

  it('throws if JWT_SECRET is missing', async () => {
    process.env = { ...originalEnv, JWT_SECRET: undefined, DATABASE_URL: 'file:./test.db' }
    const { validateEnv } = await import('@/lib/env')
    expect(() => validateEnv()).toThrow('JWT_SECRET')
  })

  it('throws if JWT_SECRET is too short', async () => {
    process.env = { ...originalEnv, JWT_SECRET: 'short', DATABASE_URL: 'file:./test.db' }
    const { validateEnv } = await import('@/lib/env')
    expect(() => validateEnv()).toThrow()
  })

  it('passes with valid env', async () => {
    process.env = {
      ...originalEnv,
      JWT_SECRET: 'a'.repeat(32),
      DATABASE_URL: 'file:./test.db',
    }
    const { validateEnv } = await import('@/lib/env')
    expect(() => validateEnv()).not.toThrow()
  })
})
```

- [x] **Step 2: Run test to verify failure**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/lib/env.test.ts
```

Expected: FAIL — module not found.

- [x] **Step 3: Implement env validation**

`packages/web/src/lib/env.ts`:
```typescript
import { z } from 'zod'

const envSchema = z.object({
  JWT_SECRET: z.string().min(32, 'JWT_SECRET must be at least 32 characters'),
  DATABASE_URL: z.string().min(1, 'DATABASE_URL is required'),
})

export function validateEnv() {
  const result = envSchema.safeParse(process.env)
  if (!result.success) {
    const errors = result.error.issues.map((i) => `${i.path}: ${i.message}`).join('\n')
    throw new Error(`Environment validation failed:\n${errors}`)
  }
  return result.data
}

export function getEnv() {
  return envSchema.parse(process.env)
}
```

- [x] **Step 4: Run env test**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/lib/env.test.ts
```

Expected: All PASS.

- [x] **Step 5: Create Prisma schema**

`packages/web/prisma/schema.prisma`:
```prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model User {
  id           String   @id @default(cuid())
  email        String   @unique
  passwordHash String
  designs      Design[]
  createdAt    DateTime @default(now())
}

model Design {
  id         String   @id @default(cuid())
  name       String
  userId     String
  user       User     @relation(fields: [userId], references: [id], onDelete: Cascade)
  templateId String?
  sceneGraph String   // JSON string
  thumbnail  String?  // base64 data URL
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
}
```

- [x] **Step 6: Create Prisma client singleton**

`packages/web/src/lib/prisma.ts`:
```typescript
import { PrismaClient } from '@prisma/client'

const globalForPrisma = globalThis as unknown as { prisma: PrismaClient }

export const prisma = globalForPrisma.prisma || new PrismaClient()

if (process.env.NODE_ENV !== 'production') {
  globalForPrisma.prisma = prisma
}
```

- [x] **Step 7: Run Prisma migration**

```bash
docker compose exec web pnpm --filter web exec prisma migrate dev --name init
```

Expected: Migration created, SQLite database initialized.

- [x] **Step 8: Commit**

```bash
git add packages/web/prisma/ packages/web/src/lib/prisma.ts packages/web/src/lib/env.ts packages/web/src/__tests__/lib/env.test.ts
git commit -m "feat(web): add Prisma schema, SQLite setup, and env validation"
```

---

### Task 7: Auth Utilities — JWT + bcrypt

**Files:**
- Create: `packages/web/src/lib/auth.ts`
- Create: `packages/web/src/lib/with-auth.ts`
- Test: `packages/web/src/__tests__/lib/auth.test.ts`

- [x] **Step 1: Write failing tests for auth utilities**

`packages/web/src/__tests__/lib/auth.test.ts`:
```typescript
import { describe, it, expect, vi, beforeAll } from 'vitest'

// Set env before importing
vi.stubEnv('JWT_SECRET', 'a'.repeat(32))
vi.stubEnv('DATABASE_URL', 'file:./test.db')

import { hashPassword, verifyPassword, signToken, verifyToken } from '@/lib/auth'

describe('password hashing', () => {
  it('hashes and verifies password', async () => {
    const hash = await hashPassword('mypassword')
    expect(hash).not.toBe('mypassword')
    expect(await verifyPassword('mypassword', hash)).toBe(true)
  })

  it('rejects wrong password', async () => {
    const hash = await hashPassword('mypassword')
    expect(await verifyPassword('wrongpassword', hash)).toBe(false)
  })
})

describe('JWT', () => {
  it('signs and verifies token', async () => {
    const token = await signToken({ userId: 'user-123' })
    expect(typeof token).toBe('string')
    const payload = await verifyToken(token)
    expect(payload.userId).toBe('user-123')
  })

  it('rejects invalid token', async () => {
    await expect(verifyToken('invalid-token')).rejects.toThrow()
  })
})
```

- [x] **Step 2: Run test to verify failure**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/lib/auth.test.ts
```

Expected: FAIL — module not found.

- [x] **Step 3: Implement auth utilities**

`packages/web/src/lib/auth.ts`:
```typescript
import { hash, compare } from 'bcryptjs'
import { SignJWT, jwtVerify } from 'jose'
import { getEnv } from './env'

const SALT_ROUNDS = 10
const TOKEN_EXPIRY = '7d'

export async function hashPassword(password: string): Promise<string> {
  return hash(password, SALT_ROUNDS)
}

export async function verifyPassword(password: string, passwordHash: string): Promise<boolean> {
  return compare(password, passwordHash)
}

function getSecret(): Uint8Array {
  return new TextEncoder().encode(getEnv().JWT_SECRET)
}

export async function signToken(payload: { userId: string }): Promise<string> {
  return new SignJWT(payload)
    .setProtectedHeader({ alg: 'HS256' })
    .setExpirationTime(TOKEN_EXPIRY)
    .setIssuedAt()
    .sign(getSecret())
}

export async function verifyToken(token: string): Promise<{ userId: string }> {
  const { payload } = await jwtVerify(token, getSecret())
  return { userId: payload.userId as string }
}
```

- [x] **Step 4: Run auth tests**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/lib/auth.test.ts
```

Expected: All PASS.

- [x] **Step 5: Implement withAuth middleware**

`packages/web/src/lib/with-auth.ts`:
```typescript
import { cookies } from 'next/headers'
import { NextResponse } from 'next/server'
import { verifyToken } from './auth'

export interface AuthenticatedRequest {
  userId: string
}

export async function withAuth(): Promise<AuthenticatedRequest | NextResponse> {
  const cookieStore = await cookies()
  const token = cookieStore.get('token')?.value

  if (!token) {
    return NextResponse.json(
      { success: false, error: { code: 'AUTH_TOKEN_EXPIRED', message: 'Not authenticated' } },
      { status: 401 },
    )
  }

  try {
    const { userId } = await verifyToken(token)
    return { userId }
  } catch {
    return NextResponse.json(
      { success: false, error: { code: 'AUTH_TOKEN_EXPIRED', message: 'Token expired or invalid' } },
      { status: 401 },
    )
  }
}

export function isAuthError(result: AuthenticatedRequest | NextResponse): result is NextResponse {
  return result instanceof NextResponse
}
```

- [x] **Step 6: Commit**

```bash
git add packages/web/src/lib/auth.ts packages/web/src/lib/with-auth.ts packages/web/src/__tests__/lib/auth.test.ts
git commit -m "feat(web): add JWT + bcrypt auth utilities and withAuth middleware"
```

---

### Task 8: Auth API Routes

**Files:**
- Create: `packages/web/src/app/api/auth/register/route.ts`
- Create: `packages/web/src/app/api/auth/login/route.ts`
- Create: `packages/web/src/app/api/auth/logout/route.ts`
- Create: `packages/web/src/app/api/health/route.ts`
- Test: `packages/web/src/__tests__/api/auth.test.ts`

- [x] **Step 1: Write failing tests for auth API**

`packages/web/src/__tests__/api/auth.test.ts`:

> Note: These are integration tests that test the auth logic functions directly (not HTTP layer). For full HTTP testing, use E2E tests in Phase F.

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

vi.stubEnv('JWT_SECRET', 'a'.repeat(32))
vi.stubEnv('DATABASE_URL', 'file:./test.db')

// We test the handler logic by mocking Prisma
vi.mock('@/lib/prisma', () => ({
  prisma: {
    user: {
      findUnique: vi.fn(),
      create: vi.fn(),
    },
  },
}))

import { prisma } from '@/lib/prisma'
import { hashPassword, verifyPassword, signToken, verifyToken } from '@/lib/auth'

describe('auth flow', () => {
  it('register → login → verify token flow', async () => {
    const password = 'testpassword123'
    const email = 'test@example.com'

    // Simulate register
    const passwordHash = await hashPassword(password)
    expect(await verifyPassword(password, passwordHash)).toBe(true)

    // Simulate login → sign token
    const token = await signToken({ userId: 'user-123' })
    expect(typeof token).toBe('string')

    // Verify token
    const payload = await verifyToken(token)
    expect(payload.userId).toBe('user-123')
  })
})
```

- [x] **Step 2: Run test to verify it passes (logic test)**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/api/auth.test.ts
```

Expected: PASS.

- [x] **Step 3: Implement health check route**

`packages/web/src/app/api/health/route.ts`:
```typescript
import { NextResponse } from 'next/server'

export async function GET() {
  return NextResponse.json({ status: 'ok' })
}
```

- [x] **Step 4: Implement register route**

`packages/web/src/app/api/auth/register/route.ts`:
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { registerSchema } from '@3d-modeler/core'
import { prisma } from '@/lib/prisma'
import { hashPassword, signToken } from '@/lib/auth'
import { cookies } from 'next/headers'

export async function POST(request: NextRequest) {
  const contentType = request.headers.get('content-type')
  if (!contentType?.includes('application/json')) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: 'Content-Type must be application/json' } },
      { status: 400 },
    )
  }

  const body = await request.json()
  const parsed = registerSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: parsed.error.issues[0].message } },
      { status: 400 },
    )
  }

  const { email, password } = parsed.data

  const existing = await prisma.user.findUnique({ where: { email } })
  if (existing) {
    return NextResponse.json(
      { success: false, error: { code: 'AUTH_EMAIL_EXISTS', message: 'Email already registered' } },
      { status: 409 },
    )
  }

  const passwordHash = await hashPassword(password)
  const user = await prisma.user.create({
    data: { email, passwordHash },
  })

  const token = await signToken({ userId: user.id })
  const cookieStore = await cookies()
  cookieStore.set('token', token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 60 * 60 * 24 * 7, // 7 days
    path: '/',
  })

  return NextResponse.json({
    success: true,
    data: { id: user.id, email: user.email },
  })
}
```

- [x] **Step 5: Implement login route**

`packages/web/src/app/api/auth/login/route.ts`:
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { loginSchema } from '@3d-modeler/core'
import { prisma } from '@/lib/prisma'
import { verifyPassword, signToken } from '@/lib/auth'
import { cookies } from 'next/headers'

export async function POST(request: NextRequest) {
  const contentType = request.headers.get('content-type')
  if (!contentType?.includes('application/json')) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: 'Content-Type must be application/json' } },
      { status: 400 },
    )
  }

  const body = await request.json()
  const parsed = loginSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: parsed.error.issues[0].message } },
      { status: 400 },
    )
  }

  const { email, password } = parsed.data

  const user = await prisma.user.findUnique({ where: { email } })
  if (!user || !(await verifyPassword(password, user.passwordHash))) {
    return NextResponse.json(
      { success: false, error: { code: 'AUTH_INVALID_CREDENTIALS', message: 'Invalid email or password' } },
      { status: 401 },
    )
  }

  const token = await signToken({ userId: user.id })
  const cookieStore = await cookies()
  cookieStore.set('token', token, {
    httpOnly: true,
    secure: process.env.NODE_ENV === 'production',
    sameSite: 'lax',
    maxAge: 60 * 60 * 24 * 7,
    path: '/',
  })

  return NextResponse.json({
    success: true,
    data: { id: user.id, email: user.email },
  })
}
```

- [x] **Step 6: Implement logout route**

`packages/web/src/app/api/auth/logout/route.ts`:
```typescript
import { NextResponse } from 'next/server'
import { cookies } from 'next/headers'

export async function POST() {
  const cookieStore = await cookies()
  cookieStore.delete('token')

  return NextResponse.json({ success: true, data: null })
}
```

- [x] **Step 7: Commit**

```bash
git add packages/web/src/app/api/auth/ packages/web/src/app/api/health/ packages/web/src/__tests__/api/auth.test.ts
git commit -m "feat(web): add auth API routes (register, login, logout, health)"
```

---

### Task 9: Rate Limiting

**Files:**
- Create: `packages/web/src/lib/rate-limit.ts`
- Test: `packages/web/src/__tests__/lib/rate-limit.test.ts`

- [x] **Step 1: Write failing tests for rate limiter**

> Per design doc: login limits count **failed** attempts only. The limiter exposes `isBlocked(key)` to check before processing, and `hit(key)` to record a failure after auth fails.

`packages/web/src/__tests__/lib/rate-limit.test.ts`:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'
import { RateLimiter } from '@/lib/rate-limit'

describe('RateLimiter', () => {
  beforeEach(() => {
    vi.useFakeTimers()
  })

  it('is not blocked before any hits', () => {
    const limiter = new RateLimiter({ windowMs: 60_000, max: 5 })
    expect(limiter.isBlocked('127.0.0.1')).toBe(false)
  })

  it('blocks after max failures', () => {
    const limiter = new RateLimiter({ windowMs: 60_000, max: 5 })
    for (let i = 0; i < 5; i++) {
      limiter.hit('127.0.0.1')
    }
    expect(limiter.isBlocked('127.0.0.1')).toBe(true)
  })

  it('does not block under the limit', () => {
    const limiter = new RateLimiter({ windowMs: 60_000, max: 5 })
    for (let i = 0; i < 4; i++) {
      limiter.hit('127.0.0.1')
    }
    expect(limiter.isBlocked('127.0.0.1')).toBe(false)
  })

  it('resets after window expires', () => {
    const limiter = new RateLimiter({ windowMs: 60_000, max: 5 })
    for (let i = 0; i < 5; i++) {
      limiter.hit('127.0.0.1')
    }
    expect(limiter.isBlocked('127.0.0.1')).toBe(true)

    vi.advanceTimersByTime(60_001)
    expect(limiter.isBlocked('127.0.0.1')).toBe(false)
  })

  it('tracks different IPs independently', () => {
    const limiter = new RateLimiter({ windowMs: 60_000, max: 1 })
    limiter.hit('1.1.1.1')
    expect(limiter.isBlocked('1.1.1.1')).toBe(true)
    expect(limiter.isBlocked('2.2.2.2')).toBe(false)
  })
})
```

- [x] **Step 2: Run test to verify failure**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/lib/rate-limit.test.ts
```

Expected: FAIL.

- [x] **Step 3: Implement rate limiter**

`packages/web/src/lib/rate-limit.ts`:
```typescript
interface RateLimitEntry {
  count: number
  resetAt: number
}

export class RateLimiter {
  private store = new Map<string, RateLimitEntry>()
  private windowMs: number
  private max: number

  constructor(opts: { windowMs: number; max: number }) {
    this.windowMs = opts.windowMs
    this.max = opts.max
  }

  /** Record a failed attempt for this key */
  hit(key: string): void {
    const now = Date.now()
    const entry = this.store.get(key)

    if (!entry || now > entry.resetAt) {
      this.store.set(key, { count: 1, resetAt: now + this.windowMs })
      return
    }

    entry.count++
  }

  /** Check if key is blocked (reached max failures in window) */
  isBlocked(key: string): boolean {
    const now = Date.now()
    const entry = this.store.get(key)
    if (!entry || now > entry.resetAt) return false
    return entry.count >= this.max
  }
}

// Pre-configured limiters for auth endpoints
// Login: 5 failed attempts per minute per IP
export const loginLimiter = new RateLimiter({ windowMs: 60_000, max: 5 })
// Register: 10 attempts per hour per IP (counts all attempts)
export const registerLimiter = new RateLimiter({ windowMs: 3_600_000, max: 10 })
```

- [x] **Step 4: Run rate limit tests**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/lib/rate-limit.test.ts
```

Expected: All PASS.

- [x] **Step 5: Wire rate limiting into auth routes**

Modify `packages/web/src/app/api/auth/login/route.ts` POST handler:
```typescript
import { loginLimiter } from '@/lib/rate-limit'

// 1. Before parsing body — check if IP is already blocked:
const ip = request.headers.get('x-forwarded-for') ?? request.headers.get('x-real-ip') ?? 'unknown'
if (loginLimiter.isBlocked(ip)) {
  return NextResponse.json(
    { success: false, error: { code: 'AUTH_RATE_LIMITED', message: 'Too many failed attempts, try again later' } },
    { status: 429 },
  )
}

// 2. After verifyPassword fails — record the failure:
if (!user || !(await verifyPassword(password, user.passwordHash))) {
  loginLimiter.hit(ip)  // count only FAILED attempts
  return NextResponse.json(
    { success: false, error: { code: 'AUTH_INVALID_CREDENTIALS', message: 'Invalid email or password' } },
    { status: 401 },
  )
}
```

For register route: use `registerLimiter.hit(ip)` on all attempts (before processing), and `registerLimiter.isBlocked(ip)` to gate.

- [x] **Step 6: Commit**

```bash
git add packages/web/src/lib/rate-limit.ts packages/web/src/__tests__/lib/rate-limit.test.ts packages/web/src/app/api/auth/
git commit -m "feat(web): add rate limiting for auth endpoints"
```

---

### Task 10: Designs CRUD API

**Files:**
- Create: `packages/web/src/app/api/designs/route.ts`
- Create: `packages/web/src/app/api/designs/[id]/route.ts`
- Create: `packages/web/src/app/api/templates/route.ts`
- Test: `packages/web/src/__tests__/api/designs.test.ts`

- [x] **Step 1: Write failing tests for designs API logic**

`packages/web/src/__tests__/api/designs.test.ts`:
```typescript
import { describe, it, expect, vi } from 'vitest'

vi.stubEnv('JWT_SECRET', 'a'.repeat(32))
vi.stubEnv('DATABASE_URL', 'file:./test.db')

import { createDesignSchema, updateDesignSchema } from '@3d-modeler/core'

describe('design validation', () => {
  it('validates create design input', () => {
    const input = {
      name: 'My Shelf',
      templateId: 'standalone',
      sceneGraph: {
        id: 'root',
        type: 'group',
        position: [0, 0, 0],
        rotation: [0, 0, 0],
        params: {},
        children: [],
      },
    }
    expect(() => createDesignSchema.parse(input)).not.toThrow()
  })

  it('validates partial update', () => {
    const input = { name: 'Renamed' }
    expect(() => updateDesignSchema.parse(input)).not.toThrow()
  })

  it('rejects empty name', () => {
    const input = { name: '' }
    expect(() => createDesignSchema.parse(input)).toThrow()
  })
})
```

- [x] **Step 2: Run test to verify it passes**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/api/designs.test.ts
```

Expected: PASS (uses already-built schemas).

- [x] **Step 3: Implement designs list + create route**

`packages/web/src/app/api/designs/route.ts`:
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { createDesignSchema } from '@3d-modeler/core'
import { prisma } from '@/lib/prisma'
import { withAuth, isAuthError } from '@/lib/with-auth'

export async function GET(request: NextRequest) {
  const auth = await withAuth()
  if (isAuthError(auth)) return auth

  const url = new URL(request.url)
  const page = Math.max(1, Number(url.searchParams.get('page')) || 1)
  const limit = Math.min(50, Math.max(1, Number(url.searchParams.get('limit')) || 20))
  const skip = (page - 1) * limit

  const [designs, total] = await Promise.all([
    prisma.design.findMany({
      where: { userId: auth.userId },
      orderBy: { updatedAt: 'desc' },
      select: {
        id: true,
        name: true,
        userId: true,
        templateId: true,
        createdAt: true,
        updatedAt: true,
        // Note: sceneGraph and thumbnail excluded from list endpoint for performance
      },
      skip,
      take: limit,
    }),
    prisma.design.count({ where: { userId: auth.userId } }),
  ])

  return NextResponse.json({
    success: true,
    data: designs.map((d) => ({
      ...d,
      createdAt: d.createdAt.toISOString(),
      updatedAt: d.updatedAt.toISOString(),
    })),
    pagination: { page, limit, total },
  })
}

export async function POST(request: NextRequest) {
  const contentType = request.headers.get('content-type')
  if (!contentType?.includes('application/json')) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: 'Content-Type must be application/json' } },
      { status: 400 },
    )
  }

  const auth = await withAuth()
  if (isAuthError(auth)) return auth

  const body = await request.json()
  const parsed = createDesignSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: parsed.error.issues[0].message } },
      { status: 400 },
    )
  }

  const design = await prisma.design.create({
    data: {
      name: parsed.data.name,
      userId: auth.userId,
      templateId: parsed.data.templateId ?? null,
      sceneGraph: JSON.stringify(parsed.data.sceneGraph),
    },
  })

  return NextResponse.json({
    success: true,
    data: {
      ...design,
      sceneGraph: JSON.parse(design.sceneGraph),
      createdAt: design.createdAt.toISOString(),
      updatedAt: design.updatedAt.toISOString(),
    },
  }, { status: 201 })
}
```

- [x] **Step 4: Implement single design routes (GET, PATCH, DELETE)**

`packages/web/src/app/api/designs/[id]/route.ts`:
```typescript
import { NextRequest, NextResponse } from 'next/server'
import { updateDesignSchema } from '@3d-modeler/core'
import { prisma } from '@/lib/prisma'
import { withAuth, isAuthError } from '@/lib/with-auth'

export async function GET(
  _request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
) {
  const auth = await withAuth()
  if (isAuthError(auth)) return auth

  const { id } = await params
  const design = await prisma.design.findUnique({ where: { id } })

  if (!design) {
    return NextResponse.json(
      { success: false, error: { code: 'DESIGN_NOT_FOUND', message: 'Design not found' } },
      { status: 404 },
    )
  }

  if (design.userId !== auth.userId) {
    return NextResponse.json(
      { success: false, error: { code: 'DESIGN_FORBIDDEN', message: 'Access denied' } },
      { status: 403 },
    )
  }

  return NextResponse.json({
    success: true,
    data: {
      ...design,
      sceneGraph: JSON.parse(design.sceneGraph),
      thumbnail: design.thumbnail ?? null, // stored as full data URL
      createdAt: design.createdAt.toISOString(),
      updatedAt: design.updatedAt.toISOString(),
    },
  })
}

export async function PATCH(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
) {
  const contentType = request.headers.get('content-type')
  if (!contentType?.includes('application/json')) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: 'Content-Type must be application/json' } },
      { status: 400 },
    )
  }

  const auth = await withAuth()
  if (isAuthError(auth)) return auth

  const { id } = await params
  const design = await prisma.design.findUnique({ where: { id } })

  if (!design) {
    return NextResponse.json(
      { success: false, error: { code: 'DESIGN_NOT_FOUND', message: 'Design not found' } },
      { status: 404 },
    )
  }

  if (design.userId !== auth.userId) {
    return NextResponse.json(
      { success: false, error: { code: 'DESIGN_FORBIDDEN', message: 'Access denied' } },
      { status: 403 },
    )
  }

  const body = await request.json()
  const parsed = updateDesignSchema.safeParse(body)
  if (!parsed.success) {
    return NextResponse.json(
      { success: false, error: { code: 'VALIDATION_ERROR', message: parsed.error.issues[0].message } },
      { status: 400 },
    )
  }

  const updateData: Record<string, unknown> = {}
  if (parsed.data.name !== undefined) updateData.name = parsed.data.name
  if (parsed.data.sceneGraph !== undefined) updateData.sceneGraph = JSON.stringify(parsed.data.sceneGraph)
  if (parsed.data.thumbnail !== undefined) updateData.thumbnail = parsed.data.thumbnail

  const updated = await prisma.design.update({
    where: { id },
    data: updateData,
  })

  return NextResponse.json({
    success: true,
    data: {
      ...updated,
      sceneGraph: JSON.parse(updated.sceneGraph),
      thumbnail: updated.thumbnail ?? null, // stored as full data URL
      createdAt: updated.createdAt.toISOString(),
      updatedAt: updated.updatedAt.toISOString(),
    },
  })
}

export async function DELETE(
  _request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
) {
  const auth = await withAuth()
  if (isAuthError(auth)) return auth

  const { id } = await params
  const design = await prisma.design.findUnique({ where: { id } })

  if (!design) {
    return NextResponse.json(
      { success: false, error: { code: 'DESIGN_NOT_FOUND', message: 'Design not found' } },
      { status: 404 },
    )
  }

  if (design.userId !== auth.userId) {
    return NextResponse.json(
      { success: false, error: { code: 'DESIGN_FORBIDDEN', message: 'Access denied' } },
      { status: 403 },
    )
  }

  await prisma.design.delete({ where: { id } })

  return NextResponse.json({ success: true, data: null })
}
```

- [x] **Step 5: Implement templates metadata route**

`packages/web/src/app/api/templates/route.ts`:
```typescript
import { NextResponse } from 'next/server'
import { getTemplateMetadata } from '@3d-modeler/core'

export async function GET() {
  return NextResponse.json({
    success: true,
    data: getTemplateMetadata(),
  })
}
```

- [x] **Step 6: Commit**

```bash
git add packages/web/src/app/api/ packages/web/src/__tests__/api/designs.test.ts
git commit -m "feat(web): add designs CRUD API and templates metadata endpoint"
```

---

## Phase C: 3D Rendering (Tasks 11-13)

### Task 11: Mesh Components (Rod, Shelf, CrossClamp)

**Files:**
- Create: `packages/web/src/components/meshes/RodMesh.tsx`
- Create: `packages/web/src/components/meshes/ShelfMesh.tsx`
- Create: `packages/web/src/components/meshes/CrossClampMesh.tsx`
- Create: `packages/web/src/components/meshes/FixedRingMesh.tsx`
- Create: `packages/web/src/components/meshes/TeeMesh.tsx`
- Create: `packages/web/src/components/meshes/registry.ts`

> R3F mesh components are visual — tested via E2E (Phase F), not unit tests.

- [x] **Step 1: Create RodMesh**

`packages/web/src/components/meshes/RodMesh.tsx`:
```tsx
'use client'

import { useRef } from 'react'
import type { Mesh } from 'three'
import type { RodParams } from '@3d-modeler/core'
import { useEditorStore } from '@/stores/editor-store'

interface Props {
  params: RodParams
  nodeId: string
}

const DIAMETER_SCALE = 0.001 // mm to meters

export function RodMesh({ params, nodeId }: Props) {
  const meshRef = useRef<Mesh>(null)
  const selectNode = useEditorStore((s) => s.selectNode)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)

  const radius = (params.diameter * DIAMETER_SCALE) / 2

  return (
    <mesh
      ref={meshRef}
      onClick={(e) => {
        e.stopPropagation()
        selectNode(nodeId)
      }}
      position={[0, params.length / 2, 0]}
    >
      <cylinderGeometry args={[radius, radius, params.length, 16]} />
      <meshStandardMaterial
        color={selectedNodeId === nodeId ? '#4a9eff' : '#c0c0c0'}
        metalness={0.8}
        roughness={0.2}
      />
    </mesh>
  )
}
```

- [x] **Step 2: Create ShelfMesh**

`packages/web/src/components/meshes/ShelfMesh.tsx`:
```tsx
'use client'

import { useRef } from 'react'
import type { Mesh } from 'three'
import type { ShelfParams } from '@3d-modeler/core'
import { useEditorStore } from '@/stores/editor-store'

interface Props {
  params: ShelfParams
  nodeId: string
}

const materialColors = {
  wood: '#c4a882',
  acrylic: '#e8f4f8',
  metal: '#808080',
} as const

export function ShelfMesh({ params, nodeId }: Props) {
  const meshRef = useRef<Mesh>(null)
  const selectNode = useEditorStore((s) => s.selectNode)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)

  const isAcrylic = params.material === 'acrylic'

  return (
    <mesh
      ref={meshRef}
      onClick={(e) => {
        e.stopPropagation()
        selectNode(nodeId)
      }}
      position={[params.width / 2, 0, params.depth / 2]}
    >
      <boxGeometry args={[params.width, params.thickness, params.depth]} />
      {isAcrylic ? (
        <meshPhysicalMaterial
          color={selectedNodeId === nodeId ? '#4a9eff' : materialColors.acrylic}
          transmission={0.9}
          roughness={0.1}
          transparent
          opacity={0.6}
        />
      ) : (
        <meshStandardMaterial
          color={selectedNodeId === nodeId ? '#4a9eff' : materialColors[params.material]}
          metalness={params.material === 'metal' ? 0.6 : 0}
          roughness={params.material === 'metal' ? 0.3 : 0.7}
        />
      )}
    </mesh>
  )
}
```

- [x] **Step 3: Create CrossClampMesh, FixedRingMesh, TeeMesh**

`packages/web/src/components/meshes/CrossClampMesh.tsx`:
```tsx
'use client'

import type { CrossClampParams } from '@3d-modeler/core'
import { useEditorStore } from '@/stores/editor-store'

interface Props {
  params: CrossClampParams
  nodeId: string
}

const DIAMETER_SCALE = 0.001

export function CrossClampMesh({ params, nodeId }: Props) {
  const selectNode = useEditorStore((s) => s.selectNode)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)
  const color = selectedNodeId === nodeId ? '#4a9eff' : '#555555'
  const outerRadius = (params.rodDiameter * DIAMETER_SCALE) / 2 + 0.003

  return (
    <group onClick={(e) => { e.stopPropagation(); selectNode(nodeId) }}>
      {/* Vertical ring */}
      <mesh>
        <torusGeometry args={[outerRadius, 0.002, 8, 16]} />
        <meshStandardMaterial color={color} metalness={0.7} roughness={0.3} />
      </mesh>
      {/* Horizontal ring (rotated 90 degrees) */}
      <mesh rotation={[Math.PI / 2, 0, 0]}>
        <torusGeometry args={[outerRadius, 0.002, 8, 16]} />
        <meshStandardMaterial color={color} metalness={0.7} roughness={0.3} />
      </mesh>
    </group>
  )
}
```

`packages/web/src/components/meshes/FixedRingMesh.tsx`:
```tsx
'use client'

import type { FixedRingParams } from '@3d-modeler/core'
import { useEditorStore } from '@/stores/editor-store'

interface Props {
  params: FixedRingParams
  nodeId: string
}

const DIAMETER_SCALE = 0.001

export function FixedRingMesh({ params, nodeId }: Props) {
  const selectNode = useEditorStore((s) => s.selectNode)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)
  const outerRadius = (params.rodDiameter * DIAMETER_SCALE) / 2 + 0.004

  return (
    <mesh onClick={(e) => { e.stopPropagation(); selectNode(nodeId) }}>
      <torusGeometry args={[outerRadius, 0.003, 8, 24]} />
      <meshStandardMaterial
        color={selectedNodeId === nodeId ? '#4a9eff' : '#444444'}
        metalness={0.7}
        roughness={0.3}
      />
    </mesh>
  )
}
```

`packages/web/src/components/meshes/TeeMesh.tsx`:
```tsx
'use client'

import type { TeeConnectorParams } from '@3d-modeler/core'
import { useEditorStore } from '@/stores/editor-store'

interface Props {
  params: TeeConnectorParams
  nodeId: string
}

const DIAMETER_SCALE = 0.001

export function TeeMesh({ params, nodeId }: Props) {
  const selectNode = useEditorStore((s) => s.selectNode)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)
  const color = selectedNodeId === nodeId ? '#4a9eff' : '#555555'
  const r = (params.rodDiameter * DIAMETER_SCALE) / 2 + 0.002
  const len = 0.02

  return (
    <group onClick={(e) => { e.stopPropagation(); selectNode(nodeId) }}>
      {/* Vertical cylinder */}
      <mesh>
        <cylinderGeometry args={[r, r, len, 12]} />
        <meshStandardMaterial color={color} metalness={0.7} roughness={0.3} />
      </mesh>
      {/* Horizontal cylinder (T branch) */}
      <mesh rotation={[0, 0, Math.PI / 2]}>
        <cylinderGeometry args={[r, r, len, 12]} />
        <meshStandardMaterial color={color} metalness={0.7} roughness={0.3} />
      </mesh>
    </group>
  )
}
```

- [x] **Step 4: Create component registry**

`packages/web/src/components/meshes/registry.ts`:
```typescript
import { RodMesh } from './RodMesh'
import { CrossClampMesh } from './CrossClampMesh'
import { FixedRingMesh } from './FixedRingMesh'
import { TeeMesh } from './TeeMesh'
import { ShelfMesh } from './ShelfMesh'
import type { SceneNodeType } from '@3d-modeler/core'
import type { ComponentType } from 'react'

export const componentRegistry: Partial<Record<SceneNodeType, ComponentType<{ params: any; nodeId: string }>>> = {
  rod: RodMesh,
  crossClamp: CrossClampMesh,
  fixedRing: FixedRingMesh,
  teeConnector: TeeMesh,
  shelf: ShelfMesh,
  // ledStrip and backPanel are Phase 3 — not registered
}
```

- [x] **Step 5: Commit**

```bash
git add packages/web/src/components/meshes/
git commit -m "feat(web): add 3D mesh components (rod, shelf, clamp, ring, tee) with registry"
```

---

### Task 12: SceneRenderer + Viewport

**Files:**
- Create: `packages/web/src/components/editor/SceneRenderer.tsx`
- Create: `packages/web/src/components/editor/Viewport.tsx`

- [x] **Step 1: Create SceneRenderer**

`packages/web/src/components/editor/SceneRenderer.tsx`:
```tsx
'use client'

import type { SceneNode } from '@3d-modeler/core'
import { componentRegistry } from '@/components/meshes/registry'

interface Props {
  node: SceneNode
}

export function SceneRenderer({ node }: Props) {
  const MeshComponent = node.type !== 'group' ? componentRegistry[node.type] : null

  return (
    <group position={node.position} rotation={node.rotation}>
      {MeshComponent && <MeshComponent params={node.params} nodeId={node.id} />}
      {node.children.map((child) => (
        <SceneRenderer key={child.id} node={child} />
      ))}
    </group>
  )
}
```

- [x] **Step 2: Create Viewport**

`packages/web/src/components/editor/Viewport.tsx`:
```tsx
'use client'

import { Canvas } from '@react-three/fiber'
import { OrbitControls, Grid, GizmoHelper, GizmoViewport } from '@react-three/drei'
import { SceneRenderer } from './SceneRenderer'
import { useEditorStore } from '@/stores/editor-store'

export function Viewport() {
  const sceneGraph = useEditorStore((s) => s.sceneGraph)
  const selectNode = useEditorStore((s) => s.selectNode)

  return (
    <div className="flex-1 h-full bg-gray-900">
      <Canvas
        camera={{ position: [3, 3, 3], fov: 50 }}
        onPointerMissed={() => selectNode(null)}
      >
        <ambientLight intensity={0.4} />
        <directionalLight position={[10, 10, 5]} intensity={0.8} />
        <OrbitControls enableDamping dampingFactor={0.1} />
        <Grid
          args={[20, 20]}
          cellSize={0.5}
          cellThickness={0.5}
          cellColor="#444444"
          sectionSize={1}
          sectionThickness={1}
          sectionColor="#666666"
          fadeDistance={25}
          infiniteGrid
        />
        <GizmoHelper alignment="bottom-right" margin={[80, 80]}>
          <GizmoViewport />
        </GizmoHelper>
        <SceneRenderer node={sceneGraph} />
      </Canvas>
    </div>
  )
}
```

- [x] **Step 3: Commit**

```bash
git add packages/web/src/components/editor/SceneRenderer.tsx packages/web/src/components/editor/Viewport.tsx
git commit -m "feat(web): add SceneRenderer and Viewport with OrbitControls + Grid"
```

---

## Phase D: State Management (Tasks 13-14)

### Task 13: Zustand EditorStore

**Files:**
- Create: `packages/web/src/stores/editor-store.ts`
- Test: `packages/web/src/__tests__/stores/editor-store.test.ts`

- [x] **Step 1: Write failing tests for EditorStore**

`packages/web/src/__tests__/stores/editor-store.test.ts`:
```typescript
import { describe, it, expect, beforeEach } from 'vitest'
import { useEditorStore, type EditorState } from '@/stores/editor-store'
import type { SceneNode, TemplateParams } from '@3d-modeler/core'

const emptyRoot: SceneNode = {
  id: 'root',
  type: 'group',
  position: [0, 0, 0],
  rotation: [0, 0, 0],
  params: {},
  children: [],
}

describe('EditorStore', () => {
  beforeEach(() => {
    useEditorStore.setState({
      sceneGraph: emptyRoot,
      selectedNodeId: null,
      mode: 'template',
      templateId: null,
      templateParams: null,
      past: [],
      future: [],
    })
  })

  it('selects a node', () => {
    useEditorStore.getState().selectNode('rod-1')
    expect(useEditorStore.getState().selectedNodeId).toBe('rod-1')
  })

  it('deselects with null', () => {
    useEditorStore.getState().selectNode('rod-1')
    useEditorStore.getState().selectNode(null)
    expect(useEditorStore.getState().selectedNodeId).toBeNull()
  })

  describe('undo/redo', () => {
    it('undoes last change', () => {
      // Add a rod via addNode
      const rod: SceneNode = {
        id: 'rod-test',
        type: 'rod',
        position: [0, 0, 0],
        rotation: [0, 0, 0],
        params: { diameter: 8, length: 1 },
        children: [],
      }
      useEditorStore.getState().addNode(rod)
      expect(useEditorStore.getState().sceneGraph.children).toHaveLength(1)

      useEditorStore.getState().undo()
      expect(useEditorStore.getState().sceneGraph.children).toHaveLength(0)
    })

    it('redoes undone change', () => {
      const rod: SceneNode = {
        id: 'rod-test',
        type: 'rod',
        position: [0, 0, 0],
        rotation: [0, 0, 0],
        params: { diameter: 8, length: 1 },
        children: [],
      }
      useEditorStore.getState().addNode(rod)
      useEditorStore.getState().undo()
      useEditorStore.getState().redo()
      expect(useEditorStore.getState().sceneGraph.children).toHaveLength(1)
    })
  })

  it('removes a node', () => {
    const rod: SceneNode = {
      id: 'rod-test',
      type: 'rod',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { diameter: 8, length: 1 },
      children: [],
    }
    useEditorStore.getState().addNode(rod)
    useEditorStore.getState().removeNode('rod-test')
    expect(useEditorStore.getState().sceneGraph.children).toHaveLength(0)
  })

  it('duplicates a node', () => {
    const rod: SceneNode = {
      id: 'rod-test',
      type: 'rod',
      position: [0, 0, 0],
      rotation: [0, 0, 0],
      params: { diameter: 8, length: 1 },
      children: [],
    }
    useEditorStore.getState().addNode(rod)
    useEditorStore.getState().duplicateNode('rod-test')
    expect(useEditorStore.getState().sceneGraph.children).toHaveLength(2)
  })
})
```

- [x] **Step 2: Run test to verify failure**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/stores/editor-store.test.ts
```

Expected: FAIL — module not found.

- [x] **Step 3: Implement EditorStore**

`packages/web/src/stores/editor-store.ts`:
```typescript
import { create } from 'zustand'
import type { SceneNode, TemplateParams, Vec3 } from '@3d-modeler/core'
import {
  findNode,
  updateNode as updateNodeInTree,
  addNode as addNodeToTree,
  removeNode as removeNodeFromTree,
  duplicateNode as duplicateNodeInTree,
  getTemplateById,
} from '@3d-modeler/core'

const MAX_HISTORY = 50

const emptyRoot: SceneNode = {
  id: 'root',
  type: 'group',
  position: [0, 0, 0],
  rotation: [0, 0, 0],
  params: {},
  children: [],
}

export interface EditorState {
  sceneGraph: SceneNode
  selectedNodeId: string | null
  mode: 'template' | 'freeform'
  templateId: string | null
  templateParams: TemplateParams | null
  past: SceneNode[]
  future: SceneNode[]

  // Design metadata
  designId: string | null
  designName: string

  selectNode: (id: string | null) => void
  updateNodeTransform: (id: string, updates: { position?: Vec3; rotation?: Vec3 }) => void
  updateNodeParams: (id: string, params: SceneNode['params']) => void
  addNode: (node: SceneNode) => void
  removeNode: (id: string) => void
  duplicateNode: (id: string) => void
  undo: () => void
  redo: () => void
  applyTemplate: (templateId: string, params: TemplateParams) => void
  updateTemplateParams: (params: Partial<TemplateParams>) => void
  loadDesign: (opts: { id: string; name: string; sceneGraph: SceneNode; templateId: string | null; templateParams: TemplateParams | null }) => void
  setDesignName: (name: string) => void
  resetScene: () => void
}

function pushHistory(state: EditorState): Partial<EditorState> {
  const past = [...state.past, state.sceneGraph]
  if (past.length > MAX_HISTORY) past.shift()
  return { past, future: [] }
}

export const useEditorStore = create<EditorState>((set, get) => ({
  sceneGraph: emptyRoot,
  selectedNodeId: null,
  mode: 'template',
  templateId: null,
  templateParams: null,
  past: [],
  future: [],
  designId: null,
  designName: 'Untitled',

  selectNode: (id) => set({ selectedNodeId: id }),

  updateNodeTransform: (id, updates) =>
    set((state) => ({
      ...pushHistory(state),
      sceneGraph: updateNodeInTree(state.sceneGraph, id, updates),
    })),

  updateNodeParams: (id, params) =>
    set((state) => ({
      ...pushHistory(state),
      sceneGraph: updateNodeInTree(state.sceneGraph, id, { params }),
    })),

  addNode: (node) =>
    set((state) => ({
      ...pushHistory(state),
      sceneGraph: addNodeToTree(state.sceneGraph, node),
    })),

  removeNode: (id) =>
    set((state) => ({
      ...pushHistory(state),
      sceneGraph: removeNodeFromTree(state.sceneGraph, id),
      selectedNodeId: state.selectedNodeId === id ? null : state.selectedNodeId,
    })),

  duplicateNode: (id) =>
    set((state) => ({
      ...pushHistory(state),
      sceneGraph: duplicateNodeInTree(state.sceneGraph, id),
    })),

  undo: () =>
    set((state) => {
      if (state.past.length === 0) return state
      const prev = state.past[state.past.length - 1]
      return {
        sceneGraph: prev,
        past: state.past.slice(0, -1),
        future: [state.sceneGraph, ...state.future],
      }
    }),

  redo: () =>
    set((state) => {
      if (state.future.length === 0) return state
      const next = state.future[0]
      return {
        sceneGraph: next,
        past: [...state.past, state.sceneGraph],
        future: state.future.slice(1),
      }
    }),

  applyTemplate: (templateId, params) => {
    const template = getTemplateById(templateId)
    if (!template) return
    set((state) => ({
      ...pushHistory(state),
      sceneGraph: template.generate(params),
      templateId,
      templateParams: params,
      mode: 'template',
      selectedNodeId: null,
    }))
  },

  updateTemplateParams: (partialParams) =>
    set((state) => {
      if (!state.templateId || !state.templateParams) return state
      const newParams = { ...state.templateParams, ...partialParams }
      const template = getTemplateById(state.templateId)
      if (!template) return state
      return {
        ...pushHistory(state),
        sceneGraph: template.generate(newParams),
        templateParams: newParams,
        selectedNodeId: null,
      }
    }),

  loadDesign: (opts) => {
    // Recover templateParams from template defaults if available
    const template = opts.templateId ? getTemplateById(opts.templateId) : null
    const templateParams = opts.templateParams ?? template?.defaultParams ?? null

    set({
      designId: opts.id,
      designName: opts.name,
      sceneGraph: opts.sceneGraph,
      templateId: opts.templateId,
      templateParams,
      mode: opts.templateId ? 'template' : 'freeform',
      past: [],
      future: [],
      selectedNodeId: null,
    })
  },

  setDesignName: (name) => set({ designName: name }),

  resetScene: () =>
    set({
      sceneGraph: emptyRoot,
      selectedNodeId: null,
      past: [],
      future: [],
      templateId: null,
      templateParams: null,
    }),
}))
```

- [x] **Step 4: Run store tests**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/stores/editor-store.test.ts
```

Expected: All PASS.

- [x] **Step 5: Commit**

```bash
git add packages/web/src/stores/ packages/web/src/__tests__/stores/
git commit -m "feat(web): add Zustand EditorStore with undo/redo, template apply, CRUD operations"
```

---

### Task 14: Auto-Save Hook

**Files:**
- Create: `packages/web/src/hooks/use-auto-save.ts`
- Create: `packages/web/src/hooks/use-auth.ts`
- Test: `packages/web/src/__tests__/hooks/use-auto-save.test.ts`

- [x] **Step 1: Write failing test for auto-save**

`packages/web/src/__tests__/hooks/use-auto-save.test.ts`:
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest'

describe('auto-save debounce logic', () => {
  beforeEach(() => {
    vi.useFakeTimers()
  })

  afterEach(() => {
    vi.useRealTimers()
  })

  it('debounces save calls to 3 seconds', async () => {
    const saveFn = vi.fn().mockResolvedValue(undefined)
    let timer: ReturnType<typeof setTimeout> | null = null

    function debouncedSave() {
      if (timer) clearTimeout(timer)
      timer = setTimeout(saveFn, 3000)
    }

    debouncedSave()
    debouncedSave()
    debouncedSave()

    expect(saveFn).not.toHaveBeenCalled()
    vi.advanceTimersByTime(3000)
    expect(saveFn).toHaveBeenCalledTimes(1)
  })

  it('cancels debounce on manual save', () => {
    const saveFn = vi.fn().mockResolvedValue(undefined)
    let timer: ReturnType<typeof setTimeout> | null = null

    function debouncedSave() {
      if (timer) clearTimeout(timer)
      timer = setTimeout(saveFn, 3000)
    }

    function cancelAutoSave() {
      if (timer) {
        clearTimeout(timer)
        timer = null
      }
    }

    debouncedSave()
    cancelAutoSave()
    vi.advanceTimersByTime(3000)
    expect(saveFn).not.toHaveBeenCalled()
  })
})
```

- [x] **Step 2: Run test to verify it passes (logic-only test)**

```bash
docker compose exec web pnpm --filter web test -- src/__tests__/hooks/use-auto-save.test.ts
```

Expected: PASS.

- [x] **Step 3: Implement auto-save hook**

`packages/web/src/hooks/use-auto-save.ts`:
```typescript
'use client'

import { useEffect, useRef, useCallback } from 'react'
import { useEditorStore } from '@/stores/editor-store'

const AUTO_SAVE_DELAY = 3000
const LOCAL_STORAGE_KEY = 'autosave-design'

export function useAutoSave() {
  const timerRef = useRef<ReturnType<typeof setTimeout> | null>(null)
  const designId = useEditorStore((s) => s.designId)
  const sceneGraph = useEditorStore((s) => s.sceneGraph)
  const designName = useEditorStore((s) => s.designName)

  const save = useCallback(async () => {
    if (!designId) return

    // Save to localStorage as fallback
    try {
      localStorage.setItem(LOCAL_STORAGE_KEY, JSON.stringify({
        designId,
        sceneGraph,
        designName,
        savedAt: new Date().toISOString(),
      }))
    } catch { /* quota exceeded — ignore */ }

    // Save to server
    try {
      await fetch(`/api/designs/${designId}`, {
        method: 'PATCH',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ sceneGraph }),
      })
    } catch {
      // Network error — localStorage fallback is already saved
    }
  }, [designId, sceneGraph, designName])

  // Debounced auto-save on sceneGraph change
  useEffect(() => {
    if (!designId) return

    if (timerRef.current) clearTimeout(timerRef.current)
    timerRef.current = setTimeout(save, AUTO_SAVE_DELAY)

    return () => {
      if (timerRef.current) clearTimeout(timerRef.current)
    }
  }, [sceneGraph, designId, save])

  const manualSave = useCallback(() => {
    if (timerRef.current) {
      clearTimeout(timerRef.current)
      timerRef.current = null
    }
    return save()
  }, [save])

  return { manualSave }
}
```

- [x] **Step 4: Implement auth hook**

`packages/web/src/hooks/use-auth.ts`:
```typescript
'use client'

import { useRouter } from 'next/navigation'
import { useCallback } from 'react'

export function useAuth() {
  const router = useRouter()

  const login = useCallback(async (email: string, password: string) => {
    const res = await fetch('/api/auth/login', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email, password }),
    })
    const data = await res.json()
    if (!data.success) throw new Error(data.error.message)
    router.push('/dashboard')
    return data.data
  }, [router])

  const register = useCallback(async (email: string, password: string) => {
    const res = await fetch('/api/auth/register', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ email, password }),
    })
    const data = await res.json()
    if (!data.success) throw new Error(data.error.message)
    router.push('/dashboard')
    return data.data
  }, [router])

  const logout = useCallback(async () => {
    await fetch('/api/auth/logout', { method: 'POST' })
    router.push('/')
  }, [router])

  return { login, register, logout }
}
```

- [x] **Step 5: Commit**

```bash
git add packages/web/src/hooks/ packages/web/src/__tests__/hooks/
git commit -m "feat(web): add auto-save hook with localStorage fallback and auth hook"
```

---

## Phase E: UI Pages (Tasks 15-20)

### Task 15: Root Layout + Landing Page

**Files:**
- Create: `packages/web/src/app/globals.css`
- Create: `packages/web/src/app/layout.tsx`
- Create: `packages/web/src/app/page.tsx`

- [ ] **Step 1: Create global CSS**

`packages/web/src/app/globals.css`:
```css
@tailwind base;
@tailwind components;
@tailwind utilities;

html, body, #__next {
  height: 100%;
}
```

- [ ] **Step 2: Create root layout**

`packages/web/src/app/layout.tsx`:
```tsx
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: '3D Modeler',
  description: 'Modular shelf design tool',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN">
      <body className={`${inter.className} bg-gray-950 text-gray-100 h-full`}>
        {children}
      </body>
    </html>
  )
}
```

- [ ] **Step 3: Create landing page**

`packages/web/src/app/page.tsx`:
```tsx
import Link from 'next/link'

export default function LandingPage() {
  return (
    <main className="flex flex-col items-center justify-center min-h-screen gap-8 p-8">
      <h1 className="text-4xl font-bold">3D Modular Designer</h1>
      <p className="text-lg text-gray-400 max-w-md text-center">
        Design modular shelf structures in 3D. Pick a template, adjust dimensions, and see your design come to life.
      </p>
      <div className="flex gap-4">
        <Link
          href="/login"
          className="px-6 py-3 bg-blue-600 rounded-lg hover:bg-blue-500 transition-colors"
        >
          Login
        </Link>
        <Link
          href="/register"
          className="px-6 py-3 bg-gray-700 rounded-lg hover:bg-gray-600 transition-colors"
        >
          Register
        </Link>
      </div>
    </main>
  )
}
```

- [ ] **Step 4: Commit**

```bash
git add packages/web/src/app/globals.css packages/web/src/app/layout.tsx packages/web/src/app/page.tsx
git commit -m "feat(web): add root layout and landing page"
```

---

### Task 16: Auth Pages (Login + Register)

**Files:**
- Create: `packages/web/src/app/login/page.tsx`
- Create: `packages/web/src/app/register/page.tsx`

- [ ] **Step 1: Create login page**

`packages/web/src/app/login/page.tsx`:
```tsx
'use client'

import { useState, type FormEvent } from 'react'
import Link from 'next/link'
import { useAuth } from '@/hooks/use-auth'

export default function LoginPage() {
  const { login } = useAuth()
  const [email, setEmail] = useState('')
  const [password, setPassword] = useState('')
  const [error, setError] = useState<string | null>(null)
  const [loading, setLoading] = useState(false)

  async function handleSubmit(e: FormEvent) {
    e.preventDefault()
    setError(null)
    setLoading(true)
    try {
      await login(email, password)
    } catch (err) {
      setError(err instanceof Error ? err.message : 'Login failed')
    } finally {
      setLoading(false)
    }
  }

  return (
    <main className="flex items-center justify-center min-h-screen p-8">
      <form onSubmit={handleSubmit} className="flex flex-col gap-4 w-full max-w-sm">
        <h1 className="text-2xl font-bold text-center">Login</h1>
        {error && <p className="text-red-400 text-sm text-center">{error}</p>}
        <input
          type="email"
          placeholder="Email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
          required
          className="px-4 py-2 bg-gray-800 rounded-lg border border-gray-700 focus:border-blue-500 outline-none"
        />
        <input
          type="password"
          placeholder="Password"
          value={password}
          onChange={(e) => setPassword(e.target.value)}
          required
          className="px-4 py-2 bg-gray-800 rounded-lg border border-gray-700 focus:border-blue-500 outline-none"
        />
        <button
          type="submit"
          disabled={loading}
          className="px-4 py-2 bg-blue-600 rounded-lg hover:bg-blue-500 disabled:opacity-50 transition-colors"
        >
          {loading ? 'Logging in...' : 'Login'}
        </button>
        <p className="text-sm text-center text-gray-400">
          No account? <Link href="/register" className="text-blue-400 hover:underline">Register</Link>
        </p>
      </form>
    </main>
  )
}
```

- [ ] **Step 2: Create register page**

`packages/web/src/app/register/page.tsx`: Same structure as login, replace `login()` with `register()`, add password min-length hint, swap the link to point to `/login`.

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/app/login/ packages/web/src/app/register/
git commit -m "feat(web): add login and register pages"
```

---

### Task 17: Dashboard Page

**Files:**
- Create: `packages/web/src/app/dashboard/page.tsx`

- [ ] **Step 1: Create dashboard page**

`packages/web/src/app/dashboard/page.tsx`:
```tsx
'use client'

import { useEffect, useState } from 'react'
import { useRouter } from 'next/navigation'
import Link from 'next/link'
import type { DesignDTO, TemplateMetadata } from '@3d-modeler/core'
import { useAuth } from '@/hooks/use-auth'

export default function DashboardPage() {
  const router = useRouter()
  const { logout } = useAuth()
  const [designs, setDesigns] = useState<DesignDTO[]>([])
  const [templates, setTemplates] = useState<TemplateMetadata[]>([])
  const [loading, setLoading] = useState(true)

  useEffect(() => {
    Promise.all([
      fetch('/api/designs').then((r) => r.json()),
      fetch('/api/templates').then((r) => r.json()),
    ]).then(([designsRes, templatesRes]) => {
      if (designsRes.success) setDesigns(designsRes.data)
      if (templatesRes.success) setTemplates(templatesRes.data)
      setLoading(false)
    }).catch(() => {
      router.push('/login')
    })
  }, [router])

  async function handleDelete(id: string) {
    if (!confirm('Delete this design?')) return
    await fetch(`/api/designs/${id}`, { method: 'DELETE' })
    setDesigns((prev) => prev.filter((d) => d.id !== id))
  }

  if (loading) {
    return <main className="flex items-center justify-center min-h-screen">Loading...</main>
  }

  return (
    <main className="max-w-6xl mx-auto p-8">
      <header className="flex justify-between items-center mb-8">
        <h1 className="text-2xl font-bold">My Designs</h1>
        <button onClick={logout} className="text-gray-400 hover:text-gray-200">Logout</button>
      </header>

      {/* New design from template */}
      <section className="mb-8">
        <h2 className="text-lg font-semibold mb-4">New from Template</h2>
        <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4">
          {templates.map((t) => (
            <Link
              key={t.id}
              href={`/editor/new?template=${t.id}`}
              className="flex flex-col items-center p-4 bg-gray-800 rounded-lg hover:bg-gray-700 transition-colors"
            >
              <div className="w-16 h-16 bg-gray-700 rounded mb-2 flex items-center justify-center text-2xl">
                +
              </div>
              <span className="text-sm">{t.name}</span>
            </Link>
          ))}
        </div>
      </section>

      {/* Existing designs */}
      <section>
        <h2 className="text-lg font-semibold mb-4">Saved Designs</h2>
        {designs.length === 0 ? (
          <p className="text-gray-400">No designs yet. Pick a template above to get started.</p>
        ) : (
          <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
            {designs.map((d) => (
              <div key={d.id} className="bg-gray-800 rounded-lg overflow-hidden">
                <Link href={`/editor/${d.id}`} className="block p-4 hover:bg-gray-700 transition-colors">
                  <div className="w-full h-32 bg-gray-700 rounded mb-2" />
                  <p className="font-medium truncate">{d.name}</p>
                  <p className="text-xs text-gray-400">{new Date(d.updatedAt).toLocaleDateString()}</p>
                </Link>
                <button
                  onClick={() => handleDelete(d.id)}
                  className="w-full py-2 text-sm text-red-400 hover:bg-red-950 transition-colors"
                >
                  Delete
                </button>
              </div>
            ))}
          </div>
        )}
      </section>
    </main>
  )
}
```

- [ ] **Step 2: Commit**

```bash
git add packages/web/src/app/dashboard/
git commit -m "feat(web): add dashboard page with design list and template picker"
```

---

### Task 18: Editor UI Components

**Files:**
- Create: `packages/web/src/components/editor/Header.tsx`
- Create: `packages/web/src/components/editor/EditorShell.tsx`
- Create: `packages/web/src/components/editor/ComponentPanel.tsx`
- Create: `packages/web/src/components/editor/PropertiesPanel.tsx`
- Create: `packages/web/src/components/editor/TemplateParamsEditor.tsx`
- Create: `packages/web/src/components/editor/NodeParamsEditor.tsx`
- Create: `packages/web/src/components/editor/StatusBar.tsx`

- [ ] **Step 1: Create Header**

`packages/web/src/components/editor/Header.tsx`:
```tsx
'use client'

import { useState } from 'react'
import { useRouter } from 'next/navigation'
import { useEditorStore } from '@/stores/editor-store'
import { useAutoSave } from '@/hooks/use-auto-save'

export function Header() {
  const router = useRouter()
  const { manualSave } = useAutoSave()
  const designName = useEditorStore((s) => s.designName)
  const setDesignName = useEditorStore((s) => s.setDesignName)
  const undo = useEditorStore((s) => s.undo)
  const redo = useEditorStore((s) => s.redo)
  const past = useEditorStore((s) => s.past)
  const future = useEditorStore((s) => s.future)
  const [saving, setSaving] = useState(false)

  async function handleSave() {
    setSaving(true)
    await manualSave()
    setSaving(false)
  }

  return (
    <header className="flex items-center justify-between px-4 py-2 bg-gray-800 border-b border-gray-700">
      <div className="flex items-center gap-4">
        <button
          onClick={() => router.push('/dashboard')}
          className="text-gray-400 hover:text-gray-200"
        >
          Back
        </button>
        <input
          value={designName}
          onChange={(e) => setDesignName(e.target.value)}
          className="bg-transparent border-b border-transparent hover:border-gray-600 focus:border-blue-500 outline-none px-1"
        />
      </div>
      <div className="flex items-center gap-2">
        <button
          onClick={undo}
          disabled={past.length === 0}
          className="px-3 py-1 text-sm bg-gray-700 rounded disabled:opacity-30 hover:bg-gray-600"
          title="Undo (Ctrl+Z)"
        >
          Undo
        </button>
        <button
          onClick={redo}
          disabled={future.length === 0}
          className="px-3 py-1 text-sm bg-gray-700 rounded disabled:opacity-30 hover:bg-gray-600"
          title="Redo (Ctrl+Shift+Z)"
        >
          Redo
        </button>
        <button
          onClick={handleSave}
          disabled={saving}
          className="px-4 py-1 text-sm bg-blue-600 rounded hover:bg-blue-500 disabled:opacity-50"
        >
          {saving ? 'Saving...' : 'Save'}
        </button>
      </div>
    </header>
  )
}
```

- [ ] **Step 2: Create EditorShell (three-column layout)**

`packages/web/src/components/editor/EditorShell.tsx`:
```tsx
'use client'

import { Header } from './Header'
import { ComponentPanel } from './ComponentPanel'
import { Viewport } from './Viewport'
import { PropertiesPanel } from './PropertiesPanel'
import { StatusBar } from './StatusBar'

export function EditorShell() {
  return (
    <div className="flex flex-col h-screen">
      <Header />
      <div className="flex flex-1 overflow-hidden">
        <ComponentPanel />
        <Viewport />
        <PropertiesPanel />
      </div>
      <StatusBar />
    </div>
  )
}
```

- [ ] **Step 3: Create ComponentPanel**

`packages/web/src/components/editor/ComponentPanel.tsx`:
```tsx
'use client'

import { useEffect, useState } from 'react'
import { useEditorStore } from '@/stores/editor-store'
import type { TemplateMetadata } from '@3d-modeler/core'

export function ComponentPanel() {
  const [templates, setTemplates] = useState<TemplateMetadata[]>([])
  const applyTemplate = useEditorStore((s) => s.applyTemplate)
  const templateId = useEditorStore((s) => s.templateId)

  useEffect(() => {
    fetch('/api/templates')
      .then((r) => r.json())
      .then((res) => {
        if (res.success) setTemplates(res.data)
      })
  }, [])

  return (
    <aside className="w-56 bg-gray-800 border-r border-gray-700 overflow-y-auto p-4">
      <h2 className="text-sm font-semibold text-gray-400 uppercase mb-4">Templates</h2>
      <div className="flex flex-col gap-2">
        {templates.map((t) => (
          <button
            key={t.id}
            onClick={() => applyTemplate(t.id, t.defaultParams)}
            className={`text-left p-3 rounded-lg transition-colors ${
              templateId === t.id ? 'bg-blue-600' : 'bg-gray-700 hover:bg-gray-600'
            }`}
          >
            <span className="text-sm font-medium">{t.name}</span>
            <span className="block text-xs text-gray-400">{t.category}</span>
          </button>
        ))}
      </div>
    </aside>
  )
}
```

- [ ] **Step 4: Create PropertiesPanel with TemplateParamsEditor + NodeParamsEditor**

`packages/web/src/components/editor/TemplateParamsEditor.tsx`:
```tsx
'use client'

import { useEditorStore } from '@/stores/editor-store'

export function TemplateParamsEditor() {
  const templateParams = useEditorStore((s) => s.templateParams)
  const updateTemplateParams = useEditorStore((s) => s.updateTemplateParams)

  if (!templateParams) return null

  return (
    <div className="flex flex-col gap-3">
      <p className="text-xs text-yellow-500">Changing template params will reset all components</p>

      <label className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Width (m)</span>
        <input
          type="number"
          step={0.1}
          min={0.1}
          max={5}
          value={templateParams.width}
          onChange={(e) => updateTemplateParams({ width: Number(e.target.value) })}
          className="px-2 py-1 bg-gray-700 rounded text-sm"
        />
      </label>

      <label className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Height (m)</span>
        <input
          type="number"
          step={0.1}
          min={0.1}
          max={5}
          value={templateParams.height}
          onChange={(e) => updateTemplateParams({ height: Number(e.target.value) })}
          className="px-2 py-1 bg-gray-700 rounded text-sm"
        />
      </label>

      <label className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Depth (m)</span>
        <input
          type="number"
          step={0.1}
          min={0.1}
          max={5}
          value={templateParams.depth}
          onChange={(e) => updateTemplateParams({ depth: Number(e.target.value) })}
          className="px-2 py-1 bg-gray-700 rounded text-sm"
        />
      </label>

      <label className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Layers</span>
        <input
          type="number"
          step={1}
          min={1}
          max={10}
          value={templateParams.layers}
          onChange={(e) => updateTemplateParams({ layers: Number(e.target.value) })}
          className="px-2 py-1 bg-gray-700 rounded text-sm"
        />
      </label>

      <label className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Rod Diameter (mm)</span>
        <select
          value={templateParams.rodDiameter}
          onChange={(e) => updateTemplateParams({ rodDiameter: Number(e.target.value) as 6 | 8 | 13 })}
          className="px-2 py-1 bg-gray-700 rounded text-sm"
        >
          <option value={6}>6mm</option>
          <option value={8}>8mm</option>
          <option value={13}>13mm</option>
        </select>
      </label>

      <label className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Shelf Material</span>
        <select
          value={templateParams.shelfMaterial}
          onChange={(e) => updateTemplateParams({ shelfMaterial: e.target.value as 'wood' | 'acrylic' | 'metal' })}
          className="px-2 py-1 bg-gray-700 rounded text-sm"
        >
          <option value="wood">Wood</option>
          <option value="acrylic">Acrylic</option>
          <option value="metal">Metal</option>
        </select>
      </label>
    </div>
  )
}
```

`packages/web/src/components/editor/NodeParamsEditor.tsx`:
```tsx
'use client'

import { useEditorStore } from '@/stores/editor-store'
import { findNode } from '@3d-modeler/core'

export function NodeParamsEditor() {
  const sceneGraph = useEditorStore((s) => s.sceneGraph)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)
  const updateNodeParams = useEditorStore((s) => s.updateNodeParams)
  const updateNodeTransform = useEditorStore((s) => s.updateNodeTransform)

  if (!selectedNodeId) return <p className="text-sm text-gray-500">Select a component to edit</p>

  const node = findNode(sceneGraph, selectedNodeId)
  if (!node || node.type === 'group') return null

  return (
    <div className="flex flex-col gap-3">
      <p className="text-xs text-gray-400 uppercase">{node.type}</p>

      {/* Position */}
      <div className="flex flex-col gap-1">
        <span className="text-xs text-gray-400">Position</span>
        <div className="grid grid-cols-3 gap-1">
          {(['X', 'Y', 'Z'] as const).map((axis, i) => (
            <label key={axis} className="flex flex-col">
              <span className="text-xs text-gray-500">{axis}</span>
              <input
                type="number"
                step={0.05}
                value={node.position[i]}
                onChange={(e) => {
                  const newPos = [...node.position] as [number, number, number]
                  newPos[i] = Number(e.target.value)
                  updateNodeTransform(node.id, { position: newPos })
                }}
                className="px-1 py-0.5 bg-gray-700 rounded text-xs w-full"
              />
            </label>
          ))}
        </div>
      </div>

      {/* Type-specific params */}
      {node.type === 'rod' && (
        <>
          <label className="flex flex-col gap-1">
            <span className="text-xs text-gray-400">Diameter (mm)</span>
            <select
              value={node.params.diameter}
              onChange={(e) => updateNodeParams(node.id, { ...node.params, diameter: Number(e.target.value) as 6 | 8 | 13 })}
              className="px-2 py-1 bg-gray-700 rounded text-sm"
            >
              <option value={6}>6</option>
              <option value={8}>8</option>
              <option value={13}>13</option>
            </select>
          </label>
          <label className="flex flex-col gap-1">
            <span className="text-xs text-gray-400">Length (m)</span>
            <input
              type="number"
              step={0.1}
              min={0.1}
              value={node.params.length}
              onChange={(e) => updateNodeParams(node.id, { ...node.params, length: Number(e.target.value) })}
              className="px-2 py-1 bg-gray-700 rounded text-sm"
            />
          </label>
        </>
      )}

      {node.type === 'shelf' && (
        <>
          <label className="flex flex-col gap-1">
            <span className="text-xs text-gray-400">Width (m)</span>
            <input
              type="number"
              step={0.1}
              value={node.params.width}
              onChange={(e) => updateNodeParams(node.id, { ...node.params, width: Number(e.target.value) })}
              className="px-2 py-1 bg-gray-700 rounded text-sm"
            />
          </label>
          <label className="flex flex-col gap-1">
            <span className="text-xs text-gray-400">Depth (m)</span>
            <input
              type="number"
              step={0.1}
              value={node.params.depth}
              onChange={(e) => updateNodeParams(node.id, { ...node.params, depth: Number(e.target.value) })}
              className="px-2 py-1 bg-gray-700 rounded text-sm"
            />
          </label>
          <label className="flex flex-col gap-1">
            <span className="text-xs text-gray-400">Thickness (m)</span>
            <input
              type="number"
              step={0.005}
              min={0.005}
              value={node.params.thickness}
              onChange={(e) => updateNodeParams(node.id, { ...node.params, thickness: Number(e.target.value) })}
              className="px-2 py-1 bg-gray-700 rounded text-sm"
            />
          </label>
          <label className="flex flex-col gap-1">
            <span className="text-xs text-gray-400">Material</span>
            <select
              value={node.params.material}
              onChange={(e) => updateNodeParams(node.id, { ...node.params, material: e.target.value as 'wood' | 'acrylic' | 'metal' })}
              className="px-2 py-1 bg-gray-700 rounded text-sm"
            >
              <option value="wood">Wood</option>
              <option value="acrylic">Acrylic</option>
              <option value="metal">Metal</option>
            </select>
          </label>
        </>
      )}

      {(node.type === 'crossClamp' || node.type === 'fixedRing' || node.type === 'teeConnector') && (
        <label className="flex flex-col gap-1">
          <span className="text-xs text-gray-400">Rod Diameter (mm)</span>
          <select
            value={node.params.rodDiameter}
            onChange={(e) => updateNodeParams(node.id, { ...node.params, rodDiameter: Number(e.target.value) as 6 | 8 | 13 })}
            className="px-2 py-1 bg-gray-700 rounded text-sm"
          >
            <option value={6}>6</option>
            <option value={8}>8</option>
            <option value={13}>13</option>
          </select>
        </label>
      )}
    </div>
  )
}
```

`packages/web/src/components/editor/PropertiesPanel.tsx`:
```tsx
'use client'

import { useEditorStore } from '@/stores/editor-store'
import { TemplateParamsEditor } from './TemplateParamsEditor'
import { NodeParamsEditor } from './NodeParamsEditor'

export function PropertiesPanel() {
  const mode = useEditorStore((s) => s.mode)
  const selectedNodeId = useEditorStore((s) => s.selectedNodeId)

  return (
    <aside className="w-64 bg-gray-800 border-l border-gray-700 overflow-y-auto p-4">
      {mode === 'template' && (
        <div className="mb-6">
          <h2 className="text-sm font-semibold text-gray-400 uppercase mb-3">Template Params</h2>
          <TemplateParamsEditor />
        </div>
      )}

      <div>
        <h2 className="text-sm font-semibold text-gray-400 uppercase mb-3">
          {selectedNodeId ? 'Component' : 'Properties'}
        </h2>
        <NodeParamsEditor />
      </div>
    </aside>
  )
}
```

- [ ] **Step 5: Create StatusBar**

`packages/web/src/components/editor/StatusBar.tsx`:
```tsx
'use client'

import { useEditorStore } from '@/stores/editor-store'

function countComponents(node: { type: string; children: { type: string; children: any[] }[] }): number {
  let count = node.type !== 'group' ? 1 : 0
  for (const child of node.children) {
    count += countComponents(child)
  }
  return count
}

export function StatusBar() {
  const sceneGraph = useEditorStore((s) => s.sceneGraph)
  const mode = useEditorStore((s) => s.mode)
  const total = countComponents(sceneGraph)

  return (
    <footer className="flex items-center justify-between px-4 py-1 bg-gray-800 border-t border-gray-700 text-xs text-gray-400">
      <span>Mode: {mode === 'template' ? 'Template' : 'Freeform'}</span>
      <span>Components: {total}</span>
    </footer>
  )
}
```

- [ ] **Step 6: Commit**

```bash
git add packages/web/src/components/editor/
git commit -m "feat(web): add editor UI components (Header, EditorShell, panels, StatusBar)"
```

---

### Task 19: Editor Pages (New + Edit)

**Files:**
- Create: `packages/web/src/app/editor/new/page.tsx`
- Create: `packages/web/src/app/editor/[id]/page.tsx`

- [ ] **Step 1: Create "new from template" page**

`packages/web/src/app/editor/new/page.tsx`:
```tsx
'use client'

import { useEffect, useRef } from 'react'
import { useSearchParams, useRouter } from 'next/navigation'
import { useEditorStore } from '@/stores/editor-store'
import { EditorShell } from '@/components/editor/EditorShell'
import { getTemplateById } from '@3d-modeler/core'

export default function NewEditorPage() {
  const searchParams = useSearchParams()
  const router = useRouter()
  const initialized = useRef(false)
  const creatingRef = useRef(false) // guard against double-creation
  const applyTemplate = useEditorStore((s) => s.applyTemplate)
  const sceneGraph = useEditorStore((s) => s.sceneGraph)
  const resetScene = useEditorStore((s) => s.resetScene)

  useEffect(() => {
    if (initialized.current) return
    initialized.current = true

    const templateId = searchParams.get('template')
    if (!templateId) {
      router.push('/dashboard')
      return
    }

    const template = getTemplateById(templateId)
    if (!template) {
      router.push('/dashboard')
      return
    }

    resetScene()
    useEditorStore.setState({ designName: `New ${template.name}` })
    applyTemplate(templateId, template.defaultParams)
  }, [searchParams, router, applyTemplate, resetScene])

  // Auto-create design on server after template is applied
  useEffect(() => {
    const { designId, designName, templateId } = useEditorStore.getState()
    if (designId) return // already persisted
    if (creatingRef.current) return // creation in flight
    if (sceneGraph.children.length === 0) return // no content yet

    creatingRef.current = true
    fetch('/api/designs', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ name: designName, templateId, sceneGraph }),
    })
      .then((r) => r.json())
      .then((res) => {
        if (res.success) {
          useEditorStore.setState({ designId: res.data.id })
          window.history.replaceState(null, '', `/editor/${res.data.id}`)
        }
      })
      .finally(() => { creatingRef.current = false })
  }, [sceneGraph]) // only react to sceneGraph changes, not designName

  return <EditorShell />
}
```

- [ ] **Step 2: Create "edit existing" page**

`packages/web/src/app/editor/[id]/page.tsx`:
```tsx
'use client'

import { useEffect, useState, use } from 'react'
import { useRouter } from 'next/navigation'
import { useEditorStore } from '@/stores/editor-store'
import { EditorShell } from '@/components/editor/EditorShell'

export default function EditEditorPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = use(params)
  const router = useRouter()
  const [loading, setLoading] = useState(true)
  const loadDesign = useEditorStore((s) => s.loadDesign)

  useEffect(() => {
    fetch(`/api/designs/${id}`)
      .then((r) => r.json())
      .then((res) => {
        if (!res.success) {
          router.push('/dashboard')
          return
        }
        loadDesign({
          id: res.data.id,
          name: res.data.name,
          sceneGraph: res.data.sceneGraph,
          templateId: res.data.templateId,
          templateParams: null, // loadDesign auto-recovers from template defaults
        })
        setLoading(false)
      })
      .catch(() => router.push('/dashboard'))
  }, [id, router, loadDesign])

  if (loading) {
    return (
      <div className="flex items-center justify-center h-screen text-gray-400">
        Loading design...
      </div>
    )
  }

  return <EditorShell />
}
```

- [ ] **Step 3: Commit**

```bash
git add packages/web/src/app/editor/
git commit -m "feat(web): add editor pages (new from template + edit existing)"
```

---

### Task 20: Keyboard Shortcuts

**Files:**
- Modify: `packages/web/src/components/editor/Viewport.tsx`

- [ ] **Step 1: Add keyboard shortcut handler to Viewport or EditorShell**

Add this effect to `EditorShell.tsx`:
```tsx
import { useEffect } from 'react'
import { useEditorStore } from '@/stores/editor-store'

// Inside EditorShell component:
useEffect(() => {
  function handleKeyDown(e: KeyboardEvent) {
    const target = e.target as HTMLElement
    if (target.tagName === 'INPUT' || target.tagName === 'TEXTAREA' || target.tagName === 'SELECT') return

    if (e.key === 'z' && (e.ctrlKey || e.metaKey) && e.shiftKey) {
      e.preventDefault()
      useEditorStore.getState().redo()
    } else if (e.key === 'z' && (e.ctrlKey || e.metaKey)) {
      e.preventDefault()
      useEditorStore.getState().undo()
    } else if (e.key === 'Delete' || e.key === 'Backspace') {
      const selected = useEditorStore.getState().selectedNodeId
      if (selected) {
        e.preventDefault()
        useEditorStore.getState().removeNode(selected)
      }
    }
  }

  window.addEventListener('keydown', handleKeyDown)
  return () => window.removeEventListener('keydown', handleKeyDown)
}, [])
```

- [ ] **Step 2: Commit**

```bash
git add packages/web/src/components/editor/EditorShell.tsx
git commit -m "feat(web): add keyboard shortcuts (Ctrl+Z, Ctrl+Shift+Z, Delete)"
```

---

## Phase F: Production Docker + E2E (Tasks 21-22)

### Task 21: Production Docker + E2E Tests

> Dev Docker was set up in Task 1. This task adds the production compose file and E2E tests.

**Files:**
- Create: `docker-compose.prod.yml`

- [ ] **Step 1: Create `docker-compose.prod.yml`**

Copy from design doc Section 16.4. Key: `target: production`, no bind mounts, `restart: unless-stopped`.

- [ ] **Step 2: Commit**

```bash
git add docker-compose.prod.yml
git commit -m "chore: add production Docker compose config"
```

---

### Task 22: E2E Tests

**Files:**
- Create: `packages/web/playwright.config.ts`
- Create: `packages/web/e2e/auth.spec.ts`
- Create: `packages/web/e2e/editor.spec.ts`

- [ ] **Step 1: Create Playwright config**

`packages/web/playwright.config.ts`:
```typescript
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './e2e',
  fullyParallel: false,
  retries: 1,
  workers: 1,
  reporter: 'html',
  use: {
    baseURL: 'http://localhost:3000',
    trace: 'on-first-retry',
  },
  projects: [
    {
      name: 'chromium',
      use: { ...devices['Desktop Chrome'] },
    },
  ],
  webServer: {
    command: 'pnpm dev',
    url: 'http://localhost:3000',
    reuseExistingServer: true,
  },
})
```

- [ ] **Step 2: Create auth E2E test**

`packages/web/e2e/auth.spec.ts`:
```typescript
import { test, expect } from '@playwright/test'

const testEmail = `test-${Date.now()}@example.com`
const testPassword = 'testpassword123'

test.describe('Authentication', () => {
  test('register → redirect to dashboard', async ({ page }) => {
    await page.goto('/register')
    await page.fill('input[type="email"]', testEmail)
    await page.fill('input[type="password"]', testPassword)
    await page.click('button[type="submit"]')
    await expect(page).toHaveURL('/dashboard', { timeout: 5000 })
  })

  test('logout → redirect to landing', async ({ page }) => {
    // Login first
    await page.goto('/login')
    await page.fill('input[type="email"]', testEmail)
    await page.fill('input[type="password"]', testPassword)
    await page.click('button[type="submit"]')
    await expect(page).toHaveURL('/dashboard', { timeout: 5000 })

    // Logout
    await page.click('text=Logout')
    await expect(page).toHaveURL('/', { timeout: 5000 })
  })

  test('login with wrong password → error message', async ({ page }) => {
    await page.goto('/login')
    await page.fill('input[type="email"]', testEmail)
    await page.fill('input[type="password"]', 'wrongpassword')
    await page.click('button[type="submit"]')
    await expect(page.locator('text=Invalid')).toBeVisible()
  })
})
```

- [ ] **Step 3: Create editor E2E test**

`packages/web/e2e/editor.spec.ts`:
```typescript
import { test, expect } from '@playwright/test'

const testEmail = `editor-${Date.now()}@example.com`
const testPassword = 'testpassword123'

test.describe('Editor', () => {
  test.beforeAll(async ({ request }) => {
    await request.post('/api/auth/register', {
      data: { email: testEmail, password: testPassword },
    })
  })

  test.beforeEach(async ({ page }) => {
    await page.goto('/login')
    await page.fill('input[type="email"]', testEmail)
    await page.fill('input[type="password"]', testPassword)
    await page.click('button[type="submit"]')
    await expect(page).toHaveURL('/dashboard', { timeout: 5000 })
  })

  test('create design from template → see 3D viewport', async ({ page }) => {
    // Click first template
    await page.locator('a[href*="/editor/new?template="]').first().click()
    await expect(page).toHaveURL(/\/editor\//, { timeout: 5000 })

    // Canvas should be present
    await expect(page.locator('canvas')).toBeVisible({ timeout: 10000 })
  })

  test('save design → appears in dashboard', async ({ page }) => {
    await page.locator('a[href*="/editor/new?template="]').first().click()
    await expect(page.locator('canvas')).toBeVisible({ timeout: 10000 })

    // Click save
    await page.click('text=Save')
    // Go back to dashboard
    await page.click('text=Back')
    await expect(page).toHaveURL('/dashboard', { timeout: 5000 })

    // Should see the saved design
    await expect(page.locator('[class*="bg-gray-800"]').first()).toBeVisible()
  })
})
```

- [ ] **Step 4: Run E2E tests**

```bash
docker compose exec web pnpm --filter web exec playwright install chromium
docker compose exec web pnpm --filter web test:e2e
```

Expected: All E2E tests pass.

- [ ] **Step 5: Commit**

```bash
git add packages/web/playwright.config.ts packages/web/e2e/
git commit -m "test: add Playwright E2E tests for auth and editor flows"
```

---

### Task 23: Final Integration & Polish

**Files:**
- Verify all unit tests pass
- Verify E2E tests pass
- Check test coverage

- [ ] **Step 1: Run full test suite**

```bash
docker compose exec web pnpm test
```

Expected: All unit tests pass.

- [ ] **Step 2: Run coverage check**

```bash
docker compose exec web pnpm --filter core test -- --coverage
docker compose exec web pnpm --filter web test -- --coverage
```

Expected: >80% coverage on core package (templates, schemas, utils). Web package coverage may be lower due to visual components.

- [ ] **Step 3: Add any missing tests to reach 80%**

Focus areas if below 80%:
- Template edge cases (min/max params)
- Schema rejection cases
- Scene tree operations on deeply nested structures
- EditorStore edge cases (undo past MAX_HISTORY)

- [ ] **Step 4: Manual smoke test**

```bash
docker compose up
# Open localhost:3000
# 1. Register new account
# 2. Select a template → verify 3D renders
# 3. Adjust template params → verify 3D updates
# 4. Click a component → verify properties panel shows params
# 5. Modify component params → verify 3D updates
# 6. Ctrl+Z → verify undo works
# 7. Save → Back to dashboard → verify design appears
# 8. Open saved design → verify it loads correctly
```

- [ ] **Step 5: Final commit**

```bash
git add -A
git commit -m "chore: phase 1 MVP complete — template-based 3D modular shelf designer"
```

---

## Summary

| Phase | Tasks | What it delivers |
|-------|-------|------------------|
| A: Foundation | 1-5 | Docker + Monorepo, types, schemas, utils, 6 templates |
| B: Backend | 6-10 | Prisma/SQLite, auth (JWT+bcrypt), designs CRUD, rate limiting |
| C: 3D Rendering | 11-12 | 5 mesh components, SceneRenderer, Viewport |
| D: State Management | 13-14 | Zustand EditorStore, auto-save, auth hook |
| E: UI Pages | 15-20 | Landing, login, register, dashboard, editor shell, keyboard shortcuts |
| F: Production + E2E | 21-23 | Production Docker, Playwright E2E, final polish |

**Total: 23 tasks, ~125 steps**

## Review Changelog

Issues fixed from plan review:
1. **(HIGH) Docker task ordering** — Moved Docker setup from Task 21 to Task 1 Steps 0a-0d, so all subsequent `docker compose exec` commands work
2. **(HIGH) Rate limiter** — Changed from `check()` (counts all attempts) to `isBlocked()`/`hit()` pattern that counts only failed auth attempts per design doc
3. **(HIGH) loadDesign mode** — `loadDesign` now sets `mode` based on `templateId` and auto-recovers `templateParams` from template defaults
4. **(MEDIUM) Schema type erasure** — Changed `z.ZodType` to `z.ZodType<SceneNode>` for proper type inference
5. **(MEDIUM) Thumbnail double-wrapping** — Store full data URL in DB, return as-is from API
6. **(MEDIUM) New editor race condition** — Added `creatingRef` guard and removed `designName` from effect dependencies
7. **(LOW) Designs list payload** — Removed `sceneGraph` from list endpoint select
8. **(LOW) Missing shelf params** — Added `depth` and `thickness` fields to NodeParamsEditor
9. **(LOW) Buggy test** — Removed invalid `updateNodeParams` call in undo/redo test
10. **(LOW) Toaster** — Removed from file structure (MVP uses inline error messages)
