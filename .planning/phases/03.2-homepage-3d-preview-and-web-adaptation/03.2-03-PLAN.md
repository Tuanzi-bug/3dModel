---
phase: 03.2-homepage-3d-preview-and-web-adaptation
plan: 3
type: execute
wave: 3
depends_on:
  - 03.2-01
  - 03.2-02
files_modified:
  - packages/web/src/app/page.tsx
  - packages/web/src/components/landing/HeroPreview.tsx
  - packages/web/src/__tests__/app/landing-page.test.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-03
  - HERO-04
must_haves:
  truths:
    - The landing hero keeps its title, supporting copy, and CTA visible immediately; the preview is an enhancement layer, not the primary content.
    - Device-specific interaction hints are present without introducing explicit editor-style control buttons into the homepage hero.
    - Replacing the placeholder does not cause layout collapse, blank states, or obvious hero-level visual jumps between poster and live 3D.
  artifacts:
    - path: packages/web/src/app/page.tsx
      provides: Landing-page integration that replaces the placeholder with the real homepage hero preview
      contains: HeroPreview
    - path: packages/web/src/components/landing/HeroPreview.tsx
      provides: Final integrated hinting, wrapper sizing, and stable hero composition behavior
      contains: 拖拽
    - path: packages/web/src/__tests__/app/landing-page.test.tsx
      provides: Landing-page regression coverage that the hero copy and CTA remain intact while the preview is integrated
      contains: 开始设计
  key_links:
    - from: packages/web/src/app/page.tsx
      to: packages/web/src/components/landing/HeroPreview.tsx
      via: Landing-page hero swaps the old placeholder card for the integrated preview component
      pattern: HeroPreview
    - from: packages/web/src/__tests__/app/landing-page.test.tsx
      to: packages/web/src/app/page.tsx
      via: Regression coverage proves the homepage still prioritizes product copy and CTA after the 3D integration lands
      pattern: 开始设计
---

<objective>
Integrate the new preview into the landing hero without sacrificing homepage clarity or stability.

Purpose: make the Phase 3.2 preview feel like part of the homepage instead of a technical widget bolted underneath it.
Output: the old placeholder is removed, the real preview sits in the hero with stable layout, and landing-page tests prove the CTA-first hierarchy is preserved.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
@packages/web/src/app/page.tsx
@packages/web/src/components/landing/HeroPreview.tsx
@packages/web/src/__tests__/app/editor-pages.test.tsx

<interfaces>
From `packages/web/src/app/page.tsx`:
```tsx
export default function LandingPage() {
  return (
    <main>
      {/* hero content */}
      {/* preview slot */}
    </main>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the homepage hierarchy around CTA-first hero content</name>
  <files>packages/web/src/__tests__/app/landing-page.test.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/app/page.tsx
packages/web/src/__tests__/app/editor-pages.test.tsx
  </read_first>
  <behavior>
    - The landing page still renders the core marketing copy and CTA immediately after the preview integration.
    - The old placeholder text no longer appears once the real preview component is wired in.
    - The test stays resilient by asserting the user-visible hero contract, not R3F internals.
  </behavior>
  <action>Write a focused landing-page test that protects the homepage hierarchy before changing the page. Assert that the hero title, supporting copy, and primary CTA are still present, and that the old placeholder copy is gone once `HeroPreview` replaces it. Mock or stub the preview component boundary as needed so the test remains stable in jsdom.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx` passes.
    - The test proves the page still prioritizes homepage copy and CTA rather than depending on live 3D.
    - The placeholder-specific copy is no longer the rendered hero contract.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx</automated>
  </verify>
  <done>The landing page's information hierarchy is locked before the real preview is inserted.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the landing placeholder with the integrated preview and device-aware hints</name>
  <files>packages/web/src/app/page.tsx, packages/web/src/components/landing/HeroPreview.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/app/page.tsx
packages/web/src/components/landing/HeroPreview.tsx
packages/web/src/__tests__/app/landing-page.test.tsx
  </read_first>
  <behavior>
    - The landing hero swaps the placeholder for the new preview without changing the headline/CTA flow.
    - Hint copy adapts to desktop vs. touch expectations and stays visually light.
    - The integrated wrapper maintains a stable aspect ratio and composition across poster, loading, ready, and fallback states.
  </behavior>
  <action>Replace the placeholder card in `page.tsx` with the real `HeroPreview` component, then tune the wrapper, hint copy, and surrounding layout so the hero feels intentional rather than bolted on. Keep the hierarchy clear: copy and CTA stay primary, the preview reads as the visual proof point, and the page should not introduce extra control chrome or interaction instructions that feel like editor UI.</action>
  <acceptance_criteria>
    - `packages/web/src/app/page.tsx` no longer renders the old placeholder text or icon-only card.
    - `packages/web/src/components/landing/HeroPreview.tsx` presents subtle, device-aware interaction hints without explicit reset/zoom buttons.
    - The hero integration keeps a stable preview footprint across poster and live states.
  </acceptance_criteria>
  <done>The homepage hero now contains the approved preview experience instead of a placeholder.</done>
</task>

</tasks>

<verification>
Run the landing-page test after integrating the preview. Any integration that hides the CTA behind preview loading or leaves the old placeholder copy reachable is incomplete.
</verification>

<success_criteria>
- The homepage preview looks integrated into the hero rather than attached after the CTA.
- The core hero message remains understandable and actionable before live 3D is ready.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-homepage-3d-preview-and-web-adaptation-03-SUMMARY.md`
</output>
