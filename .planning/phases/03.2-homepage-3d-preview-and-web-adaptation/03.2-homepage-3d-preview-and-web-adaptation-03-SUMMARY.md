---
phase: 03.2-homepage-3d-preview-and-web-adaptation
plan: 03
subsystem: ui
tags: [homepage, hero, landing, hierarchy, hinting, integration]
requires:
  - phase: 03.2-homepage-3d-preview-and-web-adaptation
    provides: Poster-first shell and live scene components for the homepage hero
provides:
  - Homepage hero now renders the real preview component instead of placeholder chrome
  - Device-aware interaction hinting without explicit control buttons
  - Landing-page regression coverage that preserves CTA-first hierarchy
affects: [homepage, hero, landing, copy, testing]
tech-stack:
  added: []
  patterns: [CTA-first hero integration, device-aware homepage interaction hinting]
key-files:
  created:
    - packages/web/src/__tests__/app/landing-page.test.tsx
  modified:
    - packages/web/src/app/page.tsx
    - packages/web/src/components/landing/HeroPreview.tsx
key-decisions:
  - "The homepage keeps its existing headline and CTA structure; the preview sits in an upgraded visual frame instead of taking over the hero copy block."
  - "Ready-state hint copy now adapts between desktop wheel zoom and touch pinch zoom without adding visible control chrome."
patterns-established:
  - "Homepage route tests should mock the heavy preview component boundary and assert user-visible hierarchy rather than R3F internals."
requirements-completed: [HERO-01, HERO-03, HERO-04]
duration: 14m
completed: 2026-03-30
---

# Phase 03.2 Plan 03 Summary

**Homepage hero now integrates the real preview component while preserving CTA-first hierarchy and device-aware hinting**

## Accomplishments

- Replaced the old landing-page placeholder card with the actual `HeroPreview` experience inside a more polished hero frame.
- Updated the preview hint copy so desktop users see wheel-zoom guidance while touch users see pinch-zoom guidance.
- Added a focused landing-page regression test proving the headline, primary CTA, and preview component all remain present while the old placeholder text disappears.

## Verification

- `docker compose exec -T web sh -lc 'pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx src/__tests__/components/HeroPreview.test.tsx'`

## Notes

- This plan kept the existing landing-page structure and visual language intact; it upgraded the preview slot instead of redesigning the whole page.
- Per the current user constraint, commit hashes for this plan will be filled in during the phase-end batch commit pass.

---
*Phase: 03.2-homepage-3d-preview-and-web-adaptation*
*Completed: 2026-03-30*
