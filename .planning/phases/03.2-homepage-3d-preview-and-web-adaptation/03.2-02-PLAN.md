---
phase: 03.2-homepage-3d-preview-and-web-adaptation
plan: 2
type: execute
wave: 2
depends_on:
  - 03.2-01
files_modified:
  - packages/web/src/components/landing/HeroPreviewScene.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
must_haves:
  truths:
    - The live preview stays intentionally lightweight and product-adjacent instead of turning into a homepage copy of the editor viewport.
    - Camera interaction allows rotate and zoom on desktop and touch while pan remains disabled.
    - Auto-rotation pauses for user interaction, then returns the camera to the default showcase angle before motion resumes.
  artifacts:
    - path: packages/web/src/components/landing/HeroPreviewScene.tsx
      provides: Lightweight R3F landing-page scene with constrained camera behavior and simple product-adjacent presentation
      contains: OrbitControls
  key_links:
    - from: packages/web/src/components/landing/HeroPreviewScene.tsx
      to: packages/web/src/components/landing/HeroPreview.tsx
      via: Scene callbacks report ready, interaction, and failure state back to the poster-first shell
      pattern: onReady
    - from: packages/web/src/components/landing/HeroPreviewScene.tsx
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: Landing interaction reuses the established OrbitControls patterns without inheriting editor-specific scene/state wiring
      pattern: OrbitControls
---

<objective>
Build the live 3D portion of the homepage preview without letting it grow into an editor feature.

Purpose: deliver the actual interactive scene contract promised by Phase 3.2 while keeping the implementation thin and homepage-appropriate.
Output: a lightweight landing preview scene with product-adjacent visuals, rotate/zoom interaction, and controlled auto-rotation recovery behavior.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
@packages/web/src/components/landing/HeroPreview.tsx
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/components/editor/SceneRenderer.tsx

<interfaces>
Target interface for `packages/web/src/components/landing/HeroPreviewScene.tsx`:
```tsx
type HeroPreviewSceneProps = {
  autoRotate: boolean
  onReady: () => void
  onInteractionStart: () => void
  onInteractionEnd: () => void
  onError: (error?: unknown) => void
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the lightweight landing-page scene with constrained camera controls</name>
  <files>packages/web/src/components/landing/HeroPreviewScene.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/components/landing/HeroPreview.tsx
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/components/editor/SceneRenderer.tsx
  </read_first>
  <behavior>
    - The scene renders a simplified product-adjacent cube/stage treatment rather than a full shelf editor scene.
    - Orbit interaction supports rotation plus zoom but not panning, and the scene remains isolated from editor stores and sceneGraph persistence.
    - The scene can report when it is ready so the shell knows when to swap from poster to live 3D.
  </behavior>
  <action>Create the landing-page preview scene with React Three Fiber and Drei using the smallest viable surface. Keep lighting, material, and staging polished enough to feel related to ShelfCraft, but do not import editor sceneGraph data or toolbar chrome. If an initialization or runtime rendering path can fail, surface that back through the shell instead of swallowing it.</action>
  <acceptance_criteria>
    - `packages/web/src/components/landing/HeroPreviewScene.tsx` uses rotate/zoom-only controls with pan disabled.
    - The rendered object remains a deliberately simplified hero asset, not a hidden copy of the editor scene.
    - The scene exposes a readiness hook or callback that the shell can use to switch from poster to live 3D.
  </acceptance_criteria>
  <done>The homepage has a lightweight live 3D scene that can be slotted into the safe shell from Plan 1.</done>
</task>

<task type="auto">
  <name>Task 2: Add interaction-aware motion control and showcase-angle recovery</name>
  <files>packages/web/src/components/landing/HeroPreviewScene.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/components/landing/HeroPreview.tsx
packages/web/src/components/landing/HeroPreviewScene.tsx
packages/web/src/components/editor/ViewportCanvas.tsx
  </read_first>
  <behavior>
    - Auto-rotation pauses during active interaction so the preview does not fight the user.
    - After an idle delay, the camera returns toward the default showcase angle before auto-rotation resumes.
    - Reduced-motion or shell-driven suppression can disable continuous rotation without removing manual rotate/zoom behavior.
  </behavior>
  <action>Implement the scene's motion behavior around a clear homepage contract: subtle auto-rotation when allowed, pause on drag/zoom, then a gentle return to the intended hero framing after the user stops. Keep this logic local to the landing preview scene and driven by explicit props from the shell rather than hard-coding it globally.</action>
  <acceptance_criteria>
    - Interaction events can pause auto-rotation and notify the shell about active user control.
    - The scene has an explicit return-to-default-angle path before auto-rotation resumes.
    - Motion suppression remains compatible with Plan 1's poster-first and reduced-motion requirements.
  </acceptance_criteria>
  <done>The hero scene feels alive when idle but cooperative when the user touches it.</done>
</task>

</tasks>

<verification>
Treat any scene implementation that introduces pan, editor chrome, or sceneGraph persistence as out of scope. The landing preview must remain a thin showcase component.
</verification>

<success_criteria>
- The homepage preview delivers real rotate/zoom interaction without becoming a mini editor.
- The motion model matches the approved homepage behavior: animate, pause on interaction, return to showcase, resume when appropriate.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-homepage-3d-preview-and-web-adaptation-02-SUMMARY.md`
</output>
