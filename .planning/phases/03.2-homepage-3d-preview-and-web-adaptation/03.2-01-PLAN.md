---
phase: 03.2-homepage-3d-preview-and-web-adaptation
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/landing/HeroPreview.tsx
  - packages/web/src/components/landing/HeroPreviewPoster.tsx
  - packages/web/src/__tests__/components/HeroPreview.test.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-03
  - HERO-04
must_haves:
  truths:
    - The landing hero starts from a stable poster-style presentation and only upgrades into live 3D when the enhancement is actually ready.
    - Unsupported or failed enhancement states fall back to the poster without leaving an empty or visibly broken preview container, while motion-suppressed states can still preserve manual interaction.
    - The preview shell remains isolated from editor-specific store and sceneGraph logic so the homepage enhancement stays lightweight.
  artifacts:
    - path: packages/web/src/components/landing/HeroPreview.tsx
      provides: Poster-first state shell that coordinates loading, ready, fallback, and reduced-motion behavior for the homepage preview
      contains: HeroPreview
    - path: packages/web/src/components/landing/HeroPreviewPoster.tsx
      provides: Stable poster-style hero visual used for initial load and fallback states
      contains: HeroPreviewPoster
    - path: packages/web/src/__tests__/components/HeroPreview.test.tsx
      provides: Component-level regression coverage for poster-first lifecycle and safe fallback states
      contains: poster-first
  key_links:
    - from: packages/web/src/components/landing/HeroPreview.tsx
      to: packages/web/src/components/landing/HeroPreviewPoster.tsx
      via: Initial load and fallback states reuse the same poster contract so layout and composition remain stable
      pattern: HeroPreviewPoster
    - from: packages/web/src/components/landing/HeroPreview.tsx
      to: packages/web/src/app/page.tsx
      via: The hero preview shell becomes the landing-page replacement for the old placeholder block
      pattern: HeroPreview
---

<objective>
Define the homepage hero preview shell before scene behavior and landing-page integration start.

Purpose: lock the poster-first enhancement path and graceful fallback contract so later scene work cannot turn the hero into a fragile blank canvas.
Output: a reusable landing-page preview shell, a stable poster component, and component tests that document the required loading and fallback behavior.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
@packages/web/src/app/page.tsx
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/__tests__/components/LoginPage.test.tsx

<interfaces>
Target interface for `packages/web/src/components/landing/HeroPreview.tsx`:
```tsx
export function HeroPreview(): JSX.Element
```

Target shell responsibilities:
```ts
type PreviewShellState = 'poster' | 'loading' | 'ready' | 'fallback'
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the poster-first preview lifecycle with component tests</name>
  <files>packages/web/src/__tests__/components/HeroPreview.test.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/app/page.tsx
packages/web/src/__tests__/components/LoginPage.test.tsx
  </read_first>
  <behavior>
    - The initial render exposes the poster contract immediately instead of waiting for live 3D to succeed.
    - Unsupported or errored states keep the hero preview visually present through the poster path, and motion-suppressed states remain visually stable without requiring continuous animation.
    - The shell state contract stays testable without requiring real WebGL in Vitest.
  </behavior>
  <action>Write component tests first for the future `HeroPreview` shell. Cover the initial poster render, the handoff into the live preview when it signals readiness, and the fallback path when runtime capability or render safety says the enhancement should not continue. Prefer mocking the inner scene component boundary rather than trying to stand up real Three.js in jsdom.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/HeroPreview.test.tsx` passes.
    - The tests prove the hero never renders as an empty placeholder while the 3D enhancement is unresolved.
    - The tests explicitly cover at least one graceful fallback trigger and one reduced-motion or motion-suppression path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/HeroPreview.test.tsx</automated>
  </verify>
  <done>The preview shell contract is documented in tests before implementation details expand.</done>
</task>

<task type="auto">
  <name>Task 2: Implement a stable hero preview shell and poster fallback component</name>
  <files>packages/web/src/components/landing/HeroPreview.tsx, packages/web/src/components/landing/HeroPreviewPoster.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/app/page.tsx
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/__tests__/components/HeroPreview.test.tsx
  </read_first>
  <behavior>
    - The shell owns the poster/loading/ready/fallback state transitions for the homepage enhancement.
    - The poster component is suitable both for the initial render and for degraded/fallback states so layout stays stable.
    - Reduced-motion or safety-driven suppression disables continuous motion without removing manual interaction capability from the eventual scene path.
  </behavior>
  <action>Create a dedicated landing-page preview shell that is client-safe but lightweight. It should render the poster immediately, expose a clean boundary for the scene implementation to report readiness or failure, and keep the same wrapper dimensions across all states. Keep any capability probing or motion-preference logic inside this homepage component tree; do not reuse editor store infrastructure or page-level auth/data wiring.</action>
  <acceptance_criteria>
    - `packages/web/src/components/landing/HeroPreview.tsx` defines a poster-first state machine rather than swapping directly from placeholder to canvas.
    - `packages/web/src/components/landing/HeroPreviewPoster.tsx` provides the shared visual for both initial and fallback states.
    - The shell can distinguish ready vs. fallback behavior without importing editor store modules.
  </acceptance_criteria>
  <done>The landing page has a stable, reusable preview shell that later plans can safely plug scene behavior and hero integration into.</done>
</task>

</tasks>

<verification>
Run the HeroPreview component test after implementation. Any solution that still leaves a blank placeholder during load or error states is incomplete, even if the 3D canvas itself works.
</verification>

<success_criteria>
- The homepage preview has a poster-first lifecycle instead of a brittle canvas-first lifecycle.
- Fallback and reduced-motion behavior are locked before scene polish starts.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-homepage-3d-preview-and-web-adaptation-01-SUMMARY.md`
</output>
