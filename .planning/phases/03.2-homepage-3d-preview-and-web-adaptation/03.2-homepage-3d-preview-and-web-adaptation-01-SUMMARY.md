---
phase: 03.2-homepage-3d-preview-and-web-adaptation
plan: 01
subsystem: ui
tags: [homepage, hero, preview, poster-first, fallback, reduced-motion]
requires:
  - phase: 03.1-precision-usability-bugfixes
    provides: Stable web test workflow and existing R3F interaction patterns
provides:
  - Poster-first homepage preview shell with explicit ready and fallback states
  - Stable poster artwork component reused for initial and degraded states
  - Focused component regression coverage for loading, ready, fallback, and reduced-motion behavior
affects: [homepage, hero, landing, testing]
tech-stack:
  added: []
  patterns: [poster-first enhancement shell, injectable scene boundary for component testing]
key-files:
  created:
    - packages/web/src/components/landing/HeroPreview.tsx
    - packages/web/src/components/landing/HeroPreviewPoster.tsx
    - packages/web/src/__tests__/components/HeroPreview.test.tsx
  modified: []
key-decisions:
  - "The homepage preview shell owns poster, ready, and fallback state transitions instead of letting the scene decide layout behavior."
  - "The inner scene stays injectable for component tests so the shell can be verified without real WebGL."
patterns-established:
  - "Homepage 3D features should degrade to a stable visual poster instead of leaving an empty canvas-shaped placeholder."
requirements-completed: [HERO-01, HERO-03, HERO-04]
duration: 15m
completed: 2026-03-30
---

# Phase 03.2 Plan 01 Summary

**Poster-first homepage preview shell with safe fallback and reduced-motion-aware enhancement gating**

## Accomplishments

- Added a dedicated landing-page preview shell that always shows a poster-stable hero surface before any live 3D enhancement succeeds.
- Created a reusable poster component so initial, loading, and fallback states keep the same composition instead of jumping between unrelated layouts.
- Locked the shell behavior with focused component tests covering unresolved load, ready transition, error fallback, and reduced-motion auto-rotation suppression.

## Verification

- `docker compose exec -T web sh -lc 'pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/HeroPreview.test.tsx'`

## Notes

- Per the current user constraint, task-level `git add`/`git commit` is deferred until the whole Phase 03.2 execution finishes; commit hashes will be backfilled during the phase closeout pass.
- The live scene implementation remains intentionally separate from the shell so Wave 2 can add real 3D behavior without destabilizing the hero layout contract.

---
*Phase: 03.2-homepage-3d-preview-and-web-adaptation*
*Completed: 2026-03-30*
