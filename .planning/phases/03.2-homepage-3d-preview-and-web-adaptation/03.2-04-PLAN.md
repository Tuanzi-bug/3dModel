---
phase: 03.2-homepage-3d-preview-and-web-adaptation
plan: 4
type: execute
wave: 4
depends_on:
  - 03.2-01
  - 03.2-02
  - 03.2-03
files_modified:
  - packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
  - packages/web/src/components/landing/HeroPreview.tsx
autonomous: true
requirements:
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
must_haves:
  truths:
    - Browser-level coverage proves the homepage preview upgrades from a stable hero state into real interaction instead of only passing mocked component tests.
    - The regression suite exercises both desktop-like and touch-like interaction expectations within the existing Chromium Playwright setup.
    - Fallback or reduced-motion paths are asserted through user-visible behavior rather than accepting a blank canvas as success.
  artifacts:
    - path: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      provides: Browser regression coverage for homepage preview readiness, interaction, and graceful fallback behavior
      contains: 拖拽旋转
    - path: packages/web/src/components/landing/HeroPreview.tsx
      provides: Stable test hooks or observability needed to prove homepage preview states without leaking editor chrome into production UX
      contains: data-testid
  key_links:
    - from: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      to: packages/web/src/components/landing/HeroPreview.tsx
      via: Browser coverage observes poster-first, ready, and fallback behavior through stable homepage selectors
      pattern: data-testid
    - from: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      to: packages/web/src/app/page.tsx
      via: The landing route itself becomes the regression surface for the new hero preview feature
      pattern: 开始设计
---

<objective>
Finish Phase 3.2 with browser evidence that the homepage hero preview works in real navigation and interaction flows.

Purpose: prevent the new homepage showcase from silently regressing after it replaces the placeholder.
Output: homepage Playwright coverage for preview readiness, desktop/touch interaction, and graceful fallback observation using stable selectors.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
@packages/web/src/app/page.tsx
@packages/web/src/components/landing/HeroPreview.tsx
@packages/web/src/components/landing/HeroPreviewScene.tsx
@packages/web/playwright.config.ts
@packages/web/tests/e2e/editor/precision-tools.spec.ts

<interfaces>
Target browser observability:
```html
<section data-testid="hero-preview">...</section>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add browser coverage for homepage preview readiness, interaction, and fallback states</name>
  <files>packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts, packages/web/src/components/landing/HeroPreview.tsx</files>
  <read_first>
.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-CONTEXT.md
packages/web/src/app/page.tsx
packages/web/src/components/landing/HeroPreview.tsx
packages/web/src/components/landing/HeroPreviewScene.tsx
packages/web/playwright.config.ts
packages/web/tests/e2e/editor/precision-tools.spec.ts
  </read_first>
  <behavior>
    - The spec proves the homepage loads with a stable hero state before interaction begins.
    - Desktop-like interaction covers drag rotation and wheel zoom; touch-like interaction covers drag rotation and pinch-style or mobile-emulated zoom expectations within the existing Chromium harness.
    - Fallback or motion-suppressed behavior is asserted through stable selectors or user-visible state instead of allowing silent canvas failure.
  </behavior>
  <action>Create a focused Playwright spec for the homepage hero preview. Prefer one route-level regression file under `tests/e2e/homepage/`. Use the existing Chromium project and add per-test emulation where needed for touch behavior rather than broadening Phase 3.2 into full test-matrix work. If reliable observation of preview state otherwise is too implicit, add the smallest possible test hooks to `HeroPreview` so the spec can distinguish poster, ready, and fallback states without relying on brittle DOM internals.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/homepage/homepage-hero-preview.spec.ts --project=chromium` passes against the running app.
    - The spec covers poster-first readiness plus at least one interactive desktop path and one touch-oriented path.
    - The spec does not treat a blank hero region as passing fallback behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/homepage/homepage-hero-preview.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 3.2 ends with browser evidence that the landing hero preview works and degrades intentionally.</done>
</task>

</tasks>

<verification>
Run the homepage Playwright spec only after Plans 1-3 land. Any regression suite that asserts only static copy and never proves preview state or interaction behavior is incomplete.
</verification>

<success_criteria>
- The homepage preview is protected by route-level browser coverage instead of only local component tests.
- Desktop and touch expectations both have automated evidence within the existing test harness.
</success_criteria>

<output>
After completion, create `.planning/phases/03.2-homepage-3d-preview-and-web-adaptation/03.2-homepage-3d-preview-and-web-adaptation-04-SUMMARY.md`
</output>
