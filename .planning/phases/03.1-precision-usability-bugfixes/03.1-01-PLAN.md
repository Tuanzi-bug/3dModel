---
phase: 03.1-precision-usability-bugfixes
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/utils/snap.ts
  - packages/core/__tests__/snap.test.ts
  - packages/web/src/components/editor/ViewportCanvas.tsx
autonomous: true
requirements:
  - FIX-05
must_haves:
  truths:
    - Dragging a component stays smooth during pointer movement instead of magnetically jumping on every transform frame.
    - When the drag completes, compatible component anchors still win over grid snapping, with grid as the fallback.
    - Final snapped transforms still persist through the canonical sceneGraph update path instead of a parallel transient precision state.
  artifacts:
    - path: packages/core/src/utils/snap.ts
      provides: Shared snap resolution that still prefers compatible anchors before grid fallback
      contains: resolveSnapPosition
    - path: packages/web/src/components/editor/ViewportCanvas.tsx
      provides: TransformControls integration that keeps drag movement raw and snaps only when the drag ends
      contains: onMouseUp
    - path: packages/core/__tests__/snap.test.ts
      provides: Deterministic regression coverage for anchor-first and grid-fallback outcomes
      contains: anchor
  key_links:
    - from: packages/core/src/utils/snap.ts
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: release-time transform finalization resolves through the shared snap utility instead of per-frame magnetic rewriting
      pattern: resolveSnapPosition
---

<objective>
Repair the precision-movement contract so dragging feels controllable again without giving up snapping assistance.

Purpose: remove the post-Phase-3 "moves too fast" regression caused by always-on snapping.
Output: release-time snap behavior, preserved anchor-first/grid-fallback rules, and focused snap regression tests.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
@packages/core/src/utils/snap.ts
@packages/core/__tests__/snap.test.ts
@packages/web/src/components/editor/ViewportCanvas.tsx

<interfaces>
From `packages/core/src/utils/snap.ts`:
```ts
export function resolveSnapPosition(
  sceneGraph: SceneNode,
  proposedPosition: Vec3,
  options?: SnapOptions,
): SnapResult
```

From `packages/web/src/components/editor/ViewportCanvas.tsx`:
```tsx
<TransformControls
  onObjectChange={(event) => { ... }}
  onMouseUp={() => { ... }}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the snap contract around anchor-first, grid-fallback release behavior</name>
  <files>packages/core/src/utils/snap.ts, packages/core/__tests__/snap.test.ts</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/core/src/utils/snap.ts
packages/core/__tests__/snap.test.ts
  </read_first>
  <behavior>
    - Shared snap resolution still prefers compatible anchor matches over grid fallback.
    - Threshold behavior remains deterministic for the same sceneGraph and proposed release position.
    - The core utility remains reusable and pure instead of embedding drag-state assumptions.
  </behavior>
  <action>Expand the snap tests first so they clearly document the intended contract for release-time snapping: anchor-first when close enough, otherwise grid fallback, and no change when already aligned. Adjust the utility only as needed to support the repaired integration contract; do not move snap math into the canvas component.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/snap.test.ts` passes.
    - `packages/core/__tests__/snap.test.ts` explicitly covers anchor-first and grid-fallback outcomes.
    - `packages/core/src/utils/snap.ts` remains the canonical snap decision path.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/snap.test.ts</automated>
  </verify>
  <done>The repo has a locked snap contract that the viewport can safely consume on drag release.</done>
</task>

<task type="auto">
  <name>Task 2: Rewire transform movement so snapping happens when the drag completes</name>
  <files>packages/web/src/components/editor/ViewportCanvas.tsx</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/core/src/utils/snap.ts
packages/web/src/components/editor/ViewportCanvas.tsx
  </read_first>
  <behavior>
    - `TransformControls` keeps the dragged object and persisted transform in sync during movement without forcibly jumping to snap points every frame.
    - Releasing the drag resolves the final position through the shared anchor-first/grid-fallback snap contract.
    - Rotation behavior remains intact and the existing sceneGraph persistence path stays canonical.
  </behavior>
  <action>Refactor the current transform event handling so `onObjectChange` no longer rewrites positions to snapped values on every frame. Keep raw position/rotation updates during drag, then finalize the position through `resolveSnapPosition()` in the appropriate drag-end event. If the current Drei event timing is insufficient, introduce the smallest possible local release-tracking state inside `ViewportCanvas`, but do not push transient drag-only state into the persisted store.</action>
  <acceptance_criteria>
    - `packages/web/src/components/editor/ViewportCanvas.tsx` no longer sets snapped positions inside the per-frame movement handler.
    - Final drag completion resolves through `resolveSnapPosition()` before the position is persisted for save/reopen continuity.
    - The implementation preserves the existing transform-mode toggle and selection flow.
  </acceptance_criteria>
  <done>Users can drag at a predictable pace and still land on valid snap targets when they let go.</done>
</task>

</tasks>

<verification>
Run the core snap tests first. Treat any implementation that still snaps in the per-frame movement handler as incomplete, even if anchor/grid math itself remains correct.
</verification>

<success_criteria>
- Drag interaction feels manual during movement instead of magnetic.
- Valid component and grid snap assistance still resolves when the drag ends.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-precision-usability-bugfixes/03.1-precision-usability-bugfixes-01-SUMMARY.md`
</output>
