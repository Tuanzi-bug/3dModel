---
phase: 03.1-precision-usability-bugfixes
plan: 4
type: execute
wave: 3
depends_on:
  - 03.1-01
  - 03.1-02
  - 03.1-03
files_modified:
  - packages/web/tests/e2e/editor/precision-tools.spec.ts
autonomous: true
requirements:
  - FIX-05
  - FIX-06
  - FIX-07
must_haves:
  truths:
    - Browser coverage asserts the repaired precision workflow instead of the old always-on snap behavior.
    - The downloaded BOM remains a `.csv` and proves Excel-safe Chinese output in a deterministic testable way.
    - Deselect, focus-selected, and zoom actions are exercised end-to-end before Phase 4 begins.
  artifacts:
    - path: packages/web/tests/e2e/editor/precision-tools.spec.ts
      provides: Browser-level regression coverage for drag snapping, export compatibility, and viewport camera actions together
      contains: 导出清单
  key_links:
    - from: packages/web/tests/e2e/editor/precision-tools.spec.ts
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: end-to-end flow exercises deselect stability and explicit viewport actions against the repaired camera contract
      pattern: 聚焦选中
    - from: packages/web/tests/e2e/editor/precision-tools.spec.ts
      to: packages/web/src/components/editor/Header.tsx
      via: browser download assertion proves the CSV export contract from the shipped header action
      pattern: 导出清单
---

<objective>
Lock the Phase 3.1 bugfixes into browser-level evidence before cross-platform work starts.

Purpose: ensure the repaired precision workflow stays stable instead of regressing silently.
Output: updated Playwright coverage for snap-on-release, Excel-safe CSV export, and stable viewport camera actions.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
@packages/web/src/components/editor/Header.tsx
@packages/web/src/components/editor/ViewPresetToolbar.tsx
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/tests/e2e/editor/precision-tools.spec.ts

<interfaces>
From `packages/web/tests/e2e/editor/precision-tools.spec.ts`:
```ts
test('supports snapping, preset inspection controls, and BOM export together', async ({ page }) => {
  // existing Phase 3 browser flow
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rewrite the precision browser regression around the repaired interaction contract</name>
  <files>packages/web/tests/e2e/editor/precision-tools.spec.ts</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/web/src/components/editor/Header.tsx
packages/web/src/components/editor/ViewPresetToolbar.tsx
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/tests/e2e/editor/precision-tools.spec.ts
  </read_first>
  <behavior>
    - The spec stops asserting immediate snap during property edits and instead verifies the new drag/release precision contract.
    - The download assertion proves the exported BOM is still a `.csv` and that the file bytes preserve readable Chinese content.
    - The spec exercises deselect-without-jump plus the new `聚焦选中`, `放大`, and `缩小` controls.
  </behavior>
  <action>Rewrite the existing Chromium precision spec so it reflects Phase 3.1's repaired behavior. Prefer one representative end-to-end flow with deterministic API route stubs and stable locators. If direct camera assertions are otherwise too implicit, add the smallest test-friendly observation strategy needed to prove deselect stability and explicit focus/zoom behavior. For export verification, inspect the downloaded bytes rather than only decoding text as plain UTF-8 so the Excel-safe encoding contract is actually covered.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/precision-tools.spec.ts --project=chromium` passes against the running app.
    - `packages/web/tests/e2e/editor/precision-tools.spec.ts` no longer asserts the old immediate-snap contract from Phase 3.
    - The spec covers all three bugfix requirements: release-time snapping, Excel-safe CSV export, and viewport focus/zoom behavior.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/precision-tools.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 3.1 ends with browser evidence that the repaired editor interaction model actually works for users.</done>
</task>

</tasks>

<verification>
Run the Playwright spec only after Plans 1-3 land. Any browser coverage that still encodes the old immediate snap assertion or ignores the export bytes is incomplete.
</verification>

<success_criteria>
- The repaired precision workflow is covered end-to-end in Chromium.
- Future regressions in snap timing, export encoding, or camera controls will be caught before Phase 4 work advances.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-precision-usability-bugfixes/03.1-precision-usability-bugfixes-04-SUMMARY.md`
</output>
