---
phase: 03.1-precision-usability-bugfixes
plan: 02
subsystem: output
tags: [csv, bom, export, excel]
requires:
  - phase: 03-smart-snapping-output
    provides: Canonical BOM generation and header export entrypoint
provides:
  - BOM CSV output is now Excel-safe for Chinese text.
  - Core serialization owns the UTF-8 BOM contract.
  - Header-level tests prove the browser download path preserves that contract.
affects: [output, editor-header, packages-core]
tech-stack:
  added: []
  patterns: [utf8 bom csv export, thin header download integration]
key-files:
  created: []
  modified:
    - packages/core/src/utils/bom.ts
    - packages/core/__tests__/bom.test.ts
    - packages/web/src/__tests__/components/editor/Header.test.tsx
key-decisions:
  - "Excel compatibility is handled by a UTF-8 BOM prefix while keeping the file as a standard comma-delimited CSV."
patterns-established:
  - "CSV export compatibility fixes stay in shared serialization utilities instead of drifting into ad-hoc browser-only string handling."
requirements-completed: [FIX-06]
duration: 12m
completed: 2026-03-30
---

# Phase 03.1 Plan 02 Summary

**BOM exports now open in Excel with readable Chinese text while staying plain CSV**

## Accomplishments

- Added a UTF-8 BOM prefix to `serializeBomCsv()` so Excel recognizes the export as UTF-8 without changing delimiter or extension.
- Extended core BOM tests to lock the BOM-prefixed CSV contract.
- Added header export regression coverage that inspects the payload passed into the browser download flow.

## Verification

- `docker compose exec -T web sh -lc 'pnpm --filter @3d-modeler/core exec vitest run __tests__/bom.test.ts'`
- `docker compose exec -T web sh -lc 'pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/Header.test.tsx'`

## Notes

- The user-facing export action and filename contract stayed unchanged; the fix lives in the serializer, not in a new export format.

---
*Phase: 03.1-precision-usability-bugfixes*
*Completed: 2026-03-30*
