---
phase: 03.1-precision-usability-bugfixes
plan: 2
type: execute
wave: 2
depends_on: []
files_modified:
  - packages/core/src/utils/bom.ts
  - packages/core/__tests__/bom.test.ts
  - packages/web/src/components/editor/Header.tsx
  - packages/web/src/__tests__/components/editor/Header.test.tsx
autonomous: true
requirements:
  - FIX-06
must_haves:
  truths:
    - Export remains a standard comma-delimited `.csv`, not `.xlsx` and not a tab-delimited workaround.
    - The downloaded BOM opens in Excel with Chinese text intact instead of mojibake.
    - Export rows still come from canonical sceneGraph-derived BOM data.
  artifacts:
    - path: packages/core/src/utils/bom.ts
      provides: BOM serialization that can emit Excel-safe CSV bytes without changing the logical row content
      contains: csv
    - path: packages/web/src/components/editor/Header.tsx
      provides: Editor export action that downloads the Excel-safe CSV
      contains: 导出清单
    - path: packages/web/src/__tests__/components/editor/Header.test.tsx
      provides: Regression coverage for the user-facing export action contract
      contains: 导出清单
  key_links:
    - from: packages/core/src/utils/bom.ts
      to: packages/web/src/components/editor/Header.tsx
      via: header download uses the core serialization contract instead of hand-built browser-only CSV strings
      pattern: serializeBomCsv
---

<objective>
Make BOM export Excel-safe without changing the file format users already asked for.

Purpose: fix the current Chinese-text mojibake in exported BOM downloads.
Output: Excel-compatible CSV serialization, a thin header download path, and regression tests that protect the format contract.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
@packages/core/src/utils/bom.ts
@packages/core/__tests__/bom.test.ts
@packages/web/src/components/editor/Header.tsx
@packages/web/src/__tests__/components/editor/Header.test.tsx

<interfaces>
From `packages/core/src/utils/bom.ts`:
```ts
export function generateBom(sceneGraph: SceneNode): BomLineItem[]
export function serializeBomCsv(items: BomLineItem[]): string
```

From `packages/web/src/components/editor/Header.tsx`:
```tsx
function handleExportBom() {
  const csv = serializeBomCsv(generateBom(sceneGraph))
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make core BOM serialization explicitly Excel-safe</name>
  <files>packages/core/src/utils/bom.ts, packages/core/__tests__/bom.test.ts</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/core/src/utils/bom.ts
packages/core/__tests__/bom.test.ts
  </read_first>
  <behavior>
    - BOM row content remains unchanged in meaning and delimiter structure.
    - The export contract can emit bytes that Excel reliably interprets as UTF-8 Chinese text.
    - The implementation stays reusable and testable in `packages/core`.
  </behavior>
  <action>Expand the BOM tests first to lock the comma-delimited header/rows and the Excel-safe encoding behavior. Then adjust the serialization contract in `packages/core` so the browser can request an Excel-safe CSV payload without reimplementing CSV rules in `Header.tsx`. Keep the public result easy to consume from the web layer; a dedicated helper or option flag is acceptable as long as the format stays `.csv` with commas.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/bom.test.ts` passes.
    - `packages/core/__tests__/bom.test.ts` covers Excel-safe CSV behavior in addition to the current row content.
    - `packages/core/src/utils/bom.ts` still owns BOM serialization instead of moving encoding logic entirely into the header component.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/bom.test.ts</automated>
  </verify>
  <done>The repo has a reusable CSV-export contract that matches both the existing BOM schema and Excel compatibility needs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update the header export action to use the Excel-safe CSV path</name>
  <files>packages/web/src/components/editor/Header.tsx, packages/web/src/__tests__/components/editor/Header.test.tsx</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/core/src/utils/bom.ts
packages/web/src/components/editor/Header.tsx
packages/web/src/__tests__/components/editor/Header.test.tsx
  </read_first>
  <behavior>
    - Clicking `导出清单` still downloads a `.csv` file from the current design.
    - The header remains a thin integration layer over the core BOM serialization contract.
    - The user-facing action and filename conventions remain stable.
  </behavior>
  <action>Add or extend the header component tests first so the export action contract is covered. Then switch `handleExportBom()` to the Excel-safe serialization/download path. If a `Blob` of bytes is required, keep the core serializer authoritative and let the header only package the returned data for download. Do not switch the extension, delimiter, or button copy.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/Header.test.tsx` passes.
    - `packages/web/src/components/editor/Header.tsx` continues to expose a `导出清单` action backed by core BOM serialization.
    - The export path still suggests a `*-bom.csv` filename.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/Header.test.tsx</automated>
  </verify>
  <done>Users can download a BOM CSV that keeps Chinese text readable in Excel without learning a new export flow.</done>
</task>

</tasks>

<verification>
Run the core BOM tests before the header tests. Any implementation that changes the export to `.xlsx`, tabs, or UI-built CSV strings is out of scope.
</verification>

<success_criteria>
- Export remains a standard `.csv`.
- Excel opens the BOM with readable Chinese text.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-precision-usability-bugfixes/03.1-precision-usability-bugfixes-02-SUMMARY.md`
</output>
