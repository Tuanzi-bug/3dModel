---
phase: 03.1-precision-usability-bugfixes
plan: 3
type: execute
wave: 2
depends_on:
  - 03.1-01
files_modified:
  - packages/web/src/components/editor/ViewPresetToolbar.tsx
  - packages/web/src/components/editor/ViewportCanvas.tsx
  - packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx
autonomous: true
requirements:
  - FIX-07
must_haves:
  truths:
    - Clicking empty space only clears selection and does not cause the camera to jump back to a whole-scene framing.
    - The viewport chrome exposes explicit `聚焦选中`, `放大`, and `缩小` controls.
    - Focus and zoom are camera-only actions and do not mutate sceneGraph data.
  artifacts:
    - path: packages/web/src/components/editor/ViewPresetToolbar.tsx
      provides: Existing viewport toolbar extended with focus and zoom controls
      contains: 聚焦选中
    - path: packages/web/src/components/editor/ViewportCanvas.tsx
      provides: Stable camera behavior on deselect plus explicit focus/zoom wiring
      contains: onPointerMissed
    - path: packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx
      provides: Regression coverage for the new toolbar actions
      contains: 放大
  key_links:
    - from: packages/web/src/components/editor/ViewPresetToolbar.tsx
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: explicit camera actions are routed through the existing viewport-control surface instead of selection side effects
      pattern: 聚焦选中
---

<objective>
Repair the viewport so deselection is stable and camera recovery is explicit.

Purpose: stop the post-Phase-3 camera jump on empty-space clicks and give users predictable focus/zoom tools.
Output: stable deselection behavior, explicit viewport actions, and regression tests for the toolbar contract.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
@.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
@packages/core/src/utils/scene-bounds.ts
@packages/web/src/components/editor/ViewPresetToolbar.tsx
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx

<interfaces>
From `packages/web/src/components/editor/ViewPresetToolbar.tsx`:
```tsx
export type ViewPreset = 'front' | 'side' | 'top' | 'iso'

interface Props {
  activePreset: ViewPreset
  onChange: (preset: ViewPreset) => void
}
```

From `packages/web/src/components/editor/ViewportCanvas.tsx`:
```tsx
const bounds = getSceneBounds(sceneGraph, selectedNodeId) ?? getSceneBounds(sceneGraph)
<Canvas onPointerMissed={() => selectNode(null)} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the viewport toolbar with explicit focus and zoom actions</name>
  <files>packages/web/src/components/editor/ViewPresetToolbar.tsx, packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/web/src/components/editor/ViewPresetToolbar.tsx
packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx
  </read_first>
  <behavior>
    - The existing localized preset toolbar gains explicit `聚焦选中`, `放大`, and `缩小` actions.
    - The toolbar API remains clear enough for `ViewportCanvas` to wire camera behavior without mixing scene mutation into the component.
    - The new controls follow the existing editor chrome instead of introducing a separate panel.
  </behavior>
  <action>Expand the current toolbar tests first, then extend `ViewPresetToolbar` with the new focus and zoom controls. Prefer a small API surface that keeps camera behavior controlled by the parent canvas component. Keep the button labels localized in Chinese and align the control styling with the existing preset buttons.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/ViewPresetToolbar.test.tsx` passes.
    - `packages/web/src/components/editor/ViewPresetToolbar.tsx` renders `聚焦选中`, `放大`, and `缩小`.
    - The toolbar still reports preset changes correctly after the API expansion.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/ViewPresetToolbar.test.tsx</automated>
  </verify>
  <done>The viewport chrome now exposes explicit camera recovery controls instead of leaving users to hidden gestures.</done>
</task>

<task type="auto">
  <name>Task 2: Decouple deselection from camera reframing and wire the new viewport actions</name>
  <files>packages/web/src/components/editor/ViewportCanvas.tsx</files>
  <read_first>
.planning/phases/03.1-precision-usability-bugfixes/03.1-CONTEXT.md
.planning/phases/03.1-precision-usability-bugfixes/03.1-RESEARCH.md
packages/core/src/utils/scene-bounds.ts
packages/web/src/components/editor/ViewPresetToolbar.tsx
packages/web/src/components/editor/ViewportCanvas.tsx
  </read_first>
  <behavior>
    - Clicking empty space clears selection but leaves the current camera position and target alone.
    - `聚焦选中` reframes around the selected component when one exists, while `放大` and `缩小` perform explicit camera-distance adjustments.
    - The existing preset buttons continue to work without fighting the new focus/zoom behavior.
  </behavior>
  <action>Refactor the camera effect wiring so preset-driven reframing and explicit focus actions are separated from selection cleanup. Keep `onPointerMissed()` boring: deselect only. Use the existing bounds utilities for focus calculations, and keep zoom changes in camera/control state rather than sceneGraph mutations. If browser verification needs a deterministic way to observe camera behavior, add the smallest possible test-friendly surface while keeping production UI unchanged.</action>
  <acceptance_criteria>
    - `packages/web/src/components/editor/ViewportCanvas.tsx` no longer reframes the camera solely because `selectedNodeId` became `null`.
    - The viewport exposes working handlers for `聚焦选中`, `放大`, and `缩小`.
    - Camera tools remain separate from sceneGraph persistence and save/reopen history.
  </acceptance_criteria>
  <done>Users can safely deselect and then deliberately recover the view they want instead of being surprised by camera jumps.</done>
</task>

</tasks>

<verification>
Run the toolbar tests first. Any implementation that still auto-fits to the whole scene on deselect or persists camera state into sceneGraph is incomplete.
</verification>

<success_criteria>
- Empty-space clicks do not trigger camera jumps.
- The viewport offers explicit focus and zoom controls that are easy to discover.
</success_criteria>

<output>
After completion, create `.planning/phases/03.1-precision-usability-bugfixes/03.1-precision-usability-bugfixes-03-SUMMARY.md`
</output>
