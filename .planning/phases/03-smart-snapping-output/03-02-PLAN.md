---
phase: 03-smart-snapping-output
plan: 2
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/web/src/components/meshes/LedStripMesh.tsx
  - packages/web/src/components/meshes/BackPanelMesh.tsx
  - packages/web/src/components/meshes/registry.ts
  - packages/web/src/components/editor/ComponentPanel.tsx
  - packages/web/src/components/editor/NodeParamsEditor.tsx
  - packages/web/src/__tests__/components/SceneRenderer.test.tsx
  - packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx
  - packages/web/src/__tests__/components/editor/NodeParamsEditor.test.tsx
autonomous: true
requirements:
  - SNAP-02
must_haves:
  truths:
    - `LED灯带` and `背板` are real editor components, not disabled placeholders.
    - New components render in the viewport and expose editable params in the properties panel.
    - Phase 3 components stay compatible with the current save/load/preview/output pipeline.
  artifacts:
    - path: packages/web/src/components/meshes/LedStripMesh.tsx
      provides: Viewport rendering for LED strip nodes
      contains: LedStripMesh
    - path: packages/web/src/components/meshes/BackPanelMesh.tsx
      provides: Viewport rendering for back panel nodes
      contains: BackPanelMesh
    - path: packages/web/src/components/editor/NodeParamsEditor.tsx
      provides: Parameter controls for Phase 3 components
      contains: LED灯带
  key_links:
    - from: packages/web/src/components/meshes/registry.ts
      to: packages/web/src/components/editor/ComponentPanel.tsx
      via: newly supported components become interactive in the library
      pattern: ledStrip
    - from: packages/web/src/components/editor/NodeParamsEditor.tsx
      to: packages/core/src/types/scene.ts
      via: UI editors align with persisted param contracts for new component types
      pattern: backPanel
---

<objective>
Ship the remaining planned editor components so Phase 3 is building on the full intended component surface.

Purpose: replace disabled placeholders with end-to-end component support for `LED灯带` and `背板`.
Output: mesh implementations, enabled library entries, parameter editors, and focused regression coverage.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-smart-snapping-output/03-CONTEXT.md
@.planning/phases/03-smart-snapping-output/03-RESEARCH.md
@packages/core/src/types/scene.ts
@packages/core/src/schemas/scene.ts
@packages/web/src/components/meshes/registry.ts
@packages/web/src/components/editor/ComponentPanel.tsx
@packages/web/src/components/editor/NodeParamsEditor.tsx
@packages/web/src/__tests__/components/SceneRenderer.test.tsx
@packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx

<interfaces>
From `packages/core/src/types/scene.ts`:
```ts
export interface LedStripParams { length: number; color: string }
export interface BackPanelParams { width: number; height: number; material: 'wood' | 'acrylic' | 'metal' }
```

From `packages/web/src/components/meshes/registry.ts`:
```ts
export const componentRegistry: Partial<Record<SceneNodeType, ComponentType<{ params: any; nodeId: string }>>>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement mesh support and library enablement for Phase 3 components</name>
  <files>packages/web/src/components/meshes/LedStripMesh.tsx, packages/web/src/components/meshes/BackPanelMesh.tsx, packages/web/src/components/meshes/registry.ts, packages/web/src/components/editor/ComponentPanel.tsx, packages/web/src/__tests__/components/SceneRenderer.test.tsx, packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/types/scene.ts
packages/core/src/schemas/scene.ts
packages/web/src/components/meshes/registry.ts
packages/web/src/components/editor/ComponentPanel.tsx
packages/web/src/__tests__/components/SceneRenderer.test.tsx
packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx
  </read_first>
  <behavior>
    - `LED灯带` and `背板` render correctly in the current viewport stack.
    - The component panel no longer presents those two entries as disabled placeholders.
    - Supported-component gating continues to reflect the true renderer/editor boundary.
  </behavior>
  <action>Write or extend focused rendering/panel tests first, then add mesh components and register them. Update the component panel so `LED灯带` and `背板` become interactive while preserving the explicit gating pattern introduced in Phase 2.1. Do not enable any additional components beyond the Phase 3 requirement without a matching renderer/editor path.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/SceneRenderer.test.tsx src/__tests__/components/editor/ComponentPanel.test.tsx` passes.
    - `packages/web/src/components/meshes/registry.ts` contains `ledStrip` and `backPanel`.
    - `packages/web/src/components/editor/ComponentPanel.tsx` no longer marks `LED灯带` and `背板` as `暂未开放`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/SceneRenderer.test.tsx src/__tests__/components/editor/ComponentPanel.test.tsx</automated>
  </verify>
  <done>The Phase 3 component list matches what the editor can actually render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose editable params for LED strip and back panel</name>
  <files>packages/web/src/components/editor/NodeParamsEditor.tsx, packages/web/src/__tests__/components/editor/NodeParamsEditor.test.tsx</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/types/scene.ts
packages/web/src/components/editor/NodeParamsEditor.tsx
  </read_first>
  <behavior>
    - LED strip nodes expose editable `length` and `color` controls.
    - Back panel nodes expose editable `width`, `height`, and `material` controls.
    - The properties panel writes params back through the existing store contract instead of introducing parallel component state.
  </behavior>
  <action>Extend `NodeParamsEditor` so the new components are as editable as existing rods and shelves. Add a targeted component test that renders the editor against representative `ledStrip` and `backPanel` selections and asserts the expected Chinese labels and update flow are present.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/NodeParamsEditor.test.tsx` passes.
    - `packages/web/src/components/editor/NodeParamsEditor.tsx` contains controls for `LED灯带` and `背板`.
    - Param edits continue to flow through `updateNodeParams`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/NodeParamsEditor.test.tsx</automated>
  </verify>
  <done>Phase 3 components are not only placeable, but also truly editable in the properties panel.</done>
</task>

</tasks>

<verification>
Treat any implementation that merely enables the panel entries without renderer and params-editor support as incomplete.
</verification>

<success_criteria>
- `LED灯带` and `背板` are fully usable editor components.
- The new components fit the existing render/edit/save pipeline instead of bypassing it.
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-snapping-output/03-smart-snapping-output-02-SUMMARY.md`
</output>
