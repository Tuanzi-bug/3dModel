---
phase: 03-smart-snapping-output
plan: 4
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
  - 03-03
files_modified:
  - packages/core/src/utils/bom.ts
  - packages/core/src/index.ts
  - packages/core/__tests__/bom.test.ts
  - packages/web/src/components/editor/Header.tsx
  - packages/web/tests/e2e/editor/precision-tools.spec.ts
autonomous: true
requirements:
  - OUT-01
  - SNAP-01
  - SNAP-02
  - SNAP-03
must_haves:
  truths:
    - The current design can export a BOM / parts list from canonical sceneGraph data.
    - BOM output covers the Phase 3 component surface, including `LED灯带` and `背板`.
    - Browser coverage proves snapping, precision inspection tools, and export behavior together before Phase 4 planning starts.
  artifacts:
    - path: packages/core/src/utils/bom.ts
      provides: Reusable sceneGraph-to-BOM aggregation and serialization helpers
      contains: generateBom
    - path: packages/web/src/components/editor/Header.tsx
      provides: User-facing export action in the current editor chrome
      contains: 导出清单
    - path: packages/web/tests/e2e/editor/precision-tools.spec.ts
      provides: End-to-end evidence for snapping, precision views, and export behavior
      contains: 导出清单
  key_links:
    - from: packages/core/src/utils/bom.ts
      to: packages/web/src/components/editor/Header.tsx
      via: header export action serializes BOM output from canonical sceneGraph data
      pattern: generateBom
    - from: packages/web/tests/e2e/editor/precision-tools.spec.ts
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: browser flow exercises snap-aware editing and preset inspection tools
      pattern: 前视
---

<objective>
Close Phase 3 with a reusable BOM contract and browser evidence that the new precision workflow works end-to-end.

Purpose: deliver a practical parts-list export instead of stopping at editor-only tooling.
Output: pure BOM generation utilities, an editor export action, and a browser regression that covers precision editing + output together.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-smart-snapping-output/03-CONTEXT.md
@.planning/phases/03-smart-snapping-output/03-RESEARCH.md
@.planning/codebase/TESTING.md
@packages/core/src/types/scene.ts
@packages/web/src/components/editor/Header.tsx
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/tests/e2e/editor/freeform-builder.spec.ts
@packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts

<interfaces>
From `packages/core/src/types/scene.ts`:
```ts
export type SceneNode = ...
```

From `packages/web/src/components/editor/Header.tsx`:
```tsx
export function Header()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build reusable BOM aggregation and serialization in packages/core</name>
  <files>packages/core/src/utils/bom.ts, packages/core/src/index.ts, packages/core/__tests__/bom.test.ts</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/types/scene.ts
packages/core/src/index.ts
  </read_first>
  <behavior>
    - BOM output groups or enumerates parts from canonical sceneGraph data in a deterministic way.
    - The output includes the full Phase 3 component surface, including `LED灯带` and `背板`.
    - BOM logic stays pure and reusable rather than being assembled from UI strings in the browser.
  </behavior>
  <action>Write focused core tests first, then add a BOM utility module under `packages/core`. Include both machine-readable line-item output and a lightweight serialization path suitable for browser download, such as CSV text. Keep grouping/format rules explicit in tests so downstream changes do not silently break procurement-facing output.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/bom.test.ts` passes.
    - `packages/core/src/utils/bom.ts` exports a BOM generator and a serialization helper.
    - The BOM tests cover rods, shelves, LED strips, and back panels.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/bom.test.ts</automated>
  </verify>
  <done>Phase 3 has a reusable output contract that later clients can share instead of re-deriving parts lists ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Surface export UX and prove the precision workflow end-to-end in the browser</name>
  <files>packages/web/src/components/editor/Header.tsx, packages/web/tests/e2e/editor/precision-tools.spec.ts</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/utils/bom.ts
packages/web/src/components/editor/Header.tsx
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/tests/e2e/editor/freeform-builder.spec.ts
packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts
  </read_first>
  <behavior>
    - Users can export a BOM / parts list from the current editor surface without leaving the design.
    - Browser coverage exercises snapping, advanced components, preset views/dimensions, and export together.
    - Export UX stays aligned with the current header/action-bar model instead of introducing a separate output page.
  </behavior>
  <action>Add a clear export affordance such as `导出清单` in the header, backed by the pure BOM generator. Then create a new Chromium browser spec that builds a representative design, verifies snapping and preset view tools are present, adds Phase 3 components, triggers export, and asserts the downloaded or generated output contains expected BOM rows. Prefer deterministic route stubs, locators, download assertions, and pollable state over sleeps.</action>
  <acceptance_criteria>
    - `packages/web/src/components/editor/Header.tsx` contains `导出清单`.
    - `packages/web/tests/e2e/editor/precision-tools.spec.ts` covers snapping, preset views or dimension tools, and BOM export in one flow.
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/precision-tools.spec.ts --project=chromium` passes against the running app.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/precision-tools.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 3 ends with browser evidence that precision editing and parts-list export work together in the shipped editor.</done>
</task>

</tasks>

<verification>
Run the core BOM tests before the browser spec. Any export flow that derives rows from UI labels instead of canonical sceneGraph data is a blocker.
</verification>

<success_criteria>
- Users can export a practical parts list from the current design.
- Browser coverage proves the Phase 3 precision workflow before the project advances to Phase 4.
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-snapping-output/03-smart-snapping-output-04-SUMMARY.md`
</output>
