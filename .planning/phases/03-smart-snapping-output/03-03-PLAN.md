---
phase: 03-smart-snapping-output
plan: 3
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - packages/core/src/utils/scene-bounds.ts
  - packages/core/src/index.ts
  - packages/core/__tests__/scene-bounds.test.ts
  - packages/web/src/components/editor/ViewportCanvas.tsx
  - packages/web/src/components/editor/ViewPresetToolbar.tsx
  - packages/web/src/components/editor/DimensionOverlay.tsx
  - packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx
  - packages/web/src/__tests__/components/editor/DimensionOverlay.test.tsx
autonomous: true
requirements:
  - SNAP-03
must_haves:
  truths:
    - Users can switch to preset camera angles without mutating scene data or breaking current viewport controls.
    - Dimension aids derive from current scene/selection bounds instead of hard-coded labels.
    - Inspection tools remain compatible with the existing viewport, gizmo, and selection flow.
  artifacts:
    - path: packages/core/src/utils/scene-bounds.ts
      provides: Reusable scene/selection bounds calculations for dimension aids
      contains: getSceneBounds
    - path: packages/web/src/components/editor/ViewPresetToolbar.tsx
      provides: Preset camera view controls in the editor
      contains: 前视
    - path: packages/web/src/components/editor/DimensionOverlay.tsx
      provides: Width/height/depth dimension aids
      contains: 宽
  key_links:
    - from: packages/core/src/utils/scene-bounds.ts
      to: packages/web/src/components/editor/DimensionOverlay.tsx
      via: overlay labels derive from reusable bounds calculations
      pattern: getSceneBounds
    - from: packages/web/src/components/editor/ViewPresetToolbar.tsx
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: preset view buttons update the existing camera/control stack
      pattern: 等轴
---

<objective>
Improve design inspection so users can verify geometry and scale without leaving the current editor flow.

Purpose: add preset camera views and dimension aids that make precision editing readable in practice.
Output: reusable bounds utilities, view preset controls, and dimension overlays with focused tests.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-smart-snapping-output/03-CONTEXT.md
@.planning/phases/03-smart-snapping-output/03-RESEARCH.md
@packages/core/src/types/scene.ts
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/components/editor/StatusBar.tsx
@packages/web/src/components/editor/SceneRenderer.tsx

<interfaces>
From `packages/web/src/components/editor/ViewportCanvas.tsx`:
```tsx
<Canvas camera={{ position: [3, 3, 3], fov: 50 }}>
```

From `packages/core/src/types/scene.ts`:
```ts
export type Vec3 = [number, number, number]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reusable bounds calculations for dimensions</name>
  <files>packages/core/src/utils/scene-bounds.ts, packages/core/src/index.ts, packages/core/__tests__/scene-bounds.test.ts</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/types/scene.ts
packages/core/src/index.ts
  </read_first>
  <behavior>
    - Scene and selection bounds can be derived from canonical sceneGraph data in a reusable way.
    - Bounds output is stable enough to drive width/height/depth labels and preset-view framing.
    - Bounds logic stays independent of React/Three component state.
  </behavior>
  <action>Write focused core tests first, then add a bounds utility module under `packages/core`. Model it around sceneGraph and optional selected node IDs so viewport tools can derive dimensions and framing from the same source of truth.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/scene-bounds.test.ts` passes.
    - `packages/core/src/utils/scene-bounds.ts` exports reusable bounds helpers.
    - The bounds tests cover both whole-scene and selected-node cases.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/scene-bounds.test.ts</automated>
  </verify>
  <done>Viewport inspection tools can rely on canonical bounds instead of improvised UI numbers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add preset view controls and dimension overlays to the viewport</name>
  <files>packages/web/src/components/editor/ViewportCanvas.tsx, packages/web/src/components/editor/ViewPresetToolbar.tsx, packages/web/src/components/editor/DimensionOverlay.tsx, packages/web/src/__tests__/components/editor/ViewPresetToolbar.test.tsx, packages/web/src/__tests__/components/editor/DimensionOverlay.test.tsx</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/utils/scene-bounds.ts
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/components/editor/StatusBar.tsx
  </read_first>
  <behavior>
    - Users get preset view buttons such as front/side/top/isometric inside the existing viewport chrome.
    - Dimension aids show width/height/depth labels for the current selection or the whole scene when nothing is selected.
    - View changes do not alter persisted scene data or interfere with current selection/gizmo behavior.
  </behavior>
  <action>Create small dedicated viewport helper components instead of bloating `ViewportCanvas` with inline UI. Wire preset view actions into the existing camera/control stack, and render dimension aids from the shared bounds utility. Add focused component tests that assert the toolbar labels and the dimension overlay labels are present and Chinese-localized.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/ViewPresetToolbar.test.tsx src/__tests__/components/editor/DimensionOverlay.test.tsx` passes.
    - `packages/web/src/components/editor/ViewPresetToolbar.tsx` contains preset buttons such as `前视`, `侧视`, `俯视`, and `等轴`.
    - `packages/web/src/components/editor/DimensionOverlay.tsx` contains dimension labels for `宽`, `高`, and `深`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/ViewPresetToolbar.test.tsx src/__tests__/components/editor/DimensionOverlay.test.tsx</automated>
  </verify>
  <done>The viewport becomes readable for inspection work instead of staying a free-orbit-only canvas.</done>
</task>

</tasks>

<verification>
Any view tool that changes the persisted sceneGraph or resets current selection/gizmo interaction is a blocker.
</verification>

<success_criteria>
- Users can quickly inspect a design from meaningful camera angles.
- Dimension aids reflect actual scene bounds instead of hand-maintained labels.
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-snapping-output/03-smart-snapping-output-03-SUMMARY.md`
</output>
