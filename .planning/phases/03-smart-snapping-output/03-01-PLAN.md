---
phase: 03-smart-snapping-output
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/utils/snap.ts
  - packages/core/src/index.ts
  - packages/core/__tests__/snap.test.ts
  - packages/web/src/stores/editor-store.ts
  - packages/web/src/components/editor/ViewportCanvas.tsx
  - packages/web/src/__tests__/stores/editor-store.test.ts
autonomous: true
requirements:
  - SNAP-01
must_haves:
  truths:
    - Placement and movement can resolve to grid or compatible connection snap points instead of persisting arbitrary transforms only.
    - Snap math is reusable and testable under `packages/core`, not buried exclusively inside canvas event handlers.
    - Snapped transforms persist through the existing store/save/reopen pipeline without breaking freeform continuity.
  artifacts:
    - path: packages/core/src/utils/snap.ts
      provides: Pure snap resolution for grid and anchor candidates
      contains: resolveSnap
    - path: packages/web/src/components/editor/ViewportCanvas.tsx
      provides: Viewport integration that applies snapping during placement and movement
      contains: snap
    - path: packages/core/__tests__/snap.test.ts
      provides: Deterministic regression coverage for snap candidate selection
      contains: grid
  key_links:
    - from: packages/core/src/utils/snap.ts
      to: packages/web/src/components/editor/ViewportCanvas.tsx
      via: viewport transforms pass proposed positions through shared snap resolution
      pattern: resolveSnap
    - from: packages/web/src/components/editor/ViewportCanvas.tsx
      to: packages/web/src/stores/editor-store.ts
      via: snapped results commit through the existing transform persistence path
      pattern: updateNodeTransform
---

<objective>
Introduce the precision foundation for Phase 3 by making placement and movement resolve through a reusable snapping contract.

Purpose: move the editor from freeform-only transforms to a deterministic precision workflow.
Output: shared snap utilities, viewport/store integration, and fast tests that prove snap decisions are stable.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-smart-snapping-output/03-CONTEXT.md
@.planning/phases/03-smart-snapping-output/03-RESEARCH.md
@packages/core/src/types/scene.ts
@packages/core/src/utils/scene-tree.ts
@packages/web/src/stores/editor-store.ts
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/__tests__/stores/editor-store.test.ts

<interfaces>
From `packages/web/src/stores/editor-store.ts`:
```ts
updateNodeTransform: (id: string, updates: { position?: Vec3; rotation?: Vec3 }) => void
addNode: (node: SceneNode) => void
```

From `packages/web/src/components/editor/ViewportCanvas.tsx`:
```tsx
<TransformControls
  onObjectChange={(e) => { ...updateNodeTransform(...) }}
/>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a reusable snap resolver in packages/core</name>
  <files>packages/core/src/utils/snap.ts, packages/core/src/index.ts, packages/core/__tests__/snap.test.ts</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/types/scene.ts
packages/core/src/utils/scene-tree.ts
packages/core/src/index.ts
  </read_first>
  <behavior>
    - Grid and anchor snap candidates are resolved by pure sceneGraph-aware logic under `packages/core`.
    - The snap result identifies whether the winning target came from grid or a compatible connection anchor.
    - Snap math is deterministic for the same sceneGraph and proposed transform input.
  </behavior>
  <action>Write focused core tests first, then add a new snap utility module under `packages/core/src/utils/`. Model the API around proposed transforms and the current sceneGraph so the web editor can reuse it for both placement and movement. Do not hide the logic inside React components or Three objects; keep it pure and export it through `packages/core/src/index.ts`.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/snap.test.ts` passes.
    - `packages/core/src/utils/snap.ts` contains an exported snap resolver rather than canvas-local helper code.
    - `packages/core/__tests__/snap.test.ts` covers both grid and compatible-anchor cases.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/snap.test.ts</automated>
  </verify>
  <done>Phase 3 has a reusable snapping contract that later plans can build on safely.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply snapping to placement and transform persistence</name>
  <files>packages/web/src/stores/editor-store.ts, packages/web/src/components/editor/ViewportCanvas.tsx, packages/web/src/__tests__/stores/editor-store.test.ts</files>
  <read_first>
.planning/phases/03-smart-snapping-output/03-CONTEXT.md
.planning/phases/03-smart-snapping-output/03-RESEARCH.md
packages/core/src/utils/snap.ts
packages/web/src/stores/editor-store.ts
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/__tests__/stores/editor-store.test.ts
  </read_first>
  <behavior>
    - New placement and movement flows commit snapped positions instead of arbitrary unsnapped transforms.
    - Existing save/reopen behavior continues to trust the snapped sceneGraph without introducing a second precision state.
    - The freeform editor remains compatible with undo/redo and duplicate flows after snapping is added.
  </behavior>
  <action>Extend the store tests to cover snapped transform persistence, then wire `ViewportCanvas` and any component-placement path through the shared resolver. Keep snapping integration compatible with existing history, selection, and save behavior. If a toggle or status affordance is needed, thread it through the store rather than stashing precision state inside the canvas only.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts` passes.
    - `packages/web/src/components/editor/ViewportCanvas.tsx` no longer writes raw movement results blindly when a snap target is available.
    - `packages/web/src/__tests__/stores/editor-store.test.ts` contains a snapped transform persistence assertion.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts</automated>
  </verify>
  <done>Users can place and move components with persisted precision instead of freehand drift only.</done>
</task>

</tasks>

<verification>
Run the pure core snap tests first, then the focused web store tests. Any implementation that only adds `translationSnap` without a reusable connection-point resolution path is incomplete.
</verification>

<success_criteria>
- Snap decisions are reusable, deterministic, and testable.
- Placement and movement persist snapped results through the existing editor contract.
</success_criteria>

<output>
After completion, create `.planning/phases/03-smart-snapping-output/03-smart-snapping-output-01-SUMMARY.md`
</output>
