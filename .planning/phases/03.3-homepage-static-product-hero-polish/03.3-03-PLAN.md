---
phase: 03.3-homepage-static-product-hero-polish
plan: 3
type: execute
wave: 2
depends_on:
  - 03.3-01
  - 03.3-02
files_modified:
  - packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
autonomous: true
requirements:
  - LAND-01
  - LAND-02
  - LAND-03
  - LAND-04
must_haves:
  truths:
    - Browser coverage reflects the new static hero contract instead of the old interactive 3D behavior.
    - Desktop and mobile checks both prove the hero remains readable, static, and free of viewport-style interaction cues.
    - The regression surface catches horizontal-overflow or copy-regression issues without reviving the old camera-state assertions.
  artifacts:
    - path: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      provides: Route-level browser regression coverage for the static product-led homepage hero
      contains: data-preview-mode
  key_links:
    - from: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      to: packages/web/src/components/landing/HeroPreview.tsx
      via: Browser assertions lock the new static preview contract through stable DOM selectors
      pattern: data-preview-mode
    - from: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      to: packages/web/src/app/page.tsx
      via: Route-level assertions prove the final hero copy and below-the-fold cleanup survived real navigation
      pattern: 模块化货架设计平台
---

<objective>
Replace the outdated interactive homepage browser regression with coverage for the new static product-hero contract.

Purpose: prevent the old live-3D expectations from becoming the automated definition of success after Phase 03.3 lands.
Output: a rewritten Playwright spec that validates the static hero on mobile, intermediate, and desktop breakpoints without camera or canvas interaction assertions.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.3-homepage-static-product-hero-polish/03.3-CONTEXT.md
@.planning/phases/03.3-homepage-static-product-hero-polish/03.3-UI-SPEC.md
@packages/web/src/app/page.tsx
@packages/web/src/components/landing/HeroPreview.tsx
@packages/web/playwright.config.ts
@packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts

<interfaces>
Required browser selector contract:
```html
<section data-testid="landing-hero-copy">...</section>
<section data-testid="hero-preview" data-preview-mode="static">...</section>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the homepage browser regression to the static hero contract</name>
  <files>packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts</files>
  <read_first>
.planning/phases/03.3-homepage-static-product-hero-polish/03.3-CONTEXT.md
.planning/phases/03.3-homepage-static-product-hero-polish/03.3-UI-SPEC.md
packages/web/src/app/page.tsx
packages/web/src/components/landing/HeroPreview.tsx
packages/web/playwright.config.ts
packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
  </read_first>
  <behavior>
    - The browser spec treats the hero as a static product presentation, not an interactive scene widget.
    - Desktop, intermediate, and mobile assertions verify readable copy, visible preview, restrained chips, and the absence of canvas-driven behavior.
    - Relative layout checks prove that the hero copy block stays above the preview at `1024px`, `768px`, and `375px` rather than collapsing into a side-by-side split.
    - The regression suite guards against horizontal overflow on mobile and against old interaction copy returning.
  </behavior>
  <action>Rewrite `packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts` per D-02, D-05, D-07, D-08, D-10, and D-24. Remove the camera-state helper, drag/wheel interaction steps, and any expectation for `data-state`, `data-motion-mode`, or `data-device-mode`. Keep the file path unchanged. Use `data-testid=\"landing-hero-copy\"` plus `data-testid=\"hero-preview\"` bounding boxes to assert at `1024px`, `768px`, and `375px` that the preview top is below the copy block bottom, proving the hero remains single-column with copy above preview. Add a desktop `1024px` test that visits `/`, asserts `data-testid=\"hero-preview\"` is visible with `data-preview-mode=\"static\"`, checks the exact hero copy `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, `开始设计`, and the four chip strings `模块扩展`, `可调层距`, `阳极氧化铝框架`, `木纹层板`, and proves old interaction/loading strings are absent. Add a second `768px` intermediate-breakpoint test that rechecks the static preview contract and repeats the copy-above-preview bounding-box assertion. Add a third test using `devices['iPhone 13']` for `375px` that rechecks the static preview contract, keeps at least the first and last chip visible, repeats the bounding-box assertion, and asserts `document.documentElement.scrollWidth <= window.innerWidth` so the single-column layout does not spill horizontally. If the easiest way to make the absence of canvas behavior explicit is a negative selector assertion such as `page.locator('canvas').toHaveCount(0)`, that is acceptable; do not add any positive canvas visibility or interaction expectations.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/homepage/homepage-hero-preview.spec.ts --project=chromium` passes against the running app.
    - The spec file contains `data-preview-mode`, `landing-hero-copy`, and breakpoint-specific layout assertions for `1024`, `768`, and `375`.
    - The spec file does not contain `hero-preview-camera-state`, `page.mouse.wheel`, `await expect(canvas).toBeVisible()`, or `const canvas =`.
    - The spec file contains the exact chip strings `模块扩展`, `可调层距`, `阳极氧化铝框架`, and `木纹层板`.
    - The spec file asserts that `拖拽旋转`, `正在准备 3D 预览`, and `3D 预览暂时使用海报模式展示` are absent.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/homepage/homepage-hero-preview.spec.ts --project=chromium</automated>
  </verify>
  <done>The browser regression surface now matches the shipped static hero instead of the retired interactive-preview contract.</done>
</task>

</tasks>

<verification>
Run the homepage Playwright spec after Plans 01 and 02 land. The spec must prove at `1024px`, `768px`, and `375px` that `landing-hero-copy` stays above `hero-preview`, and it must not require camera movement or positive canvas interaction to pass.
</verification>

<success_criteria>
- Browser coverage protects the static product-hero outcome on desktop, intermediate, and mobile breakpoints.
- The phase no longer has automated tests that define success in terms of live 3D interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-homepage-static-product-hero-polish/03.3-homepage-static-product-hero-polish-03-SUMMARY.md`
</output>
