---
phase: 03.3-homepage-static-product-hero-polish
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/app/page.tsx
  - packages/web/src/__tests__/app/landing-page.test.tsx
autonomous: true
requirements:
  - LAND-03
  - LAND-04
must_haves:
  truths:
    - The hero stays single-column, with label, headline, supporting copy, and CTA above the preview at every supported breakpoint.
    - The copy becomes more product-led and restrained without changing the page into a new marketing concept or multi-section redesign.
    - The first section below the hero keeps its existing footprint but adopts quieter styling and more concrete product-oriented messaging.
  artifacts:
    - path: packages/web/src/app/page.tsx
      provides: Landing-page hero and first-section integration for the static product-led homepage direction
      contains: 模块化货架设计平台
    - path: packages/web/src/__tests__/app/landing-page.test.tsx
      provides: Route-level regression coverage for hero hierarchy and minimal below-the-fold cleanup
      contains: 先看整体比例
  key_links:
    - from: packages/web/src/app/page.tsx
      to: packages/web/src/components/landing/HeroPreview.tsx
      via: The landing-page hero keeps copy and CTA above the preview instead of overlaying or splitting the layout
      pattern: HeroPreview
    - from: packages/web/src/__tests__/app/landing-page.test.tsx
      to: packages/web/src/app/page.tsx
      via: Route coverage proves the new copy and section cleanup landed without reviving the old motion-led marketing copy
      pattern: 模块化货架设计平台
---

<objective>
Integrate the new static preview into the homepage hero and apply only the approved first-section consistency cleanup.

Purpose: make the first screen feel like a believable product page while keeping scope tightly limited to hero hierarchy and the first section below it.
Output: updated landing-page copy/layout, minimal first-section cleanup, and route tests that lock the new hierarchy.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.3-homepage-static-product-hero-polish/03.3-CONTEXT.md
@.planning/phases/03.3-homepage-static-product-hero-polish/03.3-UI-SPEC.md
@packages/web/src/app/page.tsx
@packages/web/src/__tests__/app/landing-page.test.tsx
@packages/web/src/components/landing/HeroPreview.tsx

<interfaces>
Current route boundary:
```tsx
export default function LandingPage() {
  return <div className="min-h-screen flex flex-col">...</div>
}
```

Required hero structure after this plan:
```text
navbar
  -> landing-hero-copy
      -> hero label
      -> headline
      -> supporting copy
      -> primary and secondary CTA
  -> HeroPreview
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the single-column hero hierarchy and minimal section cleanup with a route test</name>
  <files>packages/web/src/__tests__/app/landing-page.test.tsx</files>
  <read_first>
.planning/phases/03.3-homepage-static-product-hero-polish/03.3-CONTEXT.md
.planning/phases/03.3-homepage-static-product-hero-polish/03.3-UI-SPEC.md
packages/web/src/app/page.tsx
packages/web/src/__tests__/app/landing-page.test.tsx
packages/web/src/components/landing/HeroPreview.tsx
  </read_first>
  <behavior>
    - The hero label, headline, body, and CTA remain readable before the preview and stay in single-column order.
    - The first section below the hero stays present but uses quieter, more concrete product language.
    - The old motion-led or interaction-led copy is blocked from returning.
  </behavior>
  <action>Write a landing-page test first per D-05, D-07, D-08, D-12, D-23, and D-24. Stub `HeroPreview` as `<div data-testid=\"hero-preview-stub\" />`. Assert that the page renders the exact copy `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, and `在进入细节建模前，先确认结构比例、层板关系与材质方向，让方案沟通更接近真实产品展示。`, along with the CTA labels `开始设计` and `查看示例`. Require the hero copy wrapper to expose `data-testid=\"landing-hero-copy\"`, prove the preview stub appears after that wrapper in DOM order, and assert that the first section below the hero contains `先看整体比例`, `再调材质与规格`, and `输出可沟通方案` while the old strings `实时 3D 可视化` and `支持 360° 旋转查看` are absent.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx` passes.
    - The test file contains the exact hero strings `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, and `在进入细节建模前，先确认结构比例、层板关系与材质方向，让方案沟通更接近真实产品展示。`.
    - The test file asserts `data-testid="landing-hero-copy"` and that `hero-preview-stub` follows that wrapper in DOM order.
    - The test file asserts that `实时 3D 可视化` and `支持 360° 旋转查看` are absent.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx</automated>
  </verify>
  <done>The landing-page hierarchy and cleanup boundary are documented before the page rewrite lands.</done>
</task>

<task type="auto">
  <name>Task 2: Rework the hero copy/layout and quiet the first section without expanding scope</name>
  <files>packages/web/src/app/page.tsx</files>
  <read_first>
.planning/phases/03.3-homepage-static-product-hero-polish/03.3-CONTEXT.md
.planning/phases/03.3-homepage-static-product-hero-polish/03.3-UI-SPEC.md
packages/web/src/app/page.tsx
packages/web/src/__tests__/app/landing-page.test.tsx
packages/web/src/components/landing/HeroPreview.tsx
docs/design-system.md
  </read_first>
  <behavior>
    - The homepage hero remains single-column at all breakpoints, with copy above the preview and no split marketing layout.
    - The preview becomes the main visual proof point while copy remains immediately readable and actionable.
    - The first section below the hero becomes quieter and more product-credible without turning into a full homepage redesign.
  </behavior>
  <action>Update `packages/web/src/app/page.tsx` per D-01, D-05, D-07, D-08, D-12, D-23, and D-24 plus the UI-SPEC. Keep the navbar and overall page structure, but change the hero copy to the exact strings `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, and `在进入细节建模前，先确认结构比例、层板关系与材质方向，让方案沟通更接近真实产品展示。`. Wrap the full label/headline/body/CTA block in `data-testid=\"landing-hero-copy\"` so Wave 2 can assert copy-above-preview positioning in the browser. Keep the CTA labels and destinations unchanged as `开始设计` -> `/register` and `查看示例` -> `/login` to avoid scope expansion. Place `HeroPreview` below the copy in a restrained neutral wrapper instead of the current warm glow frame, and keep brand orange `#F97316` reserved for CTA emphasis only. For the first section below the hero, retain the current three-column footprint but replace the loud orange promo treatment with quieter neutral surfaces and the exact card copy `先看整体比例` / `主图预览优先展示宽高深与层板分布，减少首屏演示噪音。`, `再调材质与规格` / `进入编辑器后继续细化杆件、层板与组合参数。`, and `输出可沟通方案` / `从首页预览到后续导出保持一致的产品语言。`. Do not redesign the lower CTA section or footer.</action>
  <acceptance_criteria>
    - `packages/web/src/app/page.tsx` contains the exact strings `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, and `在进入细节建模前，先确认结构比例、层板关系与材质方向，让方案沟通更接近真实产品展示。`.
    - `packages/web/src/app/page.tsx` contains `data-testid="landing-hero-copy"` and `HeroPreview` below that wrapper in DOM order.
    - `packages/web/src/app/page.tsx` contains the exact first-section headings `先看整体比例`, `再调材质与规格`, and `输出可沟通方案`.
    - `packages/web/src/app/page.tsx` no longer contains `实时 3D 可视化`, `支持 360° 旋转查看`, or `bg-orange-100`.
    - `packages/web/src/app/page.tsx` still contains `href=\"/register\"`, `href=\"/login\"`, and `HeroPreview`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx</automated>
  </verify>
  <done>The homepage now presents a calmer product-led hero and only the approved first-section cleanup below it.</done>
</task>

</tasks>

<verification>
Run the landing-page route test after the page rewrite. This plan locks copy, DOM order, and the stable `landing-hero-copy` anchor; Plan 03.3-03 must then prove in the browser at `375px`, `768px`, and `1024px` that `landing-hero-copy` stays above `hero-preview` with no side-by-side split. Any solution that reintroduces motion-led copy, splits the hero, or expands the cleanup beyond the first section is incomplete.
</verification>

<success_criteria>
- The homepage value proposition and primary actions remain clear above the preview.
- Wave 2 browser coverage can compare `landing-hero-copy` and `hero-preview` positions at mobile, intermediate, and desktop widths.
- The first section below the hero supports the new product-led direction without turning into a broader redesign.
</success_criteria>

<output>
After completion, create `.planning/phases/03.3-homepage-static-product-hero-polish/03.3-homepage-static-product-hero-polish-02-SUMMARY.md`
</output>
