---
phase: 02-freeform-builder
plan: 4
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
  - 02-03
files_modified:
  - packages/web/src/__tests__/stores/editor-store.test.ts
  - packages/web/src/__tests__/app/editor-pages.test.tsx
  - packages/web/tests/e2e/dashboard/dashboard.spec.ts
  - packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts
  - packages/web/tests/e2e/editor/freeform-builder.spec.ts
autonomous: true
requirements:
  - FREE-01
  - FREE-02
  - FREE-03
  - FREE-04
  - FREE-05
  - PREV-01
must_haves:
  truths:
    - Freeform entry, manipulation, and preview behavior all have automated evidence before Phase 3 begins.
    - Browser tests prove empty-canvas creation, freeform editing, save/reopen continuity, and dashboard preview visibility without `waitForTimeout(...)` gating core assertions.
    - Store/page regressions catch mode-switch and `templateId: null` persistence bugs before they reach the browser layer.
  artifacts:
    - path: packages/web/tests/e2e/editor/freeform-builder.spec.ts
      provides: End-to-end freeform creation and reopen continuity path
      contains: 空白画布
    - path: packages/web/tests/e2e/dashboard/dashboard.spec.ts
      provides: Dashboard preview-card and empty-canvas entry assertions
      contains: 预览图
    - path: packages/web/src/__tests__/app/editor-pages.test.tsx
      provides: Route bootstrap coverage for `/editor/new?mode=freeform`
      contains: mode=freeform
  key_links:
    - from: packages/web/tests/e2e/editor/freeform-builder.spec.ts
      to: packages/web/src/app/editor/new/NewEditorClient.tsx
      via: `/editor/new?mode=freeform` create flow
      pattern: mode=freeform
    - from: packages/web/tests/e2e/editor/freeform-builder.spec.ts
      to: packages/web/src/hooks/use-auto-save.ts
      via: PATCH payload with `templateId: null`
      pattern: templateId
    - from: packages/web/tests/e2e/dashboard/dashboard.spec.ts
      to: packages/web/src/app/dashboard/page.tsx
      via: preview-image rendering and empty-canvas CTA
      pattern: 空白画布
---

<objective>
Lock the freeform-builder and dashboard-preview behavior in place with automated regression coverage.

Purpose: ensure Phase 2 exits with evidence, not just implementation, before the project moves into snapping and output work.
Output: focused store/page tests and browser paths covering empty-canvas freeform, manipulation, preview cards, and save/reopen continuity.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-freeform-builder/02-CONTEXT.md
@.planning/phases/02-freeform-builder/02-RESEARCH.md
@.planning/codebase/TESTING.md
@packages/web/src/__tests__/stores/editor-store.test.ts
@packages/web/src/__tests__/app/editor-pages.test.tsx
@packages/web/tests/e2e/dashboard/dashboard.spec.ts
@packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts
@packages/web/playwright.config.ts

<interfaces>
From `packages/web/playwright.config.ts`:
```ts
export default defineConfig({
  testDir: './tests/e2e',
  use: { baseURL: process.env.BASE_URL || 'http://localhost:3000' },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend fast regression coverage for freeform entry and conversion</name>
  <files>packages/web/src/__tests__/stores/editor-store.test.ts, packages/web/src/__tests__/app/editor-pages.test.tsx</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/web/src/__tests__/stores/editor-store.test.ts
packages/web/src/__tests__/app/editor-pages.test.tsx
packages/web/src/stores/editor-store.ts
packages/web/src/app/editor/new/NewEditorClient.tsx
  </read_first>
  <behavior>
    - Store tests cover `switchToFreeform()` and any `transformMode` state introduced in Plan 02.
    - Page tests cover `/editor/new?mode=freeform` bootstrap and ensure the initial create request uses `templateId: null`.
    - Reopened freeform designs stay in freeform mode after hydration.
  </behavior>
  <action>Extend the existing store/page tests instead of starting a new harness. Add assertions that `switchToFreeform()` preserves `sceneGraph` while clearing `templateId` and `templateParams`, that `/editor/new?mode=freeform` creates exactly one design with `templateId: null`, and that a saved freeform design reopens with `mode: 'freeform'`. If Plan 02 added `transformMode`, assert its default and setter behavior here as well.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts src/__tests__/app/editor-pages.test.tsx` passes.
    - `packages/web/src/__tests__/app/editor-pages.test.tsx` contains `mode=freeform`.
    - `packages/web/src/__tests__/stores/editor-store.test.ts` contains `switchToFreeform`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts src/__tests__/app/editor-pages.test.tsx</automated>
  </verify>
  <done>Fast regression tests cover the freeform state contract before browser tests run.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser regression paths for freeform editing and dashboard previews</name>
  <files>packages/web/tests/e2e/dashboard/dashboard.spec.ts, packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts, packages/web/tests/e2e/editor/freeform-builder.spec.ts</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/web/tests/e2e/dashboard/dashboard.spec.ts
packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts
packages/web/tests/e2e/editor/editor.spec.ts
packages/web/src/app/dashboard/page.tsx
packages/web/src/app/editor/new/NewEditorClient.tsx
packages/web/src/hooks/use-auto-save.ts
  </read_first>
  <action>Update the dashboard browser spec so it asserts the empty-canvas CTA `空白画布` and preview `<img>` elements with alt text ending in `预览图`. Add a new Chromium spec at `packages/web/tests/e2e/editor/freeform-builder.spec.ts` that starts on `/dashboard`, opens `空白画布`, waits for the initial create request with `templateId: null`, adds a component from the library, uses the `移动` / `旋转` controls, duplicates or deletes a selected node, saves, captures the PATCH payload, reopens the saved design, and asserts the design still behaves as freeform with a preview visible on the dashboard card. Update `keyboard-shortcuts.spec.ts` so duplicate behavior uses `Ctrl+D` / `Cmd+D` and remove `page.waitForTimeout(...)` as a gate for the new freeform continuity assertions.</action>
  <acceptance_criteria>
    - `packages/web/tests/e2e/dashboard/dashboard.spec.ts` asserts `空白画布` and `预览图`.
    - `packages/web/tests/e2e/editor/freeform-builder.spec.ts` proves create -> edit -> save -> reopen continuity for a freeform design without using `page.waitForTimeout(...)` as the core assertion mechanism.
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/dashboard/dashboard.spec.ts tests/e2e/editor/keyboard-shortcuts.spec.ts tests/e2e/editor/freeform-builder.spec.ts --project=chromium` passes against the running app.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/dashboard/dashboard.spec.ts tests/e2e/editor/keyboard-shortcuts.spec.ts tests/e2e/editor/freeform-builder.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 2 ends with deterministic browser coverage for freeform entry, manipulation, shortcut behavior, and preview rendering.</done>
</task>

</tasks>

<verification>
Run the focused Vitest suites first, then the three Playwright specs against the running app. Any new freeform continuity test that relies on `waitForTimeout` instead of locators, route assertions, or pollable state is a blocker.
</verification>

<success_criteria>
- Freeform entry and conversion behavior are covered by fast tests.
- Browser tests prove empty-canvas freeform workflows and preview-card rendering.
- Phase 2 exits with automated evidence for FREE-01 through FREE-05 and PREV-01.
</success_criteria>

<output>
After completion, create `.planning/phases/02-freeform-builder/02-freeform-builder-04-SUMMARY.md`
</output>
