---
phase: 02-freeform-builder
plan: 2
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/web/src/stores/editor-store.ts
  - packages/web/src/components/editor/ViewportCanvas.tsx
  - packages/web/src/components/editor/EditorShell.tsx
  - packages/web/src/components/editor/NodeParamsEditor.tsx
  - packages/web/src/components/editor/PropertiesPanel.tsx
  - packages/web/src/__tests__/stores/editor-store.test.ts
  - packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts
autonomous: true
requirements:
  - FREE-02
  - FREE-03
  - FREE-05
must_haves:
  truths:
    - Freeform mode exposes move and rotate as explicit editing modes instead of a hidden translate-only gizmo.
    - Duplicate and delete are available both from explicit UI controls and keyboard shortcuts.
    - Selected-node transforms continue to round-trip back into the canonical `sceneGraph`.
  artifacts:
    - path: packages/web/src/components/editor/ViewportCanvas.tsx
      provides: Viewport transform controls and action toolbar for freeform editing
      contains: TransformControls
    - path: packages/web/src/components/editor/EditorShell.tsx
      provides: Shortcut handling for duplicate/delete without firing inside form fields
      contains: Control+d
    - path: packages/web/src/components/editor/NodeParamsEditor.tsx
      provides: Numeric transform editing for rotation and position
      contains: 旋转
  key_links:
    - from: packages/web/src/components/editor/ViewportCanvas.tsx
      to: packages/web/src/stores/editor-store.ts
      via: persisted `transformMode` and transform updates
      pattern: transformMode
    - from: packages/web/src/components/editor/EditorShell.tsx
      to: packages/web/src/stores/editor-store.ts
      via: `duplicateNode()` and `removeNode()` shortcuts
      pattern: duplicateNode
    - from: packages/web/src/components/editor/NodeParamsEditor.tsx
      to: packages/web/src/stores/editor-store.ts
      via: rotation/position updates
      pattern: updateNodeTransform
---

<objective>
Expose complete freeform manipulation controls on top of the existing scene editor primitives.

Purpose: satisfy the Phase 2 promise that users can directly compose and manipulate shelf components in freeform mode.
Output: explicit move/rotate controls, duplicate/delete affordances, and transform-aware shortcuts.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-freeform-builder/02-CONTEXT.md
@.planning/phases/02-freeform-builder/02-RESEARCH.md
@packages/web/src/stores/editor-store.ts
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/components/editor/EditorShell.tsx
@packages/web/src/components/editor/NodeParamsEditor.tsx
@packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts

<interfaces>
From `packages/web/src/stores/editor-store.ts`:
```ts
updateNodeTransform(id: string, updates: { position?: Vec3; rotation?: Vec3 }): void
addNode(node: SceneNode): void
removeNode(id: string): void
duplicateNode(id: string): void
```

From `packages/web/src/components/editor/ViewportCanvas.tsx`:
```tsx
<TransformControls object={selectedGroup} mode="translate" />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit move and rotate editing modes</name>
  <files>packages/web/src/stores/editor-store.ts, packages/web/src/components/editor/ViewportCanvas.tsx, packages/web/src/components/editor/NodeParamsEditor.tsx, packages/web/src/__tests__/stores/editor-store.test.ts</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/web/src/stores/editor-store.ts
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/components/editor/NodeParamsEditor.tsx
packages/web/src/__tests__/stores/editor-store.test.ts
  </read_first>
  <behavior>
    - The editor exposes two transform modes named `移动` and `旋转`.
    - `TransformControls` uses the selected transform mode and writes both `position` and `rotation` back into the store.
    - The property editor exposes numeric rotation inputs alongside the existing position inputs for the selected node.
  </behavior>
  <action>Update the store tests first, then add a transform-mode state surface and wire the viewport and property panel to it. Introduce a store field named `transformMode` with the literal values `'translate' | 'rotate'` plus a setter. In `ViewportCanvas.tsx`, add a compact action strip with buttons labeled `移动` and `旋转`; bind `TransformControls mode={transformMode}` and update both `position` and `rotation` from the selected object on change. In `NodeParamsEditor.tsx`, add a second three-column numeric input group labeled `旋转` for X/Y/Z and feed changes into `updateNodeTransform()`.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts` passes.
    - `packages/web/src/stores/editor-store.ts` contains `transformMode` and `setTransformMode`.
    - `packages/web/src/components/editor/ViewportCanvas.tsx` contains the strings `移动` and `旋转` and no longer hard-codes `mode=\"translate\"`.
    - `packages/web/src/components/editor/NodeParamsEditor.tsx` contains a `旋转` input group.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts</automated>
  </verify>
  <done>Move and rotate become explicit freeform editing modes with both gizmo and numeric control paths.</done>
</task>

<task type="auto">
  <name>Task 2: Surface duplicate and delete as complete freeform actions</name>
  <files>packages/web/src/components/editor/ViewportCanvas.tsx, packages/web/src/components/editor/EditorShell.tsx, packages/web/src/components/editor/PropertiesPanel.tsx, packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/components/editor/EditorShell.tsx
packages/web/src/components/editor/PropertiesPanel.tsx
packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts
  </read_first>
  <behavior>
    - A selected node can be duplicated through an explicit button labeled `复制组件` and the shortcut `Ctrl+D` / `Cmd+D`.
    - A selected node can be removed through an explicit button labeled `删除组件` and the existing `Delete` / `Backspace` shortcuts.
    - Shortcuts continue to be suppressed while focus is inside `input`, `textarea`, or `select` elements.
  </behavior>
  <action>Keep the current delete/backspace guard in `EditorShell.tsx`, add duplicate handling for `Ctrl+D` and `Cmd+D`, and render action buttons only when a node is selected. Place `复制组件` and `删除组件` in the editor control surface nearest the selection state, disable them when `selectedNodeId` is null, and route both buttons through the existing store actions instead of re-implementing tree mutations. Update `packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts` to assert `Ctrl+D` / `Cmd+D` duplicate behavior and keep the non-input guard for destructive shortcuts.</action>
  <acceptance_criteria>
    - `packages/web/src/components/editor/EditorShell.tsx` contains `Control+d` or equivalent `Ctrl/Cmd+D` duplicate handling.
    - `packages/web/src/components/editor/ViewportCanvas.tsx` or `packages/web/src/components/editor/PropertiesPanel.tsx` contains `复制组件` and `删除组件`.
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/keyboard-shortcuts.spec.ts --project=chromium` passes against the running app.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/keyboard-shortcuts.spec.ts --project=chromium</automated>
  </verify>
  <done>Duplicate and delete become first-class freeform editing actions across both mouse and keyboard flows.</done>
</task>

</tasks>

<verification>
Run the updated store test and the targeted keyboard-shortcut Playwright spec. Treat any implementation that only adds UI labels without wiring the existing tree actions as a blocker.
</verification>

<success_criteria>
- Users can switch between move and rotate while editing a selected node.
- Duplicate and delete are available from explicit UI plus keyboard shortcuts.
- Transform and duplication/deletion actions still mutate the canonical `sceneGraph`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-freeform-builder/02-freeform-builder-02-SUMMARY.md`
</output>
