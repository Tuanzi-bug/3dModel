---
phase: 02-freeform-builder
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/schemas/design.ts
  - packages/web/src/stores/editor-store.ts
  - packages/web/src/hooks/use-auto-save.ts
  - packages/web/src/app/editor/new/page.tsx
  - packages/web/src/app/editor/new/NewEditorClient.tsx
  - packages/web/src/app/dashboard/page.tsx
  - packages/web/src/components/editor/Header.tsx
  - packages/web/src/app/api/designs/[id]/route.ts
  - packages/web/src/__tests__/stores/editor-store.test.ts
  - packages/web/src/__tests__/app/editor-pages.test.tsx
  - packages/web/src/__tests__/api/designs-route.test.ts
autonomous: true
requirements:
  - FREE-01
  - FREE-04
must_haves:
  truths:
    - Dashboard offers a first-class empty-canvas path that creates a freeform design without requiring a preset.
    - Converting a preset-started design to freeform keeps the current scene graph intact and persists `templateId: null` on later saves.
    - Reopened freeform designs hydrate into `mode: freeform` without reviving preset-regeneration state.
  artifacts:
    - path: packages/web/src/app/dashboard/page.tsx
      provides: Empty-canvas dashboard entry for freeform mode
      contains: 空白画布
    - path: packages/web/src/app/editor/new/NewEditorClient.tsx
      provides: Freeform and preset-aware new-design bootstrap
      contains: mode === 'freeform'
    - path: packages/web/src/stores/editor-store.ts
      provides: Explicit freeform-mode actions and persisted conversion contract
      contains: switchToFreeform
    - path: packages/web/src/app/api/designs/[id]/route.ts
      provides: Template-to-freeform persistence through PATCH
      contains: templateId
  key_links:
    - from: packages/web/src/app/dashboard/page.tsx
      to: packages/web/src/app/editor/new/page.tsx
      via: empty-canvas entry link to `/editor/new?mode=freeform`
      pattern: '/editor/new\\?mode=freeform'
    - from: packages/web/src/hooks/use-auto-save.ts
      to: packages/web/src/app/api/designs/[id]/route.ts
      via: PATCH payload carries `templateId`
      pattern: 'templateId'
    - from: packages/web/src/stores/editor-store.ts
      to: packages/web/src/app/editor/[id]/page.tsx
      via: `loadDesign()` hydrates freeform mode from `templateId: null`
      pattern: 'mode: opts.templateId \\?'
---

<objective>
Turn freeform mode into a first-class editor entry and persistence contract.

Purpose: let users start from an empty canvas and let preset-started designs become true freeform designs without losing the current scene.
Output: empty-canvas dashboard entry, explicit freeform-mode state/actions, and persisted `templateId: null` round-trips.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-freeform-builder/02-CONTEXT.md
@.planning/phases/02-freeform-builder/02-RESEARCH.md
@packages/web/src/stores/editor-store.ts
@packages/web/src/app/dashboard/page.tsx
@packages/web/src/app/editor/new/page.tsx
@packages/web/src/app/editor/new/NewEditorClient.tsx
@packages/web/src/hooks/use-auto-save.ts
@packages/web/src/app/api/designs/[id]/route.ts

<interfaces>
From `packages/web/src/stores/editor-store.ts`:
```ts
export interface EditorState {
  mode: 'template' | 'freeform'
  templateId: string | null
  templateParams: TemplateParams | null
  sceneGraph: SceneNode
  designId: string | null
  designName: string
}
```

From `packages/core/src/schemas/design.ts`:
```ts
export const updateDesignSchema = z.object({
  name: z.string().min(1).max(255).optional(),
  sceneGraph: sceneNodeSchema.optional(),
  thumbnail: z.string().max(2_000_000).nullable().optional(),
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Persist freeform mode as a real design contract</name>
  <files>packages/core/src/schemas/design.ts, packages/web/src/stores/editor-store.ts, packages/web/src/hooks/use-auto-save.ts, packages/web/src/app/api/designs/[id]/route.ts, packages/web/src/__tests__/stores/editor-store.test.ts, packages/web/src/__tests__/api/designs-route.test.ts</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/core/src/schemas/design.ts
packages/web/src/stores/editor-store.ts
packages/web/src/hooks/use-auto-save.ts
packages/web/src/app/api/designs/[id]/route.ts
packages/web/src/__tests__/stores/editor-store.test.ts
packages/web/src/__tests__/api/designs-route.test.ts
  </read_first>
  <behavior>
    - `PATCH /api/designs/[id]` accepts `templateId` updates so a preset-started design can persist `templateId: null` after conversion to freeform.
    - The editor store exposes an explicit freeform conversion action that keeps the current `sceneGraph`, `designId`, and `designName`, but clears `templateId` and `templateParams` and sets `mode: 'freeform'`.
    - Manual save and autosave include `templateId` in the PATCH payload so freeform conversion survives reopen.
  </behavior>
  <action>Expand the store and route tests first, then update the schema, store, save hook, and PATCH route. Add `templateId: z.string().nullable().optional()` to `updateDesignSchema`. In `packages/web/src/stores/editor-store.ts`, add an explicit action named `switchToFreeform()` that preserves `sceneGraph`, `designId`, and `designName`, but sets `mode: 'freeform'`, `templateId: null`, and `templateParams: null`. In `use-auto-save.ts`, include the current `templateId` in the PATCH body for both autosave and `manualSave()`. In `packages/web/src/app/api/designs/[id]/route.ts`, write `templateId` into `updateData` when it is present so `null` clears preset provenance on the persisted design.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts src/__tests__/api/designs-route.test.ts` passes.
    - `packages/core/src/schemas/design.ts` contains `templateId: z.string().nullable().optional()`.
    - `packages/web/src/stores/editor-store.ts` contains `switchToFreeform`.
    - `packages/web/src/hooks/use-auto-save.ts` sends `templateId` in the PATCH payload.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts src/__tests__/api/designs-route.test.ts</automated>
  </verify>
  <done>Freeform mode is no longer just an in-memory flag; it is persisted and restored as part of the saved design contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add empty-canvas entry and preset-to-freeform conversion UX</name>
  <files>packages/web/src/app/dashboard/page.tsx, packages/web/src/app/editor/new/page.tsx, packages/web/src/app/editor/new/NewEditorClient.tsx, packages/web/src/components/editor/Header.tsx, packages/web/src/__tests__/app/editor-pages.test.tsx</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
docs/design-system.md
packages/web/src/app/dashboard/page.tsx
packages/web/src/app/editor/new/page.tsx
packages/web/src/app/editor/new/NewEditorClient.tsx
packages/web/src/components/editor/Header.tsx
packages/web/src/__tests__/app/editor-pages.test.tsx
  </read_first>
  <behavior>
    - Dashboard exposes an empty-canvas entry labeled `空白画布` that links to `/editor/new?mode=freeform`.
    - Visiting `/editor/new?mode=freeform` creates a design exactly once with `templateId: null`, `sceneGraph: emptyRoot`, and the initial name `新建设计`, then replaces the URL with `/editor/{id}`.
    - The editor header shows a secondary action labeled `转为自由搭建` only when the current design is still in preset/template mode, and clicking it invokes the new `switchToFreeform()` action without clearing the current scene.
  </behavior>
  <action>Add or extend `packages/web/src/__tests__/app/editor-pages.test.tsx` before wiring the UI. In `packages/web/src/app/dashboard/page.tsx`, add a first quick-start card labeled `空白画布` with supporting text `从空白场景开始自由搭建` that points to `/editor/new?mode=freeform`. In `packages/web/src/app/editor/new/page.tsx`, normalize both `template` and `mode`. In `NewEditorClient.tsx`, treat `mode === 'freeform'` as a valid entry path: call `resetScene()`, set the store to `mode: 'freeform'`, `templateId: null`, `templateParams: null`, set `designName` to `新建设计`, POST `{ name: '新建设计', templateId: null, sceneGraph: emptyRoot }`, and replace the URL only after the create response returns an ID. In `Header.tsx`, render a secondary button labeled `转为自由搭建` when `mode === 'template'` and `templateId` is truthy, and wire it to the store action without clearing the current `sceneGraph`.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/editor-pages.test.tsx` passes.
    - `packages/web/src/app/dashboard/page.tsx` contains `空白画布`, `从空白场景开始自由搭建`, and `/editor/new?mode=freeform`.
    - `packages/web/src/components/editor/Header.tsx` contains `转为自由搭建`.
    - `packages/web/src/app/editor/new/NewEditorClient.tsx` contains `mode === 'freeform'` and POSTs `templateId: null`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/editor-pages.test.tsx</automated>
  </verify>
  <done>Users can start from an empty canvas and can promote a preset-started scene into freeform editing without losing work.</done>
</task>

</tasks>

<verification>
Run the targeted Vitest suites for store, route, and editor-page bootstrap behavior. Treat any reopen path that rehydrates a converted design back into template mode as a blocker.
</verification>

<success_criteria>
- Dashboard can launch a freeform design directly from an empty canvas.
- A preset-started design can become freeform and stay freeform after save/reopen.
- The freeform conversion contract is persisted through PATCH, not just in client memory.
</success_criteria>

<output>
After completion, create `.planning/phases/02-freeform-builder/02-freeform-builder-01-SUMMARY.md`
</output>
