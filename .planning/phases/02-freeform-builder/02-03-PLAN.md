---
phase: 02-freeform-builder
plan: 3
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - packages/core/src/utils/scene-preview.ts
  - packages/core/src/index.ts
  - packages/core/src/templates/single-shelf.ts
  - packages/core/src/templates/multi-shelf.ts
  - packages/core/src/templates/standalone.ts
  - packages/core/__tests__/scene-preview.test.ts
  - packages/web/src/app/api/designs/route.ts
  - packages/web/src/app/api/designs/[id]/route.ts
  - packages/web/src/__tests__/api/designs-route.test.ts
  - packages/web/src/app/dashboard/page.tsx
autonomous: true
requirements:
  - PREV-01
must_haves:
  truths:
    - Preset cards and saved-design cards show non-empty preview images instead of generic empty placeholders.
    - Template previews and saved-design previews are generated from canonical scene data, not from ad hoc DOM-only screenshots.
    - Saved-design previews are persisted automatically on create/update without changing the database shape.
  artifacts:
    - path: packages/core/src/utils/scene-preview.ts
      provides: Deterministic preview generation from `sceneGraph`
      contains: generateScenePreviewDataUrl
    - path: packages/core/src/templates/single-shelf.ts
      provides: Non-empty preset thumbnail metadata
      contains: thumbnail
    - path: packages/web/src/app/api/designs/[id]/route.ts
      provides: Automatic saved-design thumbnail persistence on PATCH
      contains: thumbnail
    - path: packages/web/src/app/dashboard/page.tsx
      provides: Preview-image rendering for preset and saved-design cards
      contains: 预览图
  key_links:
    - from: packages/core/src/utils/scene-preview.ts
      to: packages/web/src/app/api/designs/route.ts
      via: server-side thumbnail generation on create
      pattern: generateScenePreviewDataUrl
    - from: packages/core/src/utils/scene-preview.ts
      to: packages/core/src/templates/registry.ts
      via: template metadata thumbnail population
      pattern: thumbnail
    - from: packages/web/src/app/dashboard/page.tsx
      to: packages/web/src/app/api/templates/route.ts
      via: preset cards consume template thumbnail metadata
      pattern: thumbnail
---

<objective>
Add a real preview-imagery pipeline for preset entries and saved designs on the dashboard.

Purpose: make dashboard entry visually scannable and satisfy the new preview requirement without coupling previews to the live WebGL viewport.
Output: deterministic scene-derived thumbnails, persisted saved-design previews, and dashboard cards that render them.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-freeform-builder/02-CONTEXT.md
@.planning/phases/02-freeform-builder/02-RESEARCH.md
@packages/core/src/types/design.ts
@packages/core/src/types/template.ts
@packages/core/src/templates/registry.ts
@packages/web/src/app/api/designs/route.ts
@packages/web/src/app/api/designs/[id]/route.ts
@packages/web/src/app/dashboard/page.tsx

<interfaces>
From `packages/core/src/types/template.ts`:
```ts
export interface TemplateDefinition {
  id: string
  name: string
  thumbnail: string
  generate: (params: TemplateParams) => SceneNode
}
```

From `packages/core/src/types/design.ts`:
```ts
export interface DesignDTO {
  thumbnail: string | null
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a deterministic scene-preview generator and populate preset thumbnails</name>
  <files>packages/core/src/utils/scene-preview.ts, packages/core/src/index.ts, packages/core/src/templates/single-shelf.ts, packages/core/src/templates/multi-shelf.ts, packages/core/src/templates/standalone.ts, packages/core/__tests__/scene-preview.test.ts</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/core/src/types/scene.ts
packages/core/src/types/template.ts
packages/core/src/templates/single-shelf.ts
packages/core/src/templates/multi-shelf.ts
packages/core/src/templates/standalone.ts
packages/core/src/index.ts
  </read_first>
  <behavior>
    - A new pure function named `generateScenePreviewDataUrl(sceneGraph)` returns a non-empty image string from a `SceneNode` tree.
    - The three shipped preset templates no longer use `thumbnail: ''`.
    - Template thumbnail generation depends on the template’s default generated scene, not on manual placeholder strings.
  </behavior>
  <action>Add `packages/core/src/utils/scene-preview.ts` with an exported function named `generateScenePreviewDataUrl(sceneGraph: SceneNode): string`. Use a deterministic 2D projection into a compact SVG data URL so the function stays pure and platform-agnostic. Export it from `packages/core/src/index.ts`. In `single-shelf.ts`, `multi-shelf.ts`, and `standalone.ts`, replace `thumbnail: ''` with `thumbnail: generateScenePreviewDataUrl(generate(defaultParams))`. Add a focused core test file that asserts the function returns a `data:image/` string and that the three shipped template definitions expose non-empty thumbnails.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/scene-preview.test.ts` passes.
    - `packages/core/src/utils/scene-preview.ts` contains `generateScenePreviewDataUrl`.
    - `packages/core/src/templates/single-shelf.ts`, `packages/core/src/templates/multi-shelf.ts`, and `packages/core/src/templates/standalone.ts` no longer contain `thumbnail: ''`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/scene-preview.test.ts</automated>
  </verify>
  <done>Preset thumbnails become real metadata generated from canonical scenes instead of empty placeholders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist saved-design previews automatically and render them on the dashboard</name>
  <files>packages/web/src/app/api/designs/route.ts, packages/web/src/app/api/designs/[id]/route.ts, packages/web/src/__tests__/api/designs-route.test.ts, packages/web/src/app/dashboard/page.tsx</files>
  <read_first>
.planning/phases/02-freeform-builder/02-CONTEXT.md
.planning/phases/02-freeform-builder/02-RESEARCH.md
packages/core/src/utils/scene-preview.ts
packages/web/src/app/api/designs/route.ts
packages/web/src/app/api/designs/[id]/route.ts
packages/web/src/__tests__/api/designs-route.test.ts
packages/web/src/app/dashboard/page.tsx
  </read_first>
  <behavior>
    - `POST /api/designs` persists a generated `thumbnail` from the incoming `sceneGraph`.
    - `PATCH /api/designs/[id]` regenerates `thumbnail` when `sceneGraph` changes and the caller does not supply an explicit thumbnail.
    - Dashboard preset and saved-design cards render `<img>` previews with accessible alt text ending in `预览图` and keep fallback copy only when preview data is truly missing.
  </behavior>
  <action>Update the design routes and their tests before changing the dashboard UI. In `packages/web/src/app/api/designs/route.ts`, generate `thumbnail` with `generateScenePreviewDataUrl(parsed.data.sceneGraph)` during create. In `packages/web/src/app/api/designs/[id]/route.ts`, if `parsed.data.thumbnail` is defined, persist it as-is; otherwise, when `parsed.data.sceneGraph` is present, regenerate `thumbnail` from that `sceneGraph`. In `packages/web/src/app/dashboard/page.tsx`, render `t.thumbnail` and `d.thumbnail` with `<img>` elements and the exact alt pattern `${name}预览图`, preserve `无预览图` only as a true fallback, and remove the preset-card empty gradient placeholder as the primary visual state.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/api/designs-route.test.ts` passes.
    - `packages/web/src/app/api/designs/route.ts` and `packages/web/src/app/api/designs/[id]/route.ts` contain `generateScenePreviewDataUrl`.
    - `packages/web/src/app/dashboard/page.tsx` contains `预览图` and renders both `t.thumbnail` and `d.thumbnail`.
    - `pnpm --filter @3d-modeler/web build` passes after the dashboard changes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/api/designs-route.test.ts && pnpm --filter @3d-modeler/web build</automated>
  </verify>
  <done>Saved designs and presets become visually identifiable directly from the dashboard.</done>
</task>

</tasks>

<verification>
Run the core preview-generator test, the design-route test suite, and a production build. Treat any implementation that still leaves shipped templates with empty thumbnails as a blocker.
</verification>

<success_criteria>
- Template metadata returns real non-empty preview images.
- Saved-design thumbnails are generated automatically from scene changes.
- Dashboard cards render previews instead of empty placeholders for the normal path.
</success_criteria>

<output>
After completion, create `.planning/phases/02-freeform-builder/02-freeform-builder-03-SUMMARY.md`
</output>
