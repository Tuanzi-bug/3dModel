---
phase: 03.4-homepage-original-landing-restore
plan: 2
type: execute
wave: 2
depends_on:
  - 03.4-01
files_modified:
  - packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
autonomous: true
requirements:
  - RESTORE-04
must_haves:
  truths:
    - Browser coverage reflects the restored original landing-page baseline instead of the 03.3 static-product preview contract.
    - Desktop, tablet, and mobile assertions all prove the placeholder-based hero remains readable and vertically ordered without horizontal overflow.
    - The regression surface blocks the 03.3 hero copy, chips, and preview-specific selectors from quietly returning.
  artifacts:
    - path: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      provides: Browser regression coverage for the restored homepage baseline
      contains: 3D 预览区域
  key_links:
    - from: packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
      to: packages/web/src/app/page.tsx
      via: Browser assertions prove the real route now matches the restored original landing-page contract
      pattern: 专业的 3D 模块化货架设计工具
---

<objective>
Rewrite homepage browser coverage so automation protects the restored original landing-page baseline.

Purpose: prevent the 03.2 and 03.3 homepage experiments from remaining the automated definition of success after the rollback lands.
Output: Playwright coverage that asserts the restored baseline at desktop, tablet, and mobile breakpoints.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-homepage-original-landing-restore/03.4-CONTEXT.md
@.planning/phases/03.4-homepage-original-landing-restore/03.4-UI-SPEC.md
@packages/web/src/app/page.tsx
@packages/web/playwright.config.ts
@packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts

<interfaces>
Restored browser contract:
```text
hero heading -> 专业的 3D 模块化货架设计工具
supporting copy -> 实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。
placeholder block -> 3D 预览区域
feature cards -> 实时 3D 可视化 / 丰富的模板库 / 快速导出
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the homepage Playwright spec to the restored baseline contract</name>
  <files>packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts</files>
  <read_first>
.planning/phases/03.4-homepage-original-landing-restore/03.4-CONTEXT.md
.planning/phases/03.4-homepage-original-landing-restore/03.4-UI-SPEC.md
packages/web/src/app/page.tsx
packages/web/playwright.config.ts
packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts
  </read_first>
  <behavior>
    - The browser spec verifies the original landing-page baseline instead of any preview-component contract.
    - Desktop, tablet, and mobile checks all confirm the hero remains text-first, with the placeholder block below the hero copy.
    - The regression suite guards against horizontal overflow and against the 03.3 copy/chip contract resurfacing.
  </behavior>
  <action>Rewrite `packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts` to the restored baseline contract while keeping the file path unchanged. Remove expectations for `data-preview-mode`, `landing-hero-copy`, parameter chips, and any preview-component-specific selectors. Add a `1024px` test that visits `/`, asserts the exact heading `专业的 3D 模块化货架设计工具`, the exact supporting copy `实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。`, the CTA labels `开始设计` and `查看示例`, the placeholder label `3D 预览区域`, and the feature-card headings `实时 3D 可视化`, `丰富的模板库`, and `快速导出`. Compare bounding boxes between the hero heading and the `3D 预览区域` placeholder to prove the placeholder sits below the copy. Add a `768px` test that repeats the baseline assertions and vertical-order check. Add an `iPhone 13` test that rechecks the baseline copy, asserts the placeholder remains visible, and verifies `document.documentElement.scrollWidth <= window.innerWidth`. In all browser tests, assert that the old strings `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, `模块扩展`, and `木纹层板` are absent, and optionally assert `page.locator('canvas').toHaveCount(0)` if that is the simplest guard against accidental preview reintroduction.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/homepage/homepage-hero-preview.spec.ts --project=chromium` passes against the running app.
    - The spec file contains the exact strings `专业的 3D 模块化货架设计工具`, `实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。`, and `3D 预览区域`.
    - The spec file does not contain `data-preview-mode`, `landing-hero-copy`, `模块扩展`, or `木纹层板`.
    - The spec file contains breakpoint coverage for `1024`, `768`, and `375` width behavior.
    - The spec file asserts that `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, `先看整体比例`, and `输出可沟通方案` are absent.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/homepage/homepage-hero-preview.spec.ts --project=chromium</automated>
  </verify>
  <done>The browser regression surface now defines success as the restored original homepage baseline.</done>
</task>

</tasks>

<verification>
Run the homepage Playwright spec after Plan 01 lands. The spec must prove that the original hero copy and placeholder block render at `1024px`, `768px`, and `375px`, and it must not depend on any HeroPreview selector or 03.3-specific copy.
</verification>

<success_criteria>
- Browser coverage protects the restored homepage baseline at desktop, tablet, and mobile widths.
- The 03.2 and 03.3 homepage contracts are no longer the automated definition of success.
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-homepage-original-landing-restore/03.4-homepage-original-landing-restore-02-SUMMARY.md`
</output>
