---
phase: 03.4-homepage-original-landing-restore
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/app/page.tsx
  - packages/web/src/__tests__/app/landing-page.test.tsx
  - packages/web/src/__tests__/components/HeroPreview.test.tsx
autonomous: true
requirements:
  - RESTORE-01
  - RESTORE-02
  - RESTORE-03
must_haves:
  truths:
    - The homepage route returns to the original 42a3af0 landing-page copy, CTA hierarchy, and simple placeholder preview block.
    - The homepage no longer imports or renders HeroPreview-based components.
    - Route-level test coverage defines success in terms of the restored baseline, and the obsolete HeroPreview component test is retired.
  artifacts:
    - path: packages/web/src/app/page.tsx
      provides: Restored landing-page baseline with the original hero, placeholder preview, and feature-card copy
      contains: 3D 预览区域
    - path: packages/web/src/__tests__/app/landing-page.test.tsx
      provides: Route-level regression coverage for the restored homepage baseline
      contains: 专业的 3D 模块化货架设计工具
  key_links:
    - from: packages/web/src/__tests__/app/landing-page.test.tsx
      to: packages/web/src/app/page.tsx
      via: Route coverage proves the restored homepage baseline is rendered instead of the superseded 03.3 hero contract
      pattern: 3D 预览区域
---

<objective>
Restore the homepage route to the original landing-page baseline and realign unit-test coverage around that rollback.

Purpose: make the shipped homepage match the earliest simple page the user preferred instead of continuing the 03.2 or 03.3 hero directions.
Output: restored `page.tsx`, a route test that locks the baseline contract, and retirement of the obsolete `HeroPreview` component test.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03.4-homepage-original-landing-restore/03.4-CONTEXT.md
@.planning/phases/03.4-homepage-original-landing-restore/03.4-UI-SPEC.md
@packages/web/src/app/page.tsx
@packages/web/src/__tests__/app/landing-page.test.tsx
@packages/web/src/__tests__/components/HeroPreview.test.tsx

<interfaces>
Restore target from commit `42a3af0`:
```tsx
export default function LandingPage() {
  return (
    <div className="min-h-screen flex flex-col">
      <main>
        <h1>专业的 3D 模块化货架设计工具</h1>
        <p>实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。</p>
        <p>3D 预览区域</p>
      </main>
    </div>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock the original landing-page baseline with a route test</name>
  <files>packages/web/src/__tests__/app/landing-page.test.tsx</files>
  <read_first>
.planning/phases/03.4-homepage-original-landing-restore/03.4-CONTEXT.md
.planning/phases/03.4-homepage-original-landing-restore/03.4-UI-SPEC.md
packages/web/src/app/page.tsx
packages/web/src/__tests__/app/landing-page.test.tsx
  </read_first>
  <behavior>
    - The route test asserts the restored hero headline, supporting copy, CTA labels, preview placeholder, and original feature-card copy.
    - The test blocks the 03.3 hero copy and below-the-fold cleanup copy from returning.
    - The test does not mock or depend on HeroPreview because the restored homepage should not import it at all.
  </behavior>
  <action>Rewrite `packages/web/src/__tests__/app/landing-page.test.tsx` first per D-01 through D-10. Assert that the page renders the exact headline `专业的 3D 模块化货架设计工具`, the exact supporting copy `实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。`, the CTA labels `开始设计` and `查看示例`, the placeholder label `3D 预览区域`, and the original feature-card headings `实时 3D 可视化`, `丰富的模板库`, and `快速导出`. Assert the original CTA destinations remain `/register` and `/login`. Also assert that the superseded strings `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, `先看整体比例`, and `输出可沟通方案` are absent.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx` passes.
    - The test file contains the exact strings `专业的 3D 模块化货架设计工具`, `实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。`, and `3D 预览区域`.
    - The test file asserts the headings `实时 3D 可视化`, `丰富的模板库`, and `快速导出`.
    - The test file asserts that `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, `先看整体比例`, and `输出可沟通方案` are absent.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx</automated>
  </verify>
  <done>The restored homepage baseline is documented before the route rewrite lands.</done>
</task>

<task type="auto">
  <name>Task 2: Restore the homepage route and retire the obsolete HeroPreview component test</name>
  <files>packages/web/src/app/page.tsx, packages/web/src/__tests__/components/HeroPreview.test.tsx</files>
  <read_first>
.planning/phases/03.4-homepage-original-landing-restore/03.4-CONTEXT.md
.planning/phases/03.4-homepage-original-landing-restore/03.4-UI-SPEC.md
packages/web/src/app/page.tsx
packages/web/src/__tests__/app/landing-page.test.tsx
packages/web/src/__tests__/components/HeroPreview.test.tsx
docs/design-system.md
  </read_first>
  <behavior>
    - The homepage route matches the 42a3af0 baseline instead of the 03.3 product-hero composition.
    - `HeroPreview` is removed from the homepage import/render path.
    - The dedicated HeroPreview component test is removed because it locks a superseded homepage contract.
  </behavior>
  <action>Restore `packages/web/src/app/page.tsx` to the original landing-page structure described in the context and UI-SPEC, using commit `42a3af0` as the source of truth. Remove the `HeroPreview` import and restore the centered hero headline `专业的 3D 模块化货架设计工具`, the original supporting copy, the original CTA row, the simple placeholder block with the text `3D 预览区域`, and the original feature-card titles/body copy `实时 3D 可视化`, `丰富的模板库`, and `快速导出`. Keep the nav, CTA section, and footer aligned with the original baseline. Then delete `packages/web/src/__tests__/components/HeroPreview.test.tsx` so the old static-product preview contract no longer defines success.</action>
  <acceptance_criteria>
    - `packages/web/src/app/page.tsx` no longer contains `HeroPreview`, `模块化货架设计平台`, `用更可信的方式预览模块化货架方案`, `先看整体比例`, or `输出可沟通方案`.
    - `packages/web/src/app/page.tsx` contains `专业的 3D 模块化货架设计工具`, `实时 3D 可视化，所见即所得。选择模板，调整尺寸，立即预览你的设计效果。`, `3D 预览区域`, `实时 3D 可视化`, `丰富的模板库`, and `快速导出`.
    - `packages/web/src/app/page.tsx` still contains `href="/register"` and `href="/login"` for the top-level CTAs.
    - `packages/web/src/__tests__/components/HeroPreview.test.tsx` no longer exists.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/landing-page.test.tsx</automated>
  </verify>
  <done>The homepage route is back on the original baseline, and the obsolete HeroPreview unit-test surface is gone.</done>
</task>

</tasks>

<verification>
Run the landing-page route test after restoring the page. A solution is incomplete if `page.tsx` still imports `HeroPreview`, if the placeholder text `3D 预览区域` does not return, or if the old 03.3 hero copy remains reachable.
</verification>

<success_criteria>
- The homepage route once again matches the simple original landing-page baseline.
- Unit tests define success in terms of the restored baseline instead of the superseded 03.3 hero contract.
- The route no longer depends on HeroPreview-based components.
</success_criteria>

<output>
After completion, create `.planning/phases/03.4-homepage-original-landing-restore/03.4-homepage-original-landing-restore-01-SUMMARY.md`
</output>
