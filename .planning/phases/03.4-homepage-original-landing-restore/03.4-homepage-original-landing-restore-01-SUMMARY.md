---
phase: 03.4-homepage-original-landing-restore
plan: 01
subsystem: ui
tags: [homepage, landing, rollback, testing, nextjs]
requires:
  - phase: 03.3-homepage-static-product-hero-polish
    provides: Homepage files and tests that needed to be rolled back from the static product-hero direction
provides:
  - Restored original homepage hero copy and CTA hierarchy
  - Restored placeholder preview block with the original feature-card copy
  - Route-level regression coverage for the original landing-page baseline
affects: [homepage, landing, testing, e2e]
tech-stack:
  added: []
  patterns: [homepage rollback to source baseline, route-level baseline contract testing]
key-files:
  created: []
  modified:
    - packages/web/src/app/page.tsx
    - packages/web/src/__tests__/app/landing-page.test.tsx
    - packages/web/src/__tests__/components/HeroPreview.test.tsx
key-decisions:
  - "Restore the homepage route directly to the 42a3af0 baseline instead of keeping HeroPreview in the path behind conditional logic."
  - "Retire the obsolete HeroPreview component test because it enforced a superseded 03.3 homepage contract."
patterns-established:
  - "Homepage rollback tests should assert the original copy and placeholder contract, not preview-component internals."
requirements-completed: [RESTORE-01, RESTORE-02, RESTORE-03]
duration: 3 min
completed: 2026-03-30
---

# Phase 03.4 Plan 01: Homepage Original Landing Restore Summary

**Original landing-page homepage restored with the placeholder preview block, baseline feature-card copy, and route-level rollback coverage**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-30T13:32:36Z
- **Completed:** 2026-03-30T13:35:05Z
- **Tasks:** 2
- **Files modified:** 3

## Accomplishments
- Restored `packages/web/src/app/page.tsx` to the original landing-page baseline from `42a3af0`, including the original hero copy, CTA layout, placeholder preview, and feature-card messaging.
- Rewrote the route-level landing-page test to lock the restored homepage contract instead of the 03.3 static-product hero contract.
- Removed the obsolete `HeroPreview` component test so homepage success is no longer defined by a superseded preview component.

## Task Commits

Each task was committed atomically:

1. **Task 1: Lock the original landing-page baseline with a route test** - `3f33a2e` (test)
2. **Task 2: Restore the homepage route and retire the obsolete HeroPreview component test** - `96385a8` (fix)

## Files Created/Modified
- `packages/web/src/app/page.tsx` - Restores the homepage route to the original landing-page layout and copy.
- `packages/web/src/__tests__/app/landing-page.test.tsx` - Verifies the original homepage contract and blocks 03.3 copy from returning.
- `packages/web/src/__tests__/components/HeroPreview.test.tsx` - Deleted because the homepage no longer depends on this superseded component contract.

## Decisions Made
- Restore the homepage route directly rather than keeping `HeroPreview` in the homepage path.
- Remove the component-level `HeroPreview` homepage test rather than rewriting it for a route that no longer renders the component.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

- The local shell environment did not expose `pnpm`, so verification was run inside the `web` Docker container as requested.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- The homepage route and route-level regression are restored and ready for Wave 2 browser coverage.
- `packages/web/tests/e2e/homepage/homepage-hero-preview.spec.ts` still needs to be rewritten to the restored baseline before the phase can be closed.

---
*Phase: 03.4-homepage-original-landing-restore*
*Completed: 2026-03-30*
