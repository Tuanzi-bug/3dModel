---
phase: 02.1-freeform-stability-polish
plan: 03
subsystem: editor
tags: [viewport, r3f, gizmo, camera-controls]
requires:
  - phase: 02-freeform-builder
    provides: Viewport gizmo and freeform transform controls
provides:
  - The bottom-right viewport gizmo no longer crashes when clicked.
  - Orbit controls are registered as the default control context for gizmo integration.
  - Stale transform targets are cleared when selection is removed.
affects: [editor, viewport]
tech-stack:
  added: []
  patterns: [default orbit controls, stale selection cleanup]
key-files:
  created:
    - packages/web/tests/e2e/editor/viewport-gizmo.spec.ts
  modified:
    - packages/web/src/components/editor/ViewportCanvas.tsx
key-decisions:
  - "Gizmo interactions should rely on explicit default controls rather than implicit context."
patterns-established:
  - "Viewport selection cleanup clears stale transform targets whenever selection becomes null."
requirements-completed: [FIX-03]
duration: 7m
completed: 2026-03-30
---

# Phase 02.1 Plan 03 Summary

**Viewport gizmo clicks no longer throw null-control runtime errors**

## Accomplishments

- Registered `OrbitControls` as the default controls for the viewport so `GizmoViewport` can resolve its control context safely.
- Cleared stale selected groups when editor selection is removed, preventing old transform handles from lingering.
- Added a Playwright regression that clicks the bottom-right gizmo and asserts the `getTarget` / `minPolarAngle` errors never surface.

## Verification

- `docker compose run --rm e2e sh -lc 'pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/viewport-gizmo.spec.ts --project=chromium'`

## Notes

- Executed inline in the current worktree; no per-task commit was created.

---
*Phase: 02.1-freeform-stability-polish*
*Completed: 2026-03-30*
