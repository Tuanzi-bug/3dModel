---
phase: 02.1-freeform-stability-polish
plan: 01
subsystem: editor
tags: [freeform, duplicate, zustand, scene-graph]
requires:
  - phase: 02-freeform-builder
    provides: Duplicate/delete controls for freeform components
provides:
  - Duplicate actions now create typed `type-sequence` IDs for copied nodes.
  - The duplicated root node becomes selected immediately after copy.
  - Editor counters stay aligned with duplicate creation so subsequent IDs do not collide.
affects: [editor, properties-panel, duplication]
tech-stack:
  added: []
  patterns: [typed duplicate ids, duplicate-select continuity]
key-files:
  created: []
  modified:
    - packages/core/src/utils/scene-tree.ts
    - packages/web/src/stores/editor-store.ts
    - packages/web/src/__tests__/stores/editor-store.test.ts
key-decisions:
  - "Duplicate generation now shares the same `type-sequence` ID contract as direct component insertion."
patterns-established:
  - "When a duplicate is created, selection moves to the new root copy instead of staying on the source node."
requirements-completed: [FIX-01]
duration: 12m
completed: 2026-03-30
---

# Phase 02.1 Plan 01 Summary

**Duplicate actions now land on an immediately editable copy**

## Accomplishments

- Added a typed duplicate path in `packages/core/src/utils/scene-tree.ts` so copied subtrees receive deterministic `type-sequence` IDs instead of random IDs.
- Updated the editor store to select the duplicated root node and advance `typeCounters` during copy.
- Locked the regression with a store test that verifies selection continuity and counter updates after duplication.

## Verification

- `docker compose exec -T web sh -lc 'pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts src/__tests__/components/editor/ComponentPanel.test.tsx'`

## Notes

- Executed inline in the current worktree; no per-task commit was created.

---
*Phase: 02.1-freeform-stability-polish*
*Completed: 2026-03-30*
