---
phase: 02.1-freeform-stability-polish
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/editor/ViewportCanvas.tsx
  - packages/web/src/components/editor/Viewport.tsx
  - packages/web/tests/e2e/editor/viewport-gizmo.spec.ts
autonomous: true
requirements:
  - FIX-03
must_haves:
  truths:
    - Clicking the viewport coordinate-axis gizmo no longer throws runtime errors.
    - Gizmo interactions use a valid camera-control context instead of reading properties from `null`.
    - Selection and control cleanup do not leave stale viewport references behind.
  artifacts:
    - path: packages/web/src/components/editor/ViewportCanvas.tsx
      provides: Null-safe OrbitControls/GizmoHelper integration
      contains: makeDefault
    - path: packages/web/tests/e2e/editor/viewport-gizmo.spec.ts
      provides: Browser regression coverage for clicking the bottom-right gizmo without page errors
      contains: getTarget
  key_links:
    - from: packages/web/src/components/editor/ViewportCanvas.tsx
      to: packages/web/tests/e2e/editor/viewport-gizmo.spec.ts
      via: bottom-right gizmo click no longer emits page errors
      pattern: GizmoViewport
---

<objective>
Harden the viewport coordinate-axis gizmo so it stops crashing the editor at runtime.

Purpose: remove the bottom-right gizmo click crash before the editor surface gets any more complex.
Output: valid default controls for the gizmo, null-safe cleanup, and a browser regression spec that catches future crashes.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
@.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
@packages/web/src/components/editor/ViewportCanvas.tsx
@packages/web/src/components/editor/Viewport.tsx
@packages/web/tests/e2e/editor/freeform-builder.spec.ts
@packages/web/tests/e2e/editor/keyboard-shortcuts.spec.ts

<interfaces>
From `packages/web/src/components/editor/ViewportCanvas.tsx`:
```tsx
<OrbitControls ref={orbitControlsRef} />
<GizmoHelper alignment="bottom-right">
  <GizmoViewport />
</GizmoHelper>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make gizmo clicks safe by wiring a real default control context</name>
  <files>packages/web/src/components/editor/ViewportCanvas.tsx, packages/web/src/components/editor/Viewport.tsx, packages/web/tests/e2e/editor/viewport-gizmo.spec.ts</files>
  <read_first>
.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
packages/web/src/components/editor/ViewportCanvas.tsx
packages/web/src/components/editor/Viewport.tsx
packages/web/tests/e2e/editor/freeform-builder.spec.ts
  </read_first>
  <behavior>
    - Clicking the bottom-right gizmo no longer throws page-level runtime errors.
    - The gizmo reads from valid camera controls even after selection changes or deselection.
    - The viewport safely clears stale local refs when the selected object disappears.
  </behavior>
  <action>Add a focused Playwright regression first that opens the editor, clicks the bottom-right gizmo, and fails on `pageerror` if the current null-control crash reappears. In the viewport code, make `OrbitControls` the default control context or explicitly pass the correct controls into the gizmo path, and add cleanup/null guards so stale selection groups do not survive deselection or remounts. Do not paper over the issue by removing the gizmo entirely.</action>
  <acceptance_criteria>
    - `packages/web/src/components/editor/ViewportCanvas.tsx` contains `makeDefault` or an equivalent explicit control-context handoff for gizmo interaction.
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/viewport-gizmo.spec.ts --project=chromium` passes in Docker.
    - The new Playwright spec fails if `pageerror` captures the previous `getTarget` / `minPolarAngle` null-reference family of crashes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/editor/viewport-gizmo.spec.ts --project=chromium</automated>
  </verify>
  <done>The coordinate-axis gizmo behaves as a stable viewport control instead of a crash path.</done>
</task>

</tasks>

<verification>
Any "fix" that simply hides the crash behind a guard while leaving the gizmo non-functional should be treated as incomplete.
</verification>

<success_criteria>
- Gizmo clicks no longer crash the editor.
- The viewport keeps a valid camera-control context for bottom-right axis interactions.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-freeform-stability-polish/02.1-freeform-stability-polish-03-SUMMARY.md`
</output>
