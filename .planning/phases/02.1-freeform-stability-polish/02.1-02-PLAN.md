---
phase: 02.1-freeform-stability-polish
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/components/editor/ComponentPanel.tsx
  - packages/web/src/components/meshes/registry.ts
  - packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx
autonomous: true
requirements:
  - FIX-02
must_haves:
  truths:
    - Only currently supported component entries are interactive in the library.
    - `杆` and `层板` remain enabled, while the remaining entries are visibly disabled and cannot mutate the scene.
    - The support boundary is declared explicitly instead of being implied by accidental renderer/runtime behavior.
  artifacts:
    - path: packages/web/src/components/editor/ComponentPanel.tsx
      provides: Explicit enabled/disabled treatment for component-library entries
      contains: disabled
    - path: packages/web/src/components/meshes/registry.ts
      provides: Renderer support boundary used to keep UI and rendering aligned
      contains: ledStrip and backPanel are Phase 3
    - path: packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx
      provides: Regression coverage that unsupported entries cannot be activated
      contains: 暂未开放
  key_links:
    - from: packages/web/src/components/editor/ComponentPanel.tsx
      to: packages/web/src/components/meshes/registry.ts
      via: library affordances align with actual supported component rendering
      pattern: componentRegistry
---

<objective>
Gate the component library to the supported surface the user can actually trust today.

Purpose: stop unsupported entries from behaving like working actions before Phase 3 implements them properly.
Output: explicit support-state UI, disabled unsupported entries, and regression tests for the boundary.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
@.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
@packages/web/src/components/editor/ComponentPanel.tsx
@packages/web/src/components/meshes/registry.ts
@packages/web/src/components/editor/SceneRenderer.tsx

<interfaces>
From `packages/web/src/components/editor/ComponentPanel.tsx`:
```ts
const components: ComponentDef[] = [...]
```

From `packages/web/src/components/meshes/registry.ts`:
```ts
export const componentRegistry: Partial<Record<SceneNodeType, ComponentType<...>>> = { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Disable unsupported component-library entries and document the support boundary</name>
  <files>packages/web/src/components/editor/ComponentPanel.tsx, packages/web/src/components/meshes/registry.ts, packages/web/src/__tests__/components/editor/ComponentPanel.test.tsx</files>
  <read_first>
.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
packages/web/src/components/editor/ComponentPanel.tsx
packages/web/src/components/meshes/registry.ts
  </read_first>
  <behavior>
    - `杆` and `层板` remain enabled and continue to add nodes normally.
    - Remaining component entries stay visible but disabled with clear Chinese copy indicating they are not open yet.
    - Clicking a disabled entry does not mutate the scene or selection.
  </behavior>
  <action>Add a focused component-panel test first, then introduce an explicit support flag or catalog that drives the rendered state of each library entry. Do not rely on accidental renderer failures as the UI boundary. Update the panel so unsupported entries render as disabled buttons with consistent copy such as `暂未开放`, and ensure only `杆` and `层板` remain interactive for this phase. Keep the support boundary aligned with the current mesh/feature support rather than silently widening scope.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/ComponentPanel.test.tsx` passes.
    - `packages/web/src/components/editor/ComponentPanel.tsx` makes unsupported entries non-clickable.
    - Only the first two current library entries remain enabled by default.
    - Unsupported entries show explicit disabled-state copy in Chinese.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/components/editor/ComponentPanel.test.tsx</automated>
  </verify>
  <done>The component library reflects the real supported surface instead of pretending unfinished entries already work.</done>
</task>

</tasks>

<verification>
Treat any implementation that merely hides unsupported entries without making the support boundary explicit as a warning, and any implementation that still allows unsupported clicks as a blocker.
</verification>

<success_criteria>
- Users can trust that clickable library entries are actually supported today.
- Unsupported entries remain visible but clearly disabled instead of failing implicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-freeform-stability-polish/02.1-freeform-stability-polish-02-SUMMARY.md`
</output>
