---
phase: 02.1-freeform-stability-polish
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/utils/scene-tree.ts
  - packages/core/src/utils/id.ts
  - packages/web/src/stores/editor-store.ts
  - packages/web/src/components/editor/PropertiesPanel.tsx
  - packages/web/src/components/editor/NodeParamsEditor.tsx
  - packages/web/src/__tests__/stores/editor-store.test.ts
autonomous: true
requirements:
  - FIX-01
must_haves:
  truths:
    - Duplicating a selected component keeps the duplicated component selected and immediately editable in the properties panel.
    - Duplicate IDs stay compatible with the typed `type-sequence` contract instead of falling back to raw opaque IDs.
    - Duplicated nodes preserve the copied params and transforms while leaving the original node intact.
  artifacts:
    - path: packages/web/src/stores/editor-store.ts
      provides: Duplicate contract that selects the duplicated node and preserves typed IDs
      contains: duplicateNode
    - path: packages/core/src/utils/scene-tree.ts
      provides: Duplicate helper that no longer hardcodes raw `generateId()` IDs for editor-facing duplicates
      contains: duplicateNode
    - path: packages/web/src/__tests__/stores/editor-store.test.ts
      provides: Regression coverage for duplicate selection and edit continuity
      contains: switches selection to duplicated
  key_links:
    - from: packages/web/src/stores/editor-store.ts
      to: packages/web/src/components/editor/NodeParamsEditor.tsx
      via: `selectedNodeId` points at the new duplicate so the properties panel stays populated
      pattern: selectedNodeId
    - from: packages/core/src/utils/id.ts
      to: packages/web/src/stores/editor-store.ts
      via: typed duplicate IDs continue to parse into user-facing labels and counters
      pattern: parseNodeId
---

<objective>
Repair the duplicate flow so copied nodes remain valid editor citizens instead of drifting into an uneditable state.

Purpose: eliminate the post-Phase-2 regression where duplicating a component breaks property editing continuity.
Output: typed duplicate IDs, duplicate-root selection continuity, and store tests that lock the behavior in place.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
@.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
@packages/core/src/utils/scene-tree.ts
@packages/core/src/utils/id.ts
@packages/web/src/stores/editor-store.ts
@packages/web/src/components/editor/PropertiesPanel.tsx
@packages/web/src/components/editor/NodeParamsEditor.tsx
@packages/web/src/__tests__/stores/editor-store.test.ts

<interfaces>
From `packages/web/src/stores/editor-store.ts`:
```ts
duplicateNode: (id: string) => void
selectedNodeId: string | null
typeCounters: Record<string, number>
```

From `packages/core/src/utils/scene-tree.ts`:
```ts
export function duplicateNode(root: SceneNode, id: string): SceneNode
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make duplicate selection and IDs editor-safe again</name>
  <files>packages/core/src/utils/scene-tree.ts, packages/core/src/utils/id.ts, packages/web/src/stores/editor-store.ts, packages/web/src/components/editor/NodeParamsEditor.tsx, packages/web/src/__tests__/stores/editor-store.test.ts</files>
  <read_first>
.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
packages/core/src/utils/scene-tree.ts
packages/core/src/utils/id.ts
packages/web/src/stores/editor-store.ts
packages/web/src/components/editor/NodeParamsEditor.tsx
packages/web/src/__tests__/stores/editor-store.test.ts
  </read_first>
  <behavior>
    - Duplicating a selected node produces a new duplicate root ID that still conforms to the editor's typed ID contract.
    - The store selects the duplicate root immediately after duplication so the properties panel continues to show editable fields.
    - Copied params, transforms, and child structure are preserved on the duplicate.
  </behavior>
  <action>Expand the store tests first. Replace the current raw-ID duplication path with a duplicate flow that regenerates IDs through the typed counter strategy the editor already uses for new nodes. Either extend the core duplicate helper to accept an ID factory and return the duplicated root ID, or add a new helper with that contract, but do not leave editor-facing duplication on bare `nanoid()` IDs. Update the store so `duplicateNode()` selects the duplicate root after mutation, keeps `typeCounters` in sync, and leaves the original node untouched. Keep `NodeParamsEditor` compatible with the resulting typed IDs and labels.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts` passes.
    - `packages/web/src/__tests__/stores/editor-store.test.ts` explicitly covers duplicate selection continuity.
    - The duplicate path in `packages/core/src/utils/scene-tree.ts` no longer hardcodes editor-facing duplicate IDs with plain `generateId()`.
    - `packages/web/src/stores/editor-store.ts` updates `selectedNodeId` to the duplicated node after duplication.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts</automated>
  </verify>
  <done>Duplicated nodes remain selectable, labeled, and editable immediately after the duplicate action.</done>
</task>

</tasks>

<verification>
Treat any implementation that duplicates geometry but leaves the new node unselected or inconsistently labeled as a blocker.
</verification>

<success_criteria>
- The properties panel does not fall into an empty/invalid state after duplication.
- Duplicate IDs remain compatible with labels, counters, and future edits.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-freeform-stability-polish/02.1-freeform-stability-polish-01-SUMMARY.md`
</output>
