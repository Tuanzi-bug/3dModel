---
phase: 02.1-freeform-stability-polish
plan: 4
type: execute
wave: 2
depends_on:
  - 02.1-01
  - 02.1-02
  - 02.1-03
files_modified:
  - packages/core/src/utils/scene-preview.ts
  - packages/core/__tests__/scene-preview.test.ts
  - packages/web/src/app/dashboard/page.tsx
  - packages/web/tests/e2e/dashboard/dashboard.spec.ts
autonomous: true
requirements:
  - FIX-04
must_haves:
  truths:
    - Preview generation uses a stable x-axis-oriented projection rather than the current pseudo-isometric projection.
    - Template and saved-design previews remain deterministic from canonical `sceneGraph` data.
    - Dashboard cards continue rendering valid preview images after the projection change.
  artifacts:
    - path: packages/core/src/utils/scene-preview.ts
      provides: X-axis-oriented SVG preview generation
      contains: x-axis
    - path: packages/core/__tests__/scene-preview.test.ts
      provides: Regression coverage for deterministic preview output and x-axis projection behavior
      contains: data:image/svg+xml
    - path: packages/web/tests/e2e/dashboard/dashboard.spec.ts
      provides: Browser proof that preview images still render on dashboard cards
      contains: 预览图
  key_links:
    - from: packages/core/src/utils/scene-preview.ts
      to: packages/web/src/app/dashboard/page.tsx
      via: dashboard cards render the revised preview imagery without changing the public preview contract
      pattern: generateScenePreviewDataUrl
---

<objective>
Improve preview recognizability by switching the SVG generator to a more faithful x-axis-oriented projection.

Purpose: address the current preview quality complaint without replacing the existing deterministic preview pipeline.
Output: x-axis projection math, updated preview tests, and dashboard regression coverage.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
@.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
@packages/core/src/utils/scene-preview.ts
@packages/core/__tests__/scene-preview.test.ts
@packages/web/src/app/dashboard/page.tsx
@packages/web/tests/e2e/dashboard/dashboard.spec.ts

<interfaces>
From `packages/core/src/utils/scene-preview.ts`:
```ts
export function generateScenePreviewDataUrl(sceneGraph: SceneNode): string
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Re-project previews from an x-axis-oriented side view</name>
  <files>packages/core/src/utils/scene-preview.ts, packages/core/__tests__/scene-preview.test.ts, packages/web/src/app/dashboard/page.tsx, packages/web/tests/e2e/dashboard/dashboard.spec.ts</files>
  <read_first>
.planning/phases/02.1-freeform-stability-polish/02.1-CONTEXT.md
.planning/phases/02.1-freeform-stability-polish/02.1-RESEARCH.md
packages/core/src/utils/scene-preview.ts
packages/core/__tests__/scene-preview.test.ts
packages/web/src/app/dashboard/page.tsx
packages/web/tests/e2e/dashboard/dashboard.spec.ts
  </read_first>
  <behavior>
    - Preview geometry is laid out from a stable x-axis-oriented view so side-profile structure is easier to recognize.
    - The preview generator stays deterministic and continues to return SVG data URLs.
    - Dashboard template and saved-design cards continue to render the revised previews with no contract change at the call site.
  </behavior>
  <action>Extend the core preview tests first, then replace the current pseudo-isometric projection math with an x-axis-oriented side-view projection that primarily maps `z` to horizontal placement and `y` to vertical placement, while sizing silhouettes from actual component params. Keep the generator pure and deterministic. Update dashboard browser coverage so preview images are still visible and the revised projection does not regress card rendering.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/core exec vitest run __tests__/scene-preview.test.ts` passes.
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/dashboard/dashboard.spec.ts --project=chromium` passes in Docker.
    - `packages/core/src/utils/scene-preview.ts` no longer uses the old pseudo-isometric `x * 72 - z * 30` style projection as the primary layout strategy.
    - Template and saved-design previews continue to render on the dashboard with existing `src`/`alt` contracts.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/core exec vitest run __tests__/scene-preview.test.ts && pnpm --filter @3d-modeler/web exec playwright test tests/e2e/dashboard/dashboard.spec.ts --project=chromium</automated>
  </verify>
  <done>Dashboard previews become more truthful and recognizable without changing the overall preview delivery pipeline.</done>
</task>

</tasks>

<verification>
Treat any preview change that improves styling but does not materially change the projection toward an x-axis side view as incomplete.
</verification>

<success_criteria>
- Users can recognize shelf structure more easily from preview cards.
- The preview contract stays deterministic and compatible with current dashboard rendering.
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-freeform-stability-polish/02.1-freeform-stability-polish-04-SUMMARY.md`
</output>
