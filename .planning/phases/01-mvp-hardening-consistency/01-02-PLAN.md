---
phase: 01-mvp-hardening-consistency
plan: 2
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - packages/web/src/hooks/use-auto-save.ts
  - packages/web/src/components/editor/Header.tsx
  - packages/web/src/__tests__/hooks/use-auto-save.test.ts
  - packages/web/src/__tests__/components/editor/Header.test.tsx
  - packages/web/src/app/dashboard/page.tsx
  - packages/web/src/components/editor/StatusBar.tsx
  - packages/web/src/components/editor/EditorShell.tsx
  - packages/web/src/components/editor/ComponentPanel.tsx
  - packages/web/src/components/editor/PropertiesPanel.tsx
autonomous: true
requirements:
  - RELI-02
must_haves:
  truths:
    - The dashboard frames presets as quick-start entries, not as a deeper template-editing mode.
    - Save feedback stays inline near the primary action, is announced accessibly, and tells the user what to do next on failure.
    - The editor chrome remains usable on small screens without forcing horizontal page scroll.
  artifacts:
    - path: packages/web/src/app/dashboard/page.tsx
      provides: Approved preset-entry, empty-state, and destructive-copy contract
      contains: 从预置方案开始
    - path: packages/web/src/components/editor/Header.tsx
      provides: Inline accessible save action and status feedback
      contains: 保存设计
    - path: packages/web/src/hooks/use-auto-save.ts
      provides: Reliable save-status state machine for manual save and autosave
      contains: SaveStatus
    - path: packages/web/src/components/editor/EditorShell.tsx
      provides: Responsive shell layout that keeps the viewport primary
      contains: flex
    - path: packages/web/src/components/editor/StatusBar.tsx
      provides: Quiet source/status wording aligned to preset-entry terminology
      contains: 来源
  key_links:
    - from: packages/web/src/components/editor/Header.tsx
      to: packages/web/src/hooks/use-auto-save.ts
      via: manualSave and saveStatus
      pattern: 'useAutoSave'
    - from: packages/web/src/app/dashboard/page.tsx
      to: packages/web/src/app/editor/new/page.tsx
      via: preset card links to `/editor/new?template=...`
      pattern: '/editor/new\\?template='
    - from: packages/web/src/components/editor/EditorShell.tsx
      to: packages/web/src/components/editor/ComponentPanel.tsx
      via: responsive panel stacking on small screens
      pattern: 'ComponentPanel'
---

<objective>
Align the dashboard and editor chrome with the approved preset-entry UI contract without widening Phase 1 scope.

Purpose: make the hardened persistence model legible to users through the exact copy, accessibility, and responsive behavior approved in the UI-SPEC.
Output: contract-aligned save feedback, dashboard wording, and mobile-safe editor layout.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
@.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
@.planning/phases/01-mvp-hardening-consistency/01-UI-SPEC.md
@docs/design-system.md
@packages/web/src/app/dashboard/page.tsx
@packages/web/src/components/editor/Header.tsx
@packages/web/src/components/editor/StatusBar.tsx
@packages/web/src/components/editor/EditorShell.tsx

<interfaces>
From `packages/web/src/hooks/use-auto-save.ts`:
```ts
export type SaveStatus = 'idle' | 'saving' | 'saved' | 'error'

export function useAutoSave() {
  return { manualSave, saveStatus }
}
```

From `packages/core/src/templates/registry.ts`:
```ts
export function getTemplateById(id: string): TemplateDefinition | undefined
export function getTemplateMetadata(): TemplateMetadata[]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make save feedback inline, accessible, and failure-aware</name>
  <files>packages/web/src/hooks/use-auto-save.ts, packages/web/src/components/editor/Header.tsx, packages/web/src/__tests__/hooks/use-auto-save.test.ts, packages/web/src/__tests__/components/editor/Header.test.tsx</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
.planning/phases/01-mvp-hardening-consistency/01-UI-SPEC.md
packages/web/src/hooks/use-auto-save.ts
packages/web/src/components/editor/Header.tsx
packages/web/src/__tests__/hooks/use-auto-save.test.ts
  </read_first>
  <behavior>
    - Manual save and autosave report `saving`, `saved`, and `error` states consistently.
    - Non-2xx responses are treated as save failures instead of false positives.
    - The primary header action uses the exact copy `保存设计` / `保存中...` / `已保存` / `保存失败，请重试`, announces status changes with `aria-live="polite"`, and uses announced inline failure guidance near the save action.
  </behavior>
  <action>Update the hook tests first, then implement the hook and header changes. Per D-07, D-08, D-09, and the approved UI-SPEC, keep save feedback inline instead of adding toast-heavy behavior. In `use-auto-save.ts`, fail on network errors and non-OK HTTP responses, preserve the debounced/manual-save contract, and expose a stable `SaveStatus` surface. In `Header.tsx`, keep undo/redo secondary, make `保存设计` the primary anchor, add an inline live-region/status element, and render the exact next-step copy from the UI-SPEC when save fails.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/hooks/use-auto-save.test.ts src/__tests__/components/editor/Header.test.tsx` passes.
    - `packages/web/src/components/editor/Header.tsx` contains `保存设计`, `aria-live`, and an announced inline failure state.
    - `packages/web/src/hooks/use-auto-save.ts` checks `response.ok` before setting `saveStatus` to `saved`.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/hooks/use-auto-save.test.ts src/__tests__/components/editor/Header.test.tsx</automated>
  </verify>
  <done>Users get precise, accessible save feedback at the primary action, and failed saves no longer masquerade as success.</done>
</task>

<task type="auto">
  <name>Task 2: Reframe the dashboard around preset-entry language and small-screen hierarchy</name>
  <files>packages/web/src/app/dashboard/page.tsx</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-UI-SPEC.md
docs/design-system.md
packages/web/src/app/dashboard/page.tsx
  </read_first>
  <action>Per D-01, D-02, and the approved copy contract, change the creation section from `从模板开始` to `从预置方案开始`, keep it visually ahead of saved designs, and update the empty-state heading/body plus delete confirmation to the exact approved wording. Adjust the preset and saved-design grids to a single-column-first mobile layout that does not introduce horizontal scrolling, but preserve the existing Tailwind + Inter baseline and do not add an empty-canvas entry.</action>
  <acceptance_criteria>
    - `rg -n "从预置方案开始|还没有设计|先从一个预置方案开始|删除设计：删除后无法恢复" packages/web/src/app/dashboard/page.tsx` finds the approved copy.
    - `packages/web/src/app/dashboard/page.tsx` uses mobile-first grid classes that collapse to one column on the smallest breakpoint.
    - `pnpm --filter @3d-modeler/web build` passes after the dashboard changes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web build</automated>
  </verify>
  <done>The dashboard visually and verbally presents presets as a quick-start path, not as a separate template-editing product mode.</done>
</task>

<task type="auto">
  <name>Task 3: Remove template-centric chrome and make the editor shell responsive</name>
  <files>packages/web/src/components/editor/StatusBar.tsx, packages/web/src/components/editor/EditorShell.tsx, packages/web/src/components/editor/ComponentPanel.tsx, packages/web/src/components/editor/PropertiesPanel.tsx</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
.planning/phases/01-mvp-hardening-consistency/01-UI-SPEC.md
packages/web/src/components/editor/StatusBar.tsx
packages/web/src/components/editor/EditorShell.tsx
packages/web/src/components/editor/ComponentPanel.tsx
packages/web/src/components/editor/PropertiesPanel.tsx
packages/core/src/templates/registry.ts
  </read_first>
  <action>Keep the viewport as the primary surface while making the shell comply with the responsive contract. At small widths, stack or collapse the side panels so the page does not overflow horizontally; at `lg` and above, preserve the current panel + viewport layout. Update the status/footer terminology from `模板` to `来源` / `预置方案`, show a human-readable preset name when `templateId` exists, wrap footer content for narrow screens, and do not revive `TemplateParamsEditor.tsx` or any template-regeneration affordance.</action>
  <acceptance_criteria>
    - `rg -n "来源|预置方案|flex-col|lg:flex-row|flex-wrap|overflow-x-hidden" packages/web/src/components/editor/StatusBar.tsx packages/web/src/components/editor/EditorShell.tsx packages/web/src/components/editor/ComponentPanel.tsx packages/web/src/components/editor/PropertiesPanel.tsx` finds the new wording and responsive classes.
    - No editor-shell breakpoint forces page-level horizontal scrolling through fixed side-panel widths.
    - `pnpm --filter @3d-modeler/web build` passes after the shell changes.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web build</automated>
  </verify>
  <done>The editor chrome matches the approved preset-entry language and remains usable on 375px through desktop widths without widening scope.</done>
</task>

</tasks>

<verification>
Run the save-feedback Vitest suites and a production build. Treat any remaining `模板`-first copy in the dashboard or status chrome as a phase-blocking contract violation.
</verification>

<success_criteria>
- Save states are accessible, inline, and failure-aware.
- The dashboard uses preset-entry wording and approved empty/error/destructive copy.
- The editor shell no longer relies on desktop-only fixed side panels to remain usable.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-hardening-consistency/01-mvp-hardening-consistency-02-SUMMARY.md`
</output>
