---
phase: 01-mvp-hardening-consistency
plan: 3
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - packages/web/src/__tests__/api/designs-route.test.ts
  - packages/web/tests/e2e/dashboard/dashboard.spec.ts
  - packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts
autonomous: true
requirements:
  - RELI-01
  - RELI-02
  - RELI-03
must_haves:
  truths:
    - The design API has automated coverage that fails if sceneGraph create/get/patch continuity regresses.
    - There is at least one browser regression path proving preset entry -> edit -> save -> reopen continuity.
    - Copy regressions back to template-centric language on the dashboard or save flow are caught automatically.
  artifacts:
    - path: packages/web/src/__tests__/api/designs-route.test.ts
      provides: API regression coverage for design persistence routes
      contains: PATCH
    - path: packages/web/tests/e2e/dashboard/dashboard.spec.ts
      provides: Dashboard copy and quick-start smoke coverage
      contains: 从预置方案开始
    - path: packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts
      provides: End-to-end continuity smoke path for save and reopen
      contains: 保存设计
  key_links:
    - from: packages/web/src/__tests__/api/designs-route.test.ts
      to: packages/web/src/app/api/designs/[id]/route.ts
      via: direct handler invocation with mocked Prisma/auth
      pattern: 'PATCH'
    - from: packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts
      to: packages/web/src/app/editor/new/page.tsx
      via: preset-entry create flow
      pattern: '/editor/new\\?template='
    - from: packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts
      to: packages/web/src/components/editor/Header.tsx
      via: manual save action and save-state assertions
      pattern: '保存设计'
---

<objective>
Leave Phase 1 with a dependable automated regression path for the hardened editor baseline.

Purpose: lock the persistence and UI-contract work in place before Phase 2 expands the editor surface.
Output: targeted API regression tests plus a browser continuity smoke path that exercises the user-visible save/reopen loop.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
@.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
@.planning/phases/01-mvp-hardening-consistency/01-UI-SPEC.md
@.planning/codebase/TESTING.md
@packages/web/src/app/api/designs/route.ts
@packages/web/src/app/api/designs/[id]/route.ts
@packages/web/tests/e2e/dashboard/dashboard.spec.ts
@packages/web/tests/e2e/editor/editor.spec.ts
@packages/web/playwright.config.ts

<interfaces>
From `packages/web/src/app/api/designs/[id]/route.ts`:
```ts
export async function GET(
  _request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
)

export async function PATCH(
  request: NextRequest,
  { params }: { params: Promise<{ id: string }> },
)
```

From `packages/web/playwright.config.ts`:
```ts
export default defineConfig({
  testDir: './tests/e2e',
  use: { baseURL: process.env.BASE_URL || 'http://localhost:3000' },
  projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }],
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add direct API regression coverage for design continuity</name>
  <files>packages/web/src/__tests__/api/designs-route.test.ts</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
.planning/codebase/TESTING.md
packages/web/src/app/api/designs/route.ts
packages/web/src/app/api/designs/[id]/route.ts
packages/web/src/__tests__/api/auth.test.ts
  </read_first>
  <behavior>
    - `POST /api/designs` stores a sceneGraph snapshot and returns parsed JSON.
    - `GET /api/designs/[id]` and `PATCH /api/designs/[id]` round-trip the persisted sceneGraph without mutating it into template defaults.
    - Ownership/auth failures stay explicit so the core flow cannot silently read or write another user's design.
  </behavior>
  <action>Create a node-environment Vitest file that invokes the route handlers directly with mocked Prisma and mocked `withAuth()` responses. Cover one happy-path create/get/patch round trip plus at least one forbidden or not-found case. Keep the tests focused on persistence continuity and response payload shape; do not introduce a real database or browser dependency here.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/api/designs-route.test.ts` passes.
    - The test suite asserts parsed `sceneGraph` output for create/get/patch responses rather than only schema validation.
    - At least one auth or ownership failure case remains covered.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/api/designs-route.test.ts</automated>
  </verify>
  <done>There is a fast automated regression suite proving the design routes preserve persisted scene data and guard access correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace shallow browser smoke with a save-and-reopen continuity scenario</name>
  <files>packages/web/tests/e2e/dashboard/dashboard.spec.ts, packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
.planning/phases/01-mvp-hardening-consistency/01-UI-SPEC.md
packages/web/playwright.config.ts
packages/web/tests/e2e/dashboard/dashboard.spec.ts
packages/web/tests/e2e/editor/editor.spec.ts
packages/web/src/app/dashboard/page.tsx
packages/web/src/app/editor/new/page.tsx
packages/web/src/app/editor/[id]/page.tsx
  </read_first>
  <action>Keep the existing stub-driven Playwright model, but replace `waitForTimeout`-style smoke checks with deterministic locator and route assertions. Update `dashboard.spec.ts` to assert the approved preset-entry wording. Add `packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts` with one Chromium scenario that starts on `/dashboard`, opens a preset entry, waits for the initial create request, changes user-visible scene state through the existing editor controls, triggers `保存设计`, captures the PATCH payload, reopens the same design, and asserts the persisted state and approved save/source copy are still present. Avoid deep 3D drag automation; use the component panel, property editor, status bar, URL changes, and intercepted route payloads to prove continuity.</action>
  <acceptance_criteria>
    - `packages/web/tests/e2e/dashboard/dashboard.spec.ts` asserts `从预置方案开始`.
    - `packages/web/tests/e2e/editor/save-reopen-continuity.spec.ts` proves create -> edit -> save -> reopen continuity without `page.waitForTimeout(...)`.
    - `pnpm --filter @3d-modeler/web exec playwright test tests/e2e/dashboard/dashboard.spec.ts tests/e2e/editor/save-reopen-continuity.spec.ts --project=chromium` passes against the running app.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec playwright test tests/e2e/dashboard/dashboard.spec.ts tests/e2e/editor/save-reopen-continuity.spec.ts --project=chromium</automated>
  </verify>
  <done>Phase 1 has a user-visible browser regression path that fails if save/reopen continuity or preset-entry copy regresses.</done>
</task>

</tasks>

<verification>
Run the API Vitest suite first, then the focused Playwright specs against the running web app defined by `packages/web/playwright.config.ts`. Any surviving `waitForTimeout`-based assertions in the new continuity path are a blocker.
</verification>

<success_criteria>
- The design routes have direct automated coverage for sceneGraph round trips and guard failures.
- A browser smoke test proves a user can start from a preset, edit, save, reopen, and continue without continuity drift.
- The phase exits with automated evidence for RELI-01, RELI-02, and RELI-03.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-hardening-consistency/01-mvp-hardening-consistency-03-SUMMARY.md`
</output>
