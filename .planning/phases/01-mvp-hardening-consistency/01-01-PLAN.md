---
phase: 01-mvp-hardening-consistency
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/web/src/stores/editor-store.ts
  - packages/web/src/app/editor/new/page.tsx
  - packages/web/src/app/editor/[id]/page.tsx
  - packages/web/src/__tests__/stores/editor-store.test.ts
  - packages/web/src/__tests__/app/editor-pages.test.tsx
autonomous: true
requirements:
  - RELI-01
  - RELI-02
must_haves:
  truths:
    - Reopened designs resume from the persisted scene graph instead of regenerated template defaults.
    - Starting a new design from a preset entry clears stale editor metadata before the design is created.
    - Loading an existing design seeds deterministic node-ID counters so adding more nodes after reopen does not reuse existing IDs.
  artifacts:
    - path: packages/web/src/stores/editor-store.ts
      provides: Scene-first load/reset behavior for the editor store
      contains: loadDesign
    - path: packages/web/src/app/editor/new/page.tsx
      provides: Preset-entry bootstrap and first-save creation flow
      contains: /api/designs
    - path: packages/web/src/app/editor/[id]/page.tsx
      provides: Persisted design hydration from the API
      contains: loadDesign
    - path: packages/web/src/__tests__/app/editor-pages.test.tsx
      provides: Route bootstrap regression coverage for create/open flows
      contains: NewEditorPage
  key_links:
    - from: packages/web/src/app/editor/[id]/page.tsx
      to: packages/web/src/stores/editor-store.ts
      via: loadDesign with API response sceneGraph
      pattern: 'loadDesign\('
    - from: packages/web/src/app/editor/new/page.tsx
      to: packages/web/src/stores/editor-store.ts
      via: resetScene then applyTemplate before POST
      pattern: 'applyTemplate'
    - from: packages/web/src/stores/editor-store.ts
      to: packages/web/src/components/editor/ComponentPanel.tsx
      via: generateNodeId/typeCounters after load
      pattern: 'generateNodeId'
---

<objective>
Make the persisted scene snapshot the only source of truth for entering and re-entering the editor.

Purpose: close the hidden state-drift bugs that currently make save/reopen behavior depend on template defaults or stale in-memory metadata.
Output: stable store hydration, deterministic editor entry routes, and focused regression tests for create/open continuity.
</objective>

<execution_context>
@/Users/yay/.codex/get-shit-done/workflows/execute-plan.md
@/Users/yay/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
@.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
@packages/web/src/stores/editor-store.ts
@packages/web/src/app/editor/new/page.tsx
@packages/web/src/app/editor/[id]/page.tsx

<interfaces>
From `packages/web/src/stores/editor-store.ts`:
```ts
export interface EditorState {
  sceneGraph: SceneNode
  selectedNodeId: string | null
  mode: 'template' | 'freeform'
  templateId: string | null
  templateParams: TemplateParams | null
  typeCounters: Record<string, number>
  designId: string | null
  designName: string
  applyTemplate(templateId: string, params: TemplateParams): void
  loadDesign(opts: {
    id: string
    name: string
    sceneGraph: SceneNode
    templateId: string | null
    templateParams: TemplateParams | null
  }): void
  resetScene(): void
  generateNodeId(type: string): string
}
```

From `packages/core/src/schemas/design.ts`:
```ts
export const createDesignSchema = z.object({
  name: z.string().min(1).max(255),
  templateId: z.string().nullable().optional(),
  sceneGraph: sceneNodeSchema,
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make store hydration scene-first and counter-safe</name>
  <files>packages/web/src/stores/editor-store.ts, packages/web/src/__tests__/stores/editor-store.test.ts</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
packages/web/src/stores/editor-store.ts
packages/web/src/__tests__/stores/editor-store.test.ts
packages/core/src/utils/id.ts
  </read_first>
  <behavior>
    - `loadDesign()` keeps the persisted `sceneGraph` intact and does not recover template defaults when `templateParams` is absent.
    - `loadDesign()` clears transient selection/history and seeds `typeCounters` from the loaded node IDs so later `addNode()` calls create new IDs.
    - `resetScene()` clears stale `designId`, `designName`, `mode`, `templateId`, `templateParams`, and counters before a fresh preset entry is applied.
  </behavior>
  <action>Expand `packages/web/src/__tests__/stores/editor-store.test.ts` first, then update the store to make those cases pass. Per D-01, D-02, D-03, and D-04, keep presets only as an entry path: `applyTemplate()` may still generate the initial scene, but `loadDesign()` must trust the saved `sceneGraph` and treat `templateId` as source metadata rather than a reason to regenerate. Add a helper inside `editor-store.ts` that derives `typeCounters` from existing node IDs, use it from both `loadDesign()` and any reset/new-design path, and do not add template-parameter persistence to the design contract.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts` passes with cases covering `loadDesign()` continuity, counter seeding, and `resetScene()` metadata clearing.
    - `packages/web/src/stores/editor-store.ts` no longer rebuilds `templateParams` from `template?.defaultParams` inside `loadDesign()`.
    - Adding a node after `loadDesign()` does not reuse an ID already present in the loaded scene.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/stores/editor-store.test.ts</automated>
  </verify>
  <done>Store hydration is scene-first, reset paths are clean, and reopened designs can continue editing without hidden template or ID drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stabilize preset-entry and reopen page bootstrapping around the store contract</name>
  <files>packages/web/src/app/editor/new/page.tsx, packages/web/src/app/editor/[id]/page.tsx, packages/web/src/__tests__/app/editor-pages.test.tsx</files>
  <read_first>
.planning/phases/01-mvp-hardening-consistency/01-CONTEXT.md
.planning/phases/01-mvp-hardening-consistency/01-RESEARCH.md
packages/web/src/app/editor/new/page.tsx
packages/web/src/app/editor/[id]/page.tsx
packages/web/src/stores/editor-store.ts
packages/web/src/app/api/designs/route.ts
packages/web/src/app/api/designs/[id]/route.ts
  </read_first>
  <behavior>
    - Visiting `/editor/new?template=...` clears stale editor state, applies the preset once, creates the design exactly once, and replaces the URL only after POST succeeds.
    - Visiting `/editor/[id]` hydrates the store from the GET payload and redirects to `/dashboard` on load failures.
    - Neither route injects template defaults back into reopened state.
  </behavior>
  <action>Add `packages/web/src/__tests__/app/editor-pages.test.tsx` before changing the pages. Per D-01 and D-04, keep the preset entry convenience path but make it deterministic: `new/page.tsx` should rely on the strengthened `resetScene()` contract, avoid stale `designId` short-circuiting, and move to `/editor/{id}` only after the creation response returns an ID. `packages/web/src/app/editor/[id]/page.tsx` should pass the API scene snapshot directly into `loadDesign()` and keep the existing redirect fallback for missing or forbidden designs. Do not add empty-canvas behavior or broaden the Phase 1 editor surface.</action>
  <acceptance_criteria>
    - `pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/editor-pages.test.tsx` passes.
    - `packages/web/src/app/editor/new/page.tsx` contains a single create-request path guarded against stale in-memory design state.
    - `packages/web/src/app/editor/[id]/page.tsx` hydrates the store from the GET response without reconstructing template params.
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @3d-modeler/web exec vitest run src/__tests__/app/editor-pages.test.tsx</automated>
  </verify>
  <done>The two editor entry routes consistently boot into the same persisted-scene editing model.</done>
</task>

</tasks>

<verification>
Run the two targeted Vitest suites for the store and editor entry pages. Treat any remaining use of template-default recovery during reopen as a blocker.
</verification>

<success_criteria>
- A saved design reopens into its persisted scene without recovered template defaults or duplicate future node IDs.
- Starting a new design from `/dashboard` cannot inherit a stale `designId` or stale counters from the previous editor session.
- The editor entry routes have automated tests proving the create/open bootstrap logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-mvp-hardening-consistency/01-mvp-hardening-consistency-01-SUMMARY.md`
</output>
